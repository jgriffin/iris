# Work log

<!-- Append-only. Newest at bottom. -->

<!-- STATUS · snapshot, rewritten each block · full board in BOARD.md -->
📋 **M9 — Unified shell defined; M8 landed on `main`** (M1–M8 all ✅ on main — M8 P1–P5 incl. freeze-from-live shipped, P6 dataset tie-in 🗓 backlog). M9 = one shared model + min-confidence across all three modes + a left pane driving model selection / navigation / per-page Open…/RECENT, collapsing the iOS tabs + macOS `Videos|Images` toggle. 5 phases (P1 reliability quick wins · P2 shared model store · P3 left-pane shell · P4 Capture joins · P5 simplify); demo-wiring only — the `Iris` library is left alone.
👉 Next: **Start M9 — cut `m9-unified-shell` off `main`, then P1** (macOS importer collision A1, gate Image picker till loaded A6, bookmark resolve logging A5; independent, mergeable alone). → [`BOARD.md`](./BOARD.md)
<!-- /STATUS -->

---

## 2026-05-21
- Did: migrated to the `plans/` workflow described in [`WORKFLOW.md`](./WORKFLOW.md). Moved `BRIEF.md` from repo root into `plans/`. Pulled "Open design questions" and "Milestone path" out of [`../CLAUDE.md`](../CLAUDE.md) — questions now live in [`QUESTIONS.md`](./QUESTIONS.md), milestones in [`BRIEF.md`](./BRIEF.md). Created [`DECISIONS.md`](./DECISIONS.md) capturing the M0 exploration verdicts.
- Did: deleted `explorations/RECOMMENDATIONS-PRIOR-ART.md` — judged an anti-pattern after auditing the rollup against the per-arc files. Updated the four references that pointed at it.
- Did: enriched [`DECISIONS.md`](./DECISIONS.md) entries from one-liners to paragraph-per-decision with clickable links. Updated [`WORKFLOW.md`](./WORKFLOW.md)'s `DECISIONS.md` template and pruning guidance to match: entries should give enough context to act without opening the reference, blank-line-separated, file refs as markdown links.
- Note: existing exploration folders (`prior-projects/`, `swift-ecosystem/`, `project-shape-and-tooling/`, `runtime-pipeline-architecture/`, `display-pipeline-architecture/`) don't follow the `YYYY-MM-DD-topic/` naming from [`WORKFLOW.md`](./WORKFLOW.md). Leaving them as-is to preserve links; the dated convention applies to new explorations going forward.
- Next: M1 (`IrisCapture`) plans lock once the two M1-blocking opens in [`QUESTIONS.md`](./QUESTIONS.md) are settled — package-layout fork and the `Detector` stateful-conformer shape. Sidecar format can wait until M5.

---

- Did: shipped M1 in six phases — scaffolding ([`Package.swift`](../Package.swift), Swift 6.2 / iOS+macOS 26 / strict concurrency on; full tooling chain: `.swift-format` + `.swiftlint.yml` + `.githooks/pre-commit` + `.github/workflows/ci.yml` + DocC catalog + Git LFS), shared core types (`Frame`, `Source`, `SourceState`, `SourceError`, `MediaType`, `SourceKind`, `PixelFormat`, `AssetID`, `MockSource`), the iOS-only Capture stack (`CaptureSession` actor on a custom `DispatchSerialQueue` executor, `SampleBufferRouter`, `PreviewSource`/`PreviewTarget`, `AVCapturePreviewSource`, `PreviewView`, `CameraPreview`), tests (Swift Testing — MockSource on iOS+macOS, CaptureSession smoke gated to physical-device iOS), and the iOS demo app at [`Apps/IrisDemo-iOS/`](../Apps/IrisDemo-iOS/) generated via xcodegen ([`Apps/project.yml`](../Apps/project.yml)).
- Did: smoke test passed on physical iPhone — camera preview renders, frame timestamps stream to `os.Logger` at host rate, rotation reactively re-orients the preview via `AVCaptureDevice.RotationCoordinator` KVO routed through a new `PreviewSource.previewAngles: AsyncStream<CGFloat>`.
- Did: kept developer-specific values out of git via `Shared.xcconfig` (committed) `#include?`'ing `Local.xcconfig` (gitignored). `Local.xcconfig.template` checked in for bootstrap. Scrubbed an earlier accidental commit of `dev.griff.iris.demo` from the entire history with `git filter-repo` (no remote — safe local rewrite).
- Learned: the locked `@MainActor public protocol PreviewTarget` design in [`display-pipeline-architecture/RECOMMENDATIONS.md`](../explorations/display-pipeline-architecture/RECOMMENDATIONS.md) §Type sketches doesn't satisfy Swift 6 strict concurrency — the `@MainActor`-pinned existential can't be sent into a `Task`. Working amendment: drop `@MainActor` from the protocol declaration, push it onto `setSession` and onto `PreviewSource.connect(to:)`. Caller (`CameraPreview.makeUIView`) is already `@MainActor` via `UIViewRepresentable`, so the internal `Task` hop drops out entirely.
- Learned: `swift build` on the macOS host never compiles `#if os(iOS)` files, so it doesn't catch strict-concurrency bugs in the Capture folder. `xcodebuild -destination 'generic/platform=iOS'` (or a real `xcodebuild build`) is required to validate the iOS-gated subset. Worth adding to CI's test job.
- Learned: xcodegen auto-fills `PRODUCT_BUNDLE_IDENTIFIER = <bundleIdPrefix>.<target-name>` as an INLINE target setting when the target has no explicit value — sitting at higher precedence than xcconfig. Fix: drop `bundleIdPrefix` from `project.yml` so xcconfig wins cleanly.
- Learned: `CaptureSession` conforms via `@preconcurrency Source` because the actor's `var state` can't synchronously satisfy the protocol's nonisolated `get`. Clean long-term fix is making the `Source` protocol declare `var state: SourceState { get async }`. Punted to M2.
- Learned: `MockSource.start()` yields all supplied frames synchronously, and `.bufferingNewest(1)` means a single consumer only sees the LAST frame. Tests had to run one frame per session to assert ordering. Consider letting `MockSource` take a `bufferingPolicy:` parameter in M2.
- Learned: scriptable Xcode project generation via xcodegen + checked-in `.xcodeproj` is the right answer for a one-shot demo bootstrap, even though [`project-shape-and-tooling/RECOMMENDATIONS.md`](../explorations/project-shape-and-tooling/RECOMMENDATIONS.md) §2 deferred it. Manual Xcode-UI walkthrough is unnecessary friction when `project.yml` is 30 lines.
- Deferred to M2:
    - `TODO M1+` markers in `CaptureSession.configureSession(for:)` — interruption recovery via `AVCaptureSession.wasInterruptedNotification` / `interruptionEndedNotification` with the NextLevel-scar 100ms settle delay.
    - `SampleBufferRouter` tags every `Frame` orientation as `.up`; per-frame angle lookup against `rotationCoordinator` is plumbed but not invoked. Profile in M2 before turning it on.
    - Make `Source.state` async on the protocol so `CaptureSession` doesn't need `@preconcurrency` on the conformance.
    - Add a `bufferingPolicy:` parameter to `MockSource`.
- Deferred amendment to [`display-pipeline-architecture/RECOMMENDATIONS.md`](../explorations/display-pipeline-architecture/RECOMMENDATIONS.md): the locked `@MainActor public protocol PreviewTarget` shape doesn't pass Swift 6 strict concurrency. As-shipped design is the working version; update the recommendation doc in a follow-up.
- Next: M2 — `IrisDetection` (Vision adapter, body-pose or object detection) + `IrisOverlay` (`DetectionLayer` SwiftUI `Canvas` with `ResultStore` + `TimelineView`, `NormalizedGeometryConverting`). End-to-end live demo on iOS extends the existing `Apps/IrisDemo-iOS` app. Locked design in [`runtime-pipeline-architecture/RECOMMENDATIONS.md`](../explorations/runtime-pipeline-architecture/RECOMMENDATIONS.md) + [`display-pipeline-architecture/RECOMMENDATIONS.md`](../explorations/display-pipeline-architecture/RECOMMENDATIONS.md).

---

- Did: M2 kickoff — created [`plans/features/`](./features/) as the home for milestone & big-feature plans, registered the convention in [`WORKFLOW.md`](./WORKFLOW.md), and wrote [`plans/features/M2.md`](./features/M2.md) — the 7-phase plan for `IrisDetection` + `IrisOverlay` with M1-deferred items folded in.
- Did: Closed three [`QUESTIONS.md`](./QUESTIONS.md) entries — sidecar dataset format (removed; M5+ concern), package layout fork (stay single-target; the existing 2026-05-20 decision covers it), and `Detector` stateful-conformer shape (protocol stays `Sendable`-only; stateful conformers wrap state in an internal `actor`). The Detector close drew on existing recommendations in [`explorations/swift-ecosystem/RECOMMENDATIONS.md`](../explorations/swift-ecosystem/RECOMMENDATIONS.md) and [`explorations/prior-projects/RECOMMENDATIONS.md`](../explorations/prior-projects/RECOMMENDATIONS.md) — no new exploration needed.
- Did: Shipped M2 Phase 1 — detection scaffolding & core types. New files: `Sources/Iris/Detection/{Detector,Detection,TimestampedDetections,MockDetector}.swift`. `Detector` protocol matches the swift-ecosystem RECOMMENDATIONS shape (four members: `availability`, `modelIdentifier`, `prewarm()`, `detect(in:)`); top-level `DetectorAvailability` enum. `Detection` is a value-type struct with normalized bounding box, label, confidence, optional keypoints, optional `Mask` placeholder, source model ID.
- Did: Resolved M1-deferred `Source.state` `async` cleanup — [`Sources/Iris/Source.swift`](../Sources/Iris/Source.swift) requirement is now `{ get async }`; [`Sources/Iris/Capture/CaptureSession.swift`](../Sources/Iris/Capture/CaptureSession.swift) no longer needs `@preconcurrency` on its `Source` conformance. `MockSource` needed no code change.
- Did: 3 new Swift Testing cases in `Tests/IrisTests/Detection/DetectorTests.swift`; `swift build` clean (macOS + iOS `xcodebuild -destination 'generic/platform=iOS'`), `swift test` 6/6 passing.
- Learned: Swift forbids nested types in protocol extensions, so the swift-ecosystem RECOMMENDATIONS spelling `var availability: Detector.Availability { get }` isn't literally expressible (PFM's `LanguageModelBackend.Availability` only works because `LanguageModelBackend` is a struct). Shipped as top-level `DetectorAvailability`; amended the recommendation doc inline with the rationale.
- Learned: A non-async getter satisfies an `async` protocol requirement. Useful — `MockSource.state` didn't need touching when the `Source` protocol moved to `{ get async }`.
- Deferred to later in M2:
    - Mask payload shape on `Detection.Mask` — `// TODO M2+:` markers; placeholder `width`/`height` only. Phase 3's first segmentation adapter pins down the pixel-payload shape.
    - `MockSource.bufferingPolicy:` parameter — still pending; slated for Phase 1 or 2, will land alongside `ResultStore` tests that benefit from multi-frame replay.
    - 5th `Detector` member `detectStream(in:)` for trajectory / temporal streaming — additive default-impl per swift-ecosystem RECOMMENDATIONS; lands when the first temporal conformer needs it.
- Next: M2 Phase 2 — `ResultStore`. `@MainActor @Observable` sorted ring buffer keyed by `CMTime`; `append` / `clear` / `lookup(at:)` with staleness thresholds (500ms live / 2s playback). Pure value-type tests, no SwiftUI / camera in the test path. Folds in `MockSource.bufferingPolicy:` so multi-frame replay tests are tractable.

---

- Did: Shipped M2 Phase 2 — `ResultStore` and the M1-deferred `MockSource.bufferingPolicy:`. New files: [`Sources/Iris/Overlay/ResultStore.swift`](../Sources/Iris/Overlay/ResultStore.swift) (`@MainActor @Observable public final class`, sorted ring buffer keyed by `CMTime`, `append` / `clear` / `lookup(at:stale:)`, 500ms-live / 2s-playback staleness thresholds as public knobs), and [`Tests/IrisTests/Overlay/ResultStoreTests.swift`](../Tests/IrisTests/Overlay/ResultStoreTests.swift) (8 cases under one `@MainActor @Suite`). `MockSource.init` now takes `bufferingPolicy: AsyncStream<Frame>.Continuation.BufferingPolicy = .bufferingNewest(1)` — default preserves the existing `Source` contract; tests pass `.unbounded` when they need every frame to reach a single consumer. New [`Tests/IrisTests/Capture/MockSourceTests.swift`](../Tests/IrisTests/Capture/MockSourceTests.swift) case covers that. `swift test` 15/15, `xcodebuild -destination 'generic/platform=iOS' build` clean under Swift 6 strict concurrency.
- Learned: the locked `ResultStore` sketch in [`../explorations/display-pipeline-architecture/RECOMMENDATIONS.md`](../explorations/display-pipeline-architecture/RECOMMENDATIONS.md) §Type sketches compiled essentially as-written — `@MainActor` isolation gives `@Observable`'s generated tracking code a coherent home and the public surface stays clean. No amendment needed (unlike M1's `PreviewTarget` scar).
- Learned: swapped the sketch's `firstIndex(where:)` for a shared `upperBound` (C++ `std::upper_bound` semantics) primitive used by both `append` (insertion point preserving FIFO for equal timestamps) and `lookup` (rightmost `timestamp <= displayTime`). Locked rule M2 #2 mandates O(log n) lookup; same primitive cheaply makes the `append` search step O(log n) too.
- Learned: `var state: SourceState { get async }` is satisfiable by a plain synchronous getter, so `MockSource.state == .idle` test sites needed no `await` rewrite. M1-deferred protocol cleanup completed cleanly.
- Learned: the default `liveStalenessThreshold` (500ms) is permissive enough that timestamps in the `1..40 / 60` range used in tests get suppressed by staleness unless you pass a tighter `stale:`. The per-call `stale:` parameter is doing real work in the test surface, not a nicety — kept public.
- Deferred: no new items added. Phase 1's outstanding deferred list still stands (`Detection.Mask` pixel-payload shape pending first segmentation adapter; `detectStream(in:)` 5th `Detector` member additive when first temporal conformer needs it).
- Next: M2 Phase 3 — Vision detector adapter under `Sources/Iris/Detection/Vision/`. First target likely `VNDetectFaceRectanglesRequest` or `VNDetectHumanBodyPoseRequest` (stateless, so the locked `Sendable`-only `Detector` shape is trivially satisfied). `DetectorPipeline.swift` for chaining. Fixture-based test runs the detector over a sample clip in `Tests/IrisTests/Fixtures/` — first real use of the Git LFS-tracked fixture lane.

---

## 2026-05-22

- Did: Shipped M2 Phase 3 — Vision rectangle-detection adapter + `DetectorPipeline`. New files: [`Sources/Iris/Detection/Vision/VisionRectanglesDetector.swift`](../Sources/Iris/Detection/Vision/VisionRectanglesDetector.swift) (struct, stateless `Detector` conformer wrapping Apple's new value-type Vision Swift API), [`Sources/Iris/Detection/DetectorPipeline.swift`](../Sources/Iris/Detection/DetectorPipeline.swift) (parallel detector fan-out via `withThrowingTaskGroup` with indexed slots so input order is preserved), and tests under [`Tests/IrisTests/Detection/`](../Tests/IrisTests/Detection/) — `VisionRectanglesDetectorTests.swift` (hermetic CoreGraphics-drawn rect + pipeline equivalence) and `VisionRectanglesDetectorFixtureTests.swift` (clip-based via `AVAssetReader`, asserts ≥5/10 frames produce ≥1 detection with confidence ≥0.5 — observed 10/10). First LFS-tracked content: [`Tests/IrisTests/Fixtures/clipboard-blank-page.mp4`](../Tests/IrisTests/Fixtures/clipboard-blank-page.mp4) (Pexels, Artem Podrez, Pexels License, 1280×720 / 30 fps / ~9.5 s / 3.0 MB) plus [`Tests/IrisTests/Fixtures/README.md`](../Tests/IrisTests/Fixtures/README.md) documenting per-fixture provenance + license. Two atomic commits — `8f734bb` (hermetic code) and `a83e7aa` (fixture + clip test); binary-content commit shipped only after explicit user approval of the candidate clip. `swift test` 20/20, `xcodebuild -destination 'generic/platform=iOS' build` clean.
- Learned: Picked Apple's new value-type Vision Swift API (`DetectRectanglesRequest`, `RectangleObservation`, `@available(macOS 15.0, iOS 18.0, *)`) over the classic `VN…` NSObject API. Both request and observation conform to `Sendable` natively, `perform(on:orientation:)` is `async throws` directly (no `withCheckedThrowingContinuation` bridge), and tunables are plain `var Float` on a struct. Zero strict-concurrency escape hatches needed at the adapter boundary — `@preconcurrency import Vision` is not required. The [`DECISIONS.md`](./DECISIONS.md) "strict-concurrency escape hatches" entry stays theoretical for built-in detectors; the first Core ML / `VNCoreMLRequest` adapter will re-test that claim.
- Learned: `DetectRectanglesRequest.maximumAspectRatio` defaults to `0.5` — counterintuitively narrow. Only half-aspect-or-narrower rects pass through, so naïvely instantiating the request and pointing it at a square sheet of paper returns zero detections. Tests pass `maximumAspectRatio: 1.0` to accept square-ish rectangles; the default is flagged as a footgun in the adapter's doc comment.
- Learned: `prewarm()` is a defensible no-op for Vision built-ins. The new Swift API exposes no explicit prewarm hook, and running a throwaway request against a synthetic buffer would shift first-frame cost rather than remove it. The `Detector.prewarm()` contract permits an empty implementation. Revisit if first-frame latency is visible in the M2 Phase 7 demo.
- Learned: `Package.swift` already declared `resources: [.process("Fixtures")]` on the test target from M1 setup, so `Bundle.module.url(forResource:withExtension:)` works for fixture lookup with no new package wiring.
- Learned: `swift-format --strict` (run by the pre-commit hook) enforces 100-column line length independent of `.swiftlint.yml`, where `line_length` is disabled. The two linters have independent rulesets and the hook chains both. Worth aligning the configs so a `swiftlint`-passing edit doesn't fail the `swift-format` gate later.
- Deferred: Align `.swift-format` and `.swiftlint.yml` rule sets so the two linters don't disagree on line length / formatting style. Low priority — easy to fix in-place when a commit gets blocked.
- Deferred to later in M2: `Detection.Mask` pixel-payload shape (first segmentation adapter pins it); `Detector.detectStream(in:)` 5th member (additive when first temporal conformer needs it). Both carried over from Phase 1.
- Next: M2 Phase 4 — `NormalizedGeometryConverting` protocol + `PreviewLayerConverter` + `PlayerLayerConverter` under `Sources/Iris/Overlay/`. [`features/M2.md`](./features/M2.md) §Risks flags that `PreviewLayerConverter` needs `@MainActor` access analogous to M1's `PreviewTarget` scar — land the [`../explorations/display-pipeline-architecture/RECOMMENDATIONS.md`](../explorations/display-pipeline-architecture/RECOMMENDATIONS.md) amendment in the same commit so the recommendation doc reflects what shipped. Unit tests cover aspect-fit math directly so `PlayerLayerConverter` ships with coverage even though M3 adds end-to-end playback.

---

- Did: Shipped M2 Phase 4 — `NormalizedGeometryConverting` + `PreviewLayerConverter` + `PlayerLayerConverter`. Three new source files under [`Sources/Iris/Overlay/`](../Sources/Iris/Overlay/) plus [`Tests/IrisTests/Overlay/PlayerLayerConverterTests.swift`](../Tests/IrisTests/Overlay/PlayerLayerConverterTests.swift) (7 cases covering identity, Y-flip top-/bottom-left quadrants, vertical letterbox, horizontal pillarbox, and the two corner-point conversions). `PreviewLayerConverter` is `#if os(iOS)`-gated and delegates to `AVCaptureVideoPreviewLayer.layerRectConverted(fromMetadataOutputRect:)` / `layerPointConverted(fromCaptureDevicePoint:)`. `PlayerLayerConverter` carries an `AVPlayerLayer?` and routes through internal `static func convert(normalizedRect:videoRect:)` / `convert(normalizedPoint:videoRect:)` helpers — the held layer is unused on the math path, which is what makes the static helpers unit-testable without an AVF layer. Commit `80ef0f8`. `swift test` 27/27, `xcodebuild -destination 'generic/platform=iOS' build` clean.
- Learned: **The locked `NormalizedGeometryConverting` protocol shape compiled under Swift 6 strict concurrency on first try — no [`../explorations/display-pipeline-architecture/RECOMMENDATIONS.md`](../explorations/display-pipeline-architecture/RECOMMENDATIONS.md) amendment needed.** The `@MainActor` pressure that bit M1's `PreviewTarget` is absorbed *inside* `PreviewLayerConverter` via `MainActor.assumeIsolated { ... }`, relying on the fact that `DetectionLayer`'s `Canvas` body runs on MainActor by SwiftUI contract. The protocol stays `Sendable`-only. Concrete converters carry `@unchecked Sendable` with documented invariants (layer captured once at init; reads-only) per the [`DECISIONS.md`](./DECISIONS.md) escape-hatches doctrine. The risk flagged in [`features/M2.md`](./features/M2.md) §Risks turned out not to materialize.
- Learned: `PreviewLayerConverter`'s `viewRect(forNormalized:in:)` doesn't need its `in: videoRect` argument — `AVCaptureVideoPreviewLayer.layerRectConverted(fromMetadataOutputRect:)` already accounts for letterbox/aspect internally per §26 of the recommendations doc. Kept the parameter for protocol symmetry; documented the divergence inline.
- Learned: `@preconcurrency import AVFoundation` was needed in both converter implementation files, but neither type leaks AVF through its public surface — the import is contained at the seam.
- Deferred: `PreviewLayerConverter` end-to-end coverage requires a live `AVCaptureSession`, so it lands in the **Phase 7** physical-device smoke alongside the existing capture smoke. Math-only `PlayerLayerConverter` ships with full coverage this commit; the AVF-side will get exercised in M3 playback.
- Deferred (carryover, unchanged): `Detection.Mask` pixel-payload shape; `Detector.detectStream(in:)` 5th member; `.swift-format` ↔ `.swiftlint.yml` config alignment.
- Open for Phase 5: `DetectionLayer`'s default `converter:` argument shape. [`../explorations/display-pipeline-architecture/RECOMMENDATIONS.md`](../explorations/display-pipeline-architecture/RECOMMENDATIONS.md) §Type sketches defaults to `PlayerLayerConverter()`, but a converter without a backing `AVPlayerLayer` can't do anything useful at runtime — the `nil`-layer path is purely for the unit-test surface. Options: (a) drop the default and require callers to supply a converter, (b) ship a no-op `IdentityConverter` as the default for testing/preview scaffolding, (c) keep the `PlayerLayerConverter()` default and document that real use requires `.with(layer:)`. Land in Phase 5 when the call site exists.
- Next: M2 Phase 5 — `DetectionLayer` view in [`Sources/Iris/Overlay/`](../Sources/Iris/Overlay/). SwiftUI `Canvas` driven by `TimelineView(.animation(minimumInterval: 1.0/60))`, `.drawingGroup()`, `.allowsHitTesting(false)`. Reads `resultStore.lookup(at: displayTime)`, runs each `Detection.boundingBox` through an injected `NormalizedGeometryConverting`, draws stroked boxes. Resolves the default-converter open question above before locking the public init signature.

---

- Did: Shipped M2 Phase 5 — `DetectionLayer<Converter: NormalizedGeometryConverting>: View` in [`Sources/Iris/Overlay/DetectionLayer.swift`](../Sources/Iris/Overlay/DetectionLayer.swift) (139 lines) plus [`Tests/IrisTests/Overlay/DetectionLayerTests.swift`](../Tests/IrisTests/Overlay/DetectionLayerTests.swift) (2 cases — init smoke + MainActor-bound construction with synthetic `TimestampedDetections`). Generic over the converter (not existential `any`) for clean `Sendable`. `TimelineView(.animation(minimumInterval: 1.0/60))` wraps a single `Canvas` with `.drawingGroup()` + `.allowsHitTesting(false)`; the body samples `displayTime = CMClockGetTime(CMClockGetHostTimeClock())`, calls `store.lookup(at:stale:)`, and draws stroked rects + label backplates per detection. Hardcoded styling — `OverlayStyle` ships in Phase 6. Commit `3a15d06`. `swift test` 29/29, `xcodebuild -destination 'generic/platform=iOS' build` clean.
- Did: Resolved the Phase 4 open question — **dropped the `converter:` default arg** on `DetectionLayer.init`. The recommendations sketch defaulted to `PlayerLayerConverter()`, but a converter with no backing layer can't do useful runtime work; the call site has to be explicit. Same commit amended [`../explorations/display-pipeline-architecture/RECOMMENDATIONS.md`](../explorations/display-pipeline-architecture/RECOMMENDATIONS.md) §Type sketches + §M2 item 6 with inline `_Amendment 2026-05-22:_` notes (matching Phase 1's `DetectorAvailability` precedent in [`../explorations/swift-ecosystem/RECOMMENDATIONS.md`](../explorations/swift-ecosystem/RECOMMENDATIONS.md)).
- Learned: `TimelineView.Context.date` is wall-clock `Date` — *not* the right `displayTime` source. `Frame.timestamp` from `AVCaptureVideoDataOutput` is in the host-clock reference frame of `CMSampleBuffer.presentationTimeStamp`. The honest read is `CMClockGetTime(CMClockGetHostTimeClock())` directly inside the Canvas body. M3 playback will need a different read (`AVPlayer.currentTime`) — likely a second `init` overload or a `@Binding<CMTime>` override, called out as an M3 open below.
- Learned: `GraphicsContext.ResolvedText.measure(in:)` doesn't tolerate `CGSize(width: .infinity, ...)` — Swift/CoreFoundation overload ambiguity. Use `CGFloat.greatestFiniteMagnitude`. Separately, a 4-arg `CGRect(...)` literal with mixed arithmetic on the y-component blew Swift 6's type-checker budget; split components into pre-computed `CGFloat` locals to unstick it. Both are pure structural fixes, no design impact, but worth knowing as Canvas-body footguns.
- Open for Phase 6: (1) `OverlayStyle` shape — per-class color map and stroke width are floor; whether the label format is a closure / `LocalizedStringKey` / dedicated struct is the open knob. (2) Whether the hardcoded label backplate (semi-transparent black + white 11pt semibold) gets exposed via `OverlayStyle` or stays hardcoded. (3) `#Preview` cases need synthetic `Detection` arrays + a static `videoRect` — `MockDetector` is one option, a `[TimestampedDetections]` literal is another. (4) Static HTML reference for box rendering per CLAUDE.md "favorite pattern" — co-located in `Sources/Iris/Overlay/` showing box rendering at multiple sizes / letterbox configurations.
- Open for M3 (carried, sharpened): `displayTime` plumbing for playback. M2's host-clock read is correct for live capture; M3 will need an override path so the playback consumer can pipe `AVPlayer.currentTime` through. Probably a second `DetectionLayer.init` overload taking `@Binding<CMTime>` rather than a generalized parameter on the existing one — keeps the live-capture call site terse. May want pre-emptive consideration in Phase 6 so adding it after `OverlayStyle` doesn't churn the init twice.
- Deferred (carryover, unchanged): `Detection.Mask` pixel-payload shape; `Detector.detectStream(in:)` 5th member; `.swift-format` ↔ `.swiftlint.yml` config alignment; `PreviewLayerConverter` end-to-end coverage (Phase 7 physical-device smoke).
- Next: M2 Phase 6 — `OverlayStyle` + `#Preview` cases + co-located static HTML reference. Resolve the four Phase 6 opens above; sharpen the M3 `displayTime` override path *before* committing `OverlayStyle` so the init reshape happens once.

---

- Did: Shipped M2 Phase 6 — [`Sources/Iris/Overlay/OverlayStyle.swift`](../Sources/Iris/Overlay/OverlayStyle.swift) (public `Sendable` struct with closure-based `strokeColor: (String) -> Color` + `labelFormat: (Detection) -> String`, plus `strokeWidth`, `labelTextColor`, `labelBackgroundColor`, `labelFont`; defaults preserve Phase 5's hardcoded cyan-stroke + white-on-black-0.6-backplate look), updated [`DetectionLayer`](../Sources/Iris/Overlay/DetectionLayer.swift) to read all styling from `style:` and to absorb the M3 `displayTime` override path *pre-emptively* via `displayTimeSource: @Sendable () -> CMTime = { CMClockGetTime(CMClockGetHostTimeClock()) }` — so M3 playback callers pass `{ player.currentTime() }` without a second init overload. Two `#Preview` cases (default + custom style) driven by a synthetic 4-detection fixture covering centered / edge-clipped / low-confidence / empty-label boxes. Co-located static HTML reference at [`Sources/Iris/Overlay/box-rendering.html`](../Sources/Iris/Overlay/box-rendering.html) with five scenes (three full-frame sizes + pillarbox + letterbox) using the same fixture, per CLAUDE.md's favorite-pattern instinct — opens directly via `open <file>`, no build step. 5 new `OverlayStyle` tests + 1 new `DetectionLayer` test exercising `displayTimeSource`. Commit `a6bfc45`. `swift test` 35/35, `xcodebuild -destination 'generic/platform=iOS' build` clean.
- Did: Resolved the four Phase 5 opens and the M3 displayTime open as briefed — label format = closure, backplate exposed via `OverlayStyle`, `#Preview` data = `[TimestampedDetections]` literal, single co-located HTML reference, M3 override = closure parameter on the existing init (no second overload). All five calls landed without contradiction; the init reshape happened once.
- Learned: SwiftUI `Color` and `Font` are both `Sendable` under iOS / macOS 26 strict concurrency — `OverlayStyle` ships as plain `Sendable`, no `@unchecked` escape hatch. The pattern of storing a closure for per-class color lookup survived strict concurrency with just `@Sendable @escaping` on the parameter.
- Learned: `Package.swift` flags raw `.html` files under a `.target` source root as unhandled resources and fails the build. Workaround: explicit `exclude: ["Overlay/box-rendering.html"]` on the target keeps the HTML co-located with the code it visualizes while keeping it out of the SwiftPM build graph. The favorite-pattern intent (file ships in the repo, opens directly) is preserved.
- Learned: A stored `displayTime: CMTime` value (the recommendations sketch's pre-amendment shape) would be read once at view construction — not per `TimelineView` tick — and would freeze. The closure shape (`displayTimeSource: @Sendable () -> CMTime`) is the right primitive for a per-tick read. Amended [`../explorations/display-pipeline-architecture/RECOMMENDATIONS.md`](../explorations/display-pipeline-architecture/RECOMMENDATIONS.md) §Type sketches inline with `_Amendment 2026-05-22 (Phase 6):_` capturing the rationale.
- Deferred (carryover, unchanged): `Detection.Mask` pixel-payload shape; `Detector.detectStream(in:)` 5th member; `.swift-format` ↔ `.swiftlint.yml` config alignment; `PreviewLayerConverter` end-to-end coverage (Phase 7).
- Next: M2 Phase 7 — end-to-end smoke on physical iOS device. Wire the live pipeline into [`Apps/IrisDemo-iOS`](../Apps/IrisDemo-iOS/): drive `CaptureSession.frames` into a `VisionRectanglesDetector` task, `append` timestamped results to a `ResultStore`, stack `DetectionLayer` over `CameraPreview` in the demo view (with `PreviewLayerConverter` constructed from the preview layer). Exit criteria: detection boxes overlay correctly at host frame rate on a real device, `PreviewLayerConverter`'s AVF-side path gets its first end-to-end exercise (Phase 4's tests covered math only), and `displayTimeSource`'s default host-clock read keeps live boxes registered with the preview.

---

- Did: Shipped M2 Phase 7 — end-to-end live smoke. Wired [`Apps/IrisDemo-iOS/ContentView.swift`](../Apps/IrisDemo-iOS/ContentView.swift) to stack [`DetectionLayer`](../Sources/Iris/Overlay/DetectionLayer.swift) over [`CameraPreview`](../Sources/Iris/Capture/CameraPreview.swift), with `CaptureSession.frames` driving a `VisionRectanglesDetector` (`minimumAspectRatio: 0.3`, `maximumAspectRatio: 1.0`, `minimumSize: 0.1`, `minimumConfidence: 0.7`, `label: "rect"`) whose results land in a MainActor `ResultStore` keyed by `frame.timestamp`. Added an additive `onPreviewLayerReady: @escaping @MainActor (AVCaptureVideoPreviewLayer) -> Void = { _ in }` hook to `CameraPreview` so the demo can hand the live AVF layer to a `PreviewLayerConverter` (default is a no-op so existing M1 callers + the `MockPreviewSource` `#Preview` keep working). On-device smoke on a physical iPhone (iOS 26): preview oriented correctly, detection boxes register with subjects ("close enough to ship" verdict). `swift test` 35/35; `xcodebuild -scheme Iris -destination 'generic/platform=iOS' build` and the `IrisDemo-iOS` demo-target build both clean. **Closes M2.**
- Did: Fixed a latent M1 rotation bug surfaced by the on-device smoke. Diagnostic logging showed AVF set `previewLayer.connection?.videoRotationAngle` to the correct 90° initially, then the M1 stream plumbing overwrote it with 0 — because [`CaptureSession.configureSession`](../Sources/Iris/Capture/CaptureSession.swift) created its `AVCaptureDevice.RotationCoordinator` with `previewLayer: nil`, and `videoRotationAngleForHorizonLevelPreview` only compensates for the layer's interface orientation when the coordinator is initialized with the actual layer. Relocated preview-side rotation ownership into [`PreviewView`](../Sources/Iris/Capture/PreviewView.swift) (the MainActor site that holds the layer): `setSession(_:)` now spins up a local `RotationCoordinator` bound to `self.previewLayer` plus the device extracted from `session.inputs.first`, observes `videoRotationAngleForHorizonLevelPreview`, and applies it directly to `previewLayer.connection`. Stripped the now-redundant stream plumbing — `PreviewSource.previewAngles` requirement gone from the protocol, `AVCapturePreviewSource.publishPreviewAngle` / `finishAngleStream` deleted, `PreviewView.observePreviewAngles` deleted, `CaptureSession.previewRotationObservation` deleted. Capture-side rotation (data-output connection) stays with `CaptureSession`'s coordinator (still `previewLayer: nil`, which is correct for the capture-side property).
- Learned: `AVCaptureDevice.RotationCoordinator` with `previewLayer: nil` returns 0 from `videoRotationAngleForHorizonLevelPreview` regardless of device orientation in portrait — the preview-angle property *requires* the layer reference to apply layer-orientation compensation. M1's LOG entry claiming "rotation reactively re-orients the preview" was right for rotation *change* events (KVO fired on device rotation), but wrong about initial orientation — the bug had been latent since M1. The correct architecture co-locates the preview-side `RotationCoordinator` with the layer it observes, matching Apple's AVCam sample. Capture-side rotation is independent and stays with the capture actor.
- Learned: For diagnosing a layered SwiftUI/AVF pipeline, a synthetic-data ticker (here: append a hardcoded `Detection` to `ResultStore` at the host-clock timestamp every 100ms) + a HUD overlay + targeted `os.Logger` lines isolated both the overlay-mounting question ("does `DetectionLayer` draw anything?") and the rotation question ("does the connection's `videoRotationAngle` stick?") in a single device run. Worth keeping in the toolkit — the favorite-pattern instinct from CLAUDE.md applied to runtime state, not just static visuals. Removed before close to keep the demo clean.
- Learned: Vision rectangle detection on a live 30 fps stream is visibly jittery at `minimumConfidence: 0.5` — many low-confidence candidate rects flicker frame-to-frame. `0.7` is "close enough" for the Phase 7 smoke; further filtering (`maximumObservations: 1` to keep only the strongest, raise `minimumSize`) and temporal smoothing remain options for any downstream use case that needs cleaner boxes (e.g. doc-scanner ergonomics).
- Deferred: Detection-jitter polish (smoothing, lower max-observations, tighter size threshold). Demo accepted at current tuning; revisit alongside the first real downstream use case.
- Deferred: `.resizeAspectFill` vs `.resizeAspect` interaction with `PreviewLayerConverter`'s `layerRectConverted` math — not isolated because rotation took priority and the boxes look right enough at `.resizeAspectFill`. Revisit if the geometry feels off in a future smoke.
- Deferred: `PreviewSource.previewAngles` public-API removal is a breaking change for any external Iris consumer. Pre-v1 library with one in-tree consumer — fine to ship, but worth noting if Iris ever gets external dependents before v1.
- Deferred (carryover, unchanged): `Detection.Mask` pixel-payload shape; `Detector.detectStream(in:)` 5th member; `.swift-format` ↔ `.swiftlint.yml` config alignment.
- Next: **M2 closes.** M3 is playback parity on macOS — `AVAssetReader`-backed `Frame` stream, scrubber, `PlayerLayerConverter` end-to-end exercise, the `@Binding<CMTime>`-style `displayTimeSource` overlay knob exercised live. M2 Phase 6 baked the closure-based `displayTimeSource` in pre-emptively so M3 callers wire `{ player.currentTime() }` without touching `DetectionLayer`'s init.

---

- Did: Opened M3 planning. Drafted [`features/M3.md`](./features/M3.md) — six phases (`PlaybackSource` core → controls + seek/step → `PlaybackView` → `Scrubber` → `IrisDemo-macOS` end-to-end → iOS playback parity smoke). Folded two M2 carryovers into M3 explicitly: `SampleBufferRouter` per-frame orientation (Phase 1, since playback frames need the orientation answer too) and `AVCaptureSession` interruption recovery (Phase 6, since the iOS demo extending into a playback mode is the natural venue to smoke-test it).
- Did: Locked the M2-flagged "`AVAssetReader` vs `AVPlayerItemVideoOutput`" open question: **`AVPlayerItemVideoOutput`-backed `PlaybackSource` for M3** (real-time playback + scrubbing is the M3 deliverable; `AVAssetReader`'s sequential-decode shape is wrong for that). Deferred `AVAssetReader`-backed prefetch/cache source — likely M5 alongside dataset workflows, or earlier if the displayTime divergence work below wants it as an enabler. The [`BRIEF.md`](./BRIEF.md) §IrisPlayback "AVAssetReader-backed" line is a scope note, not a locked architecture decision; M3.md supersedes it. Left BRIEF untouched since the note was scoping intent rather than mechanism.
- Did: Landed a new `[open]` entry in [`QUESTIONS.md`](./QUESTIONS.md) tracking the `displayTime` semantic divergence between capture (host clock) and playback (asset time), and the exact-equality assumption baked into `ResultStore.lookup(at:)`. Per-source contract holds (playback's `Frame.timestamp` *is* asset time; detector inherits) but skipped frames / decoder jitter / post-scrub re-emit will break exact match in practice. Eventual direction: best-effort temporal match — adaptive `stale:` window, or interpolation between the two nearest `TimestampedDetections`. The deferred `AVAssetReader`-backed prefetch cache is a candidate enabler (decoder runs ahead so lookups land exactly). Not blocking M3; tracked so it doesn't get lost.
- Learned: The M2-deferred carryovers (`SampleBufferRouter` orientation; capture-session interruption recovery) live conceptually inside M3 even though M3's main theme is playback. Folding them into M3 phases rather than treating them as orphaned capture-side debt keeps related work atomic — orientation gets one resolution across both subsystems, and interruption recovery gets exercised in the same demo where playback-as-fallback might one day be the actual user-visible behavior.
- Deferred: `AVAssetReader`-backed `OfflineSource` / prefetch-cache source — likely M5, or earlier if the displayTime best-effort-match work wants it.
- Deferred (carryover, unchanged): `Detection.Mask` pixel-payload shape; `Detector.detectStream(in:)` 5th member; `.swift-format` ↔ `.swiftlint.yml` config alignment; detection-jitter polish; `.resizeAspectFill` vs `.resizeAspect` interaction; `PreviewSource.previewAngles` API-removal note.
- Next: M3 Phase 1 — `PlaybackSource` core. `AVPlayer` + `AVPlayerItemVideoOutput`-backed `Source` conformer at `Sources/Iris/Playback/PlaybackSource.swift`. `frames: AsyncStream<Frame>` driven by `CADisplayLink` (iOS) / `CVDisplayLink` (macOS) polling `copyPixelBuffer(forItemTime:itemTimeForDisplay:)`. Timestamps from `AVPlayerItem.currentTime()` — document the per-source `Frame.timestamp` semantics on [`Frame.swift`](../Sources/Iris/Frame.swift) and re-test `ResultStore` staleness. Fixture-based test reuses the M2 LFS clip and asserts monotonic-`CMTime` ordering + frame count within ±1 of `frameRate × duration`. Includes the M2-deferred `SampleBufferRouter` per-frame orientation resolution.

---

- Did: Shipped M3 Phase 1 — `PlaybackSource` core. New files: [`Sources/Iris/Playback/PlaybackSource.swift`](../Sources/Iris/Playback/PlaybackSource.swift) (`Source` conformer wrapping `AVPlayer` + `AVPlayerItemVideoOutput`; `frames: AsyncStream<Frame>` with `.bufferingNewest(1)`; `play()` / `pause()` / `stop()` / `invalidate()` mapped onto `SourceState`; `@unchecked Sendable + NSLock + documented invariant` per the escape-hatch doctrine) and [`Tests/IrisTests/Playback/PlaybackSourceTests.swift`](../Tests/IrisTests/Playback/PlaybackSourceTests.swift) (fixture-based, reuses `clipboard-blank-page.mp4` — asserts monotonic-`CMTime` ordering and that `Frame.timestamp` is asset-time, not host-clock). Doc comment on [`Frame.swift`](../Sources/Iris/Frame.swift)'s `timestamp` documents per-source semantics (capture = host clock, playback = asset time, mock = test-supplied). Commit `39ebf10`. `swift test` 37/37, `swift build` + `xcodebuild -scheme Iris -destination 'generic/platform=iOS' build` both clean.
- Did: Resolved the M2-deferred `SampleBufferRouter` per-frame orientation question by removing the dead plumbing. The `rotation: @Sendable () async -> CGFloat` closure was wired from [`CaptureSession.configureSession`](../Sources/Iris/Capture/CaptureSession.swift) → [`SampleBufferRouter.init`](../Sources/Iris/Capture/SampleBufferRouter.swift) → stored property, but `captureOutput(_:didOutput:from:)` never read it — frames went out with `orientation: .up` regardless. The real source of truth is `AVCaptureConnection.videoRotationAngle`, set + KVO-tracked from `RotationCoordinator.videoRotationAngleForHorizonLevelCapture` via the M2 Phase 7 rotation fix. Stripped the closure, the call-site, and the orphaned `currentRotation()` helper; inline comment in `SampleBufferRouter` documents the cleanup so the question doesn't re-surface.
- Learned: **Deviation from the brief — punted `CADisplayLink`/`CVDisplayLink` integration to Phase 3.** Both display-link APIs need a host view (`CADisplayLink(target:selector:)` on iOS, `NSView.displayLink(target:selector:)` on macOS 14+). Phase 1 ships before `PlaybackView` exists, so a host-view-bound tick source isn't available yet. Solved by introducing a `PlaybackTickDriver` protocol with `TaskTickDriver` (production, `Task.sleep`-based at a nominal 60 Hz) and `ManualTickDriver` (test, explicit `await tick()`). Phase 3's `PlaybackView` vends a real display-link-backed driver, swapped via the protocol without touching `PlaybackSource`'s public API. The protocol abstraction generalizes the brief's "hidden init knob for tests" suggestion — production *also* uses the injected driver. Cost: real display-link integration moves from Phase 1 to Phase 3. Benefit: `PlaybackSource` is fully unit-testable without AVF tick simulation, and the platform-uniform driver code path means no `#if os(iOS)` fork in `PlaybackSource` itself. Acceptable trade given Phase 3 already owns the view-side AVF wiring.
- Learned: `AVPlayerItemVideoOutput.init(pixelBufferAttributes: [String: Any]?)` warns under Swift 6 strict concurrency (`[String: Any]` isn't `Sendable`). The newer typed `init(outputSettings: [String: any Sendable]?)` / typed `CVPixelBufferAttributes` initializer is `Sendable`-clean and is what we use. The legacy initializer should be considered closed for new code.
- Learned: A `Source.stop()` → `pause()` / `Source.invalidate()` → terminate-the-stream split keeps the "quiet but alive" path distinct from the "session over" path. Worth carrying into M3 Phase 2 when `seek`/`step` get added — they belong to the "alive" arm and shouldn't pun on the terminating arm.
- Deferred: Real `CADisplayLink` (iOS) / `CVDisplayLink` (macOS) tick driver — lands in Phase 3 alongside `PlaybackView`, swapped via the `PlaybackTickDriver` protocol without changes to `PlaybackSource`'s public API. Production `TaskTickDriver` at 60 Hz is the placeholder.
- Deferred (carryover, unchanged): `Detection.Mask` pixel-payload shape; `Detector.detectStream(in:)` 5th member; `.swift-format` ↔ `.swiftlint.yml` config alignment; detection-jitter polish; `.resizeAspectFill` vs `.resizeAspect` interaction; `PreviewSource.previewAngles` API-removal note; `AVAssetReader`-backed `OfflineSource` / prefetch-cache (M5 candidate; eventual enabler for the displayTime best-effort-match work tracked in [`QUESTIONS.md`](./QUESTIONS.md)); `AVCaptureSession` interruption recovery (M3 Phase 6).
- Next: M3 Phase 2 — playback controls + seek / step. `PlaybackSource.seek(to: CMTime)` (frame-accurate via `AVPlayer.seek(to:toleranceBefore: .zero, toleranceAfter: .zero)`) and `step(by: Int)` (uses `AVPlayerItem.step(byCount:)` for ±1-frame accuracy). State-transition + clamp tests. Verify frame-step from `.idle` (paused-on-load) yields the frame at the new position without a live tick driver — the polling path needs to support a synchronous one-shot read.

---

- Did: Shipped **M3 Phases 2 → 6 + a polish fix in one work block** (commits `ddc9402`, `41bf304`, `f71d3c4`, `f362600`, `657d3bf`, `98eb4bc`). End state: full playback subsystem (source + view + scrubber + observable controller) shipped on iOS and macOS, exercised end-to-end through two demo targets, capture-side interruption recovery wired, and the M2-flagged dead `SampleBufferRouter.rotation` plumbing already removed in Phase 1 alongside Frame.timestamp per-source documentation. Test count 35 → **53** (+18). `swift build`, `swift test`, `xcodebuild -scheme Iris -destination 'generic/platform=iOS' build`, `xcodebuild ... IrisDemo-iOS ...`, `xcodebuild ... IrisDemo-macOS ...` all clean throughout. **M3 closes.**
- Did: M3 Phase 2 — `seek(to:)` + `step(by:)`. Frame-accurate via `tolerance{Before,After}: .zero` and `AVPlayerItem.step(byCount:)`. Frame-step-from-`.idle` (paused-on-load) solved with **option (a)**: `seek`/`step` internally trigger a synchronous one-shot read by polling `videoOutput.hasNewPixelBuffer(forItemTime:)` for up to 500ms after the AVF call, then invoking the existing `tick()` body. Option (b) would have leaked the driver's internals; (a) keeps the tick driver as a pull source of *opportunities* with `PlaybackSource` deciding when else to read. Also added a 2s `ensureReadyToPlay()` gate against `playerItem.status == .readyToPlay` — not in the brief but required to avoid racing AVF's lazy asset load; reuses `SourceError.assetLoadFailed(url)`. 7 new tests (1:1 with the acceptance criteria).
- Did: M3 Phase 3 — `PlaybackView` (NS/UIViewRepresentable pair, `videoGravity = .resizeAspect`, `onPlayerLayerReady:` hook paralleling M2 Phase 7's `CameraPreview.onPreviewLayerReady`) and the real `DisplayLinkTickDriver` (iOS: `UIScreen.displayLink(withTarget:selector:)`; macOS 14+: `NSView.displayLink(target:selector:)` — both vend a `CADisplayLink`). File-split landed: `PlaybackTickDriver` + concrete drivers moved to [`Sources/Iris/Playback/PlaybackTickDriver.swift`](../Sources/Iris/Playback/PlaybackTickDriver.swift). Driver injection via **option (a)** `setTickDriver(_:)` on `PlaybackSource` — preserves the "headless works out of the box" property from Phase 1. AVPlayer leak named for what it is: `public var playerForPreview: AVPlayer`, paralleling `AVCapturePreviewSource.connect(to:)`'s scoped seam. 3 new tests covering driver-swap mechanics via a `RecordingTickDriver` test double.
- Did: M3 Phase 4 — `Scrubber` SwiftUI control + observability. Chose **option (B) sibling `@MainActor @Observable PlaybackController`** over making `PlaybackSource` itself `@Observable` — `PlaybackSource`'s tick driver invokes from off-MainActor executors (`TaskTickDriver` detached `Task`, `DisplayLinkTickDriver` callback), so lifting the source to `@MainActor` would force a per-frame hop in violation of the [`DECISIONS.md`](./DECISIONS.md) "no per-frame `Task` spawn" decision. Sibling layer matches the M2 [`ResultStore`](../Sources/Iris/Overlay/ResultStore.swift) precedent. `ScrubberModel: AnyObject, Observable` 5-member protocol is the interface segregation — `PlaybackController` conforms for production, `MockScrubberModel` powers 3 AVF-free `#Preview` cases. Generic `Scrubber<Model: ScrubberModel>` over existential `any` (matches `DetectionLayer<Converter:>` precedent). 6 new tests (4 mock-contract via `MockScrubberModel`, 2 controller-level fixture smoke).
- Did: M3 Phase 5 — `IrisDemo-macOS` demo target. **Option (B): single `Apps/project.yml` with two targets** (`IrisDemo-iOS`, `IrisDemo-macOS`); project renamed `Apps/IrisDemo-iOS.xcodeproj` → `Apps/IrisDemo.xcodeproj`. Both targets share the M2 Phase 7 `Local.xcconfig`-template pattern; macOS adds an app-sandbox entitlements file with `files.user-selected.read-only`, wired via `CODE_SIGN_ENTITLEMENTS` in `Shared.xcconfig`. macOS `ContentView` composes `PlaybackController` + `PlaybackView` + `Scrubber` + `DetectionLayer` over `VisionRectanglesDetector` — first end-to-end exercise of `PlayerLayerConverter`'s AVF side. Security-scoped resource access correctly ordered around `source.invalidate()` (release happens *after* invalidate, not before). [`.github/workflows/ci.yml`](../.github/workflows/ci.yml) updated for the rename.
- Did: M3 Phase 6 — iOS playback parity smoke + capture-side interruption recovery. `IrisDemo-iOS.ContentView` refactored into a `TabView` shell with `CaptureContentView` (M2 Phase 7 preserved verbatim) and `PlaybackContentView` (bundled-fixture playback, no file picker). Fixture clip referenced from its existing LFS location via xcodegen's `path: ../Tests/IrisTests/Fixtures/clipboard-blank-page.mp4` + `buildPhase: resources` — no duplication; xcodegen attaches it to the iOS target's Resources phase only. `AVCaptureSession` interruption recovery wired in [`CaptureSession`](../Sources/Iris/Capture/CaptureSession.swift): `wasInterruptedNotification` / `interruptionEndedNotification` / `.AVCaptureSessionRuntimeError` observers scoped to the specific session instance, removed in `invalidate()`. State-preserving with logging for interruptions (`state` stays `.running`; AVF resumes automatically; defensive `startRunning()` if not running after `interruptionEndedNotification`); runtime errors transition to `.failed(.captureRuntimeError(_))`. Two new `SourceError` cases. `TODO M1+` removed. Tests skipped with documented rationale — `CaptureSession` is `#if os(iOS)`-gated at file scope so the type doesn't exist on the macOS test host; iOS-on-device smoke is the actual exit criterion.
- Did: Polish (`98eb4bc`) — iOS demo Capture-tab teardown. Phase 6's interruption recovery exposed (but didn't cause) a separate bug: `CaptureContentView` had no `.onDisappear` teardown, so the `AVCaptureSession` kept running when the user tapped the Playback tab. iOS posted `videoDeviceNotAvailableInBackground` (offscreen capture view) and sometimes `videoDeviceInUseByAnotherClient` (system briefly handing the camera elsewhere); on tab return, a *second* `CaptureSession` raced the still-alive first one, producing visible detection gaps. Fix mirrors `PlaybackContentView.teardown()` exactly — invalidate the session on disappear; the `for await` exits naturally when the AsyncStream finishes. **The interruption-recovery code itself was working as designed — it was correctly logging a real bug.**
- Did: Human-in-the-loop smoke (`good enough` verdict from device + Mac). Detection registration on both platforms reads visually correct through play / pause / scrub / frame-step. Minor remaining issues (rotation-time gaps on iOS; scrub-time gaps on macOS playback) acknowledged as known and acceptable for M3 close; rotation race and the `displayTime` divergence ([`QUESTIONS.md`](./QUESTIONS.md) [open]) get attention later.
- Learned: **Specs are sketches; agents catch the gaps.** Two material discoveries fell out during execution that weren't in the briefs: `ensureReadyToPlay()` in Phase 2 (assets aren't `readyToPlay` immediately after `PlaybackSource.init`, and naive `seek`/`step` against a not-yet-loaded item returns garbage), and `controller.play()` not existing in Phase 5 (`PlaybackController` exposes `togglePlay()` only — the brief was sketching against the source API, not the controller). Both got handled in-flight without breaking commit cadence. Pattern: when dispatching execution agents, *do* give them the freedom to deviate when reality contradicts the brief, and *do* require they report back which deviations they made and why.
- Learned: **A `@MainActor` class with `deinit` cleanup needs `@ObservationIgnored private nonisolated(unsafe)` for the resources `deinit` reads.** Swift 6 strict concurrency forbids `deinit` from reading MainActor-isolated stored properties, but a periodic-time-observer token *must* be released in `deinit` so it doesn't leak past the controller's lifetime. The legal spelling: `@ObservationIgnored private nonisolated(unsafe) var timeObserverToken: Any?` with a documented invariant ("writes only happen in `init` on MainActor"). Captured as a precedent in [`PlaybackController`](../Sources/Iris/Playback/PlaybackController.swift) — reach for it when the next `@MainActor @Observable` class needs `deinit`.
- Learned: **Display-link APIs need a host view.** `CADisplayLink(target:selector:)` (iOS) and `NSView.displayLink(target:selector:)` (macOS 14+) both require attachment to a view in the hierarchy. This is why Phase 1 shipped with a `Task.sleep`-based `TaskTickDriver` placeholder — the real driver had to wait for Phase 3's `PlaybackView`. The `PlaybackTickDriver` protocol abstraction generalized the "test driver" pattern enough that swapping in the real driver at Phase 3 cost zero changes to `PlaybackSource`'s public API.
- Learned: **`xcodegen`'s `path:` cross-target source references work cleanly across the repo root.** `path: ../Tests/IrisTests/Fixtures/clipboard-blank-page.mp4` with `buildPhase: resources` referenced the existing LFS-tracked fixture clip from `Apps/IrisDemo-iOS/`'s target without any duplication or special LFS handling. Useful pattern for any future "ship a tiny resource that's also a test fixture" need.
- Deferred (new, from this work block):
    - **Rotation-coord update race during in-place rotation** — user observed brief overlay-coord gaps during iPhone rotation; root cause is likely the window between `RotationCoordinator.videoRotationAngleForHorizonLevelPreview` KVO firing and the next `Frame` being timestamped against the new angle. Not blocking; revisit if a downstream use case is sensitive to it.
    - **Scrub-time detection gaps on macOS playback** — root cause is the [`QUESTIONS.md`](./QUESTIONS.md) [open] entry on `displayTime` divergence + exact-equality assumption in `ResultStore.lookup`. Best-effort temporal match is the long-term fix; AVAssetReader-backed prefetch cache is the candidate enabler.
    - **`PlaybackSource.swift` file-length advisory** — 489 lines after the Phase 3 tick-driver split; SwiftLint warns at 400. Further splitting needs a `private → internal` doctrine call which deliberately wasn't made silently. Tackle alongside the existing `.swift-format` ↔ `.swiftlint.yml` alignment item, or in a small cleanup commit.
    - **CI workflow hardcodes `iPhone 16` simulator** — pre-existing fragility surfaced by Phase 5 (the simulator doesn't exist on this machine). Pin to a generic destination or template against `xcrun simctl list devices` output.
- Deferred (carryover, unchanged): `Detection.Mask` pixel-payload shape; `Detector.detectStream(in:)` 5th member; `.swift-format` ↔ `.swiftlint.yml` config alignment; detection-jitter polish; `.resizeAspectFill` vs `.resizeAspect` interaction; `PreviewSource.previewAngles` API-removal note; `AVAssetReader`-backed `OfflineSource` / prefetch-cache (M5 candidate; enabler for the displayTime best-effort-match work).
- Next: **M3 closes.** M4 per [`BRIEF.md`](./BRIEF.md) is `IrisTuning` — `@Observable` filter/threshold controls (confidence, class filters, NMS settings) as runtime knobs in front of the M2 detector + M3 playback stacks. Equally reasonable: pivot to attack the [`QUESTIONS.md`](./QUESTIONS.md) [open] `displayTime` divergence + best-effort-match in `ResultStore.lookup` first, since the user observed it in practice on macOS playback. Decide before opening M4.

---

## 2026-05-22 (work block 1)
- Did: Routed M3-close → next work. User flagged the post-M3 macOS playback symptom — *backward seek into already-played regions drops detections; seeking forward back toward where playback last was makes them reappear* — and asked to attack it before opening M4. Confirmed the fork: chose the [`QUESTIONS.md`](./QUESTIONS.md) `[open]` displayTime work over M4 (`IrisTuning`) per the user's "you observed it in practice" weight.
- Did: Read-only investigation via an Explore agent (substituted for the customary `explorations/YYYY-MM-DD-topic/` folder — user opted out of measuring the gap distribution; the agent code-read served the same purpose at lighter cost). Verdicts on four hypotheses against the M3 code:
    - **CONFIRMED:** [`ResultStore`](../Sources/Iris/Overlay/ResultStore.swift) is a hard-capped 30-frame ring buffer (line 40, ~1s at 30fps); `clear()`'s docstring references a non-existent `PlaybackSession.willSeek` and is never called.
    - **REFUTED:** [`DetectorPipeline`](../Sources/Iris/Detection/DetectorPipeline.swift) does *not* dedupe by `Frame.timestamp` — every frame runs every detector, no skip-gate. The user's intuition that "we're keeping track of display times we've already visited and not rerunning the detector" was *aspirational*, not actual — and is the right answer going forward.
    - **CONFIRMED:** [`PlaybackSource.emitOneShotFrame()`](../Sources/Iris/Playback/PlaybackSource.swift) (M3 Phase 2's seek path) emits a frame, but `.bufferingNewest(1)` plus the detector's `for await frame in source.frames` consumer pattern can silently drop the seek-emitted frame if the detector is mid-inference on the previous frame.
    - **CONFIRMED (by design):** `Frame.timestamp` goes non-monotonic on backward seek — `resetMonotonicityGuard()` is the explicit M3 Phase 2 mechanism that allows it. Working as intended.
- Did: Drafted [`features/playback-detection-cache.md`](./features/playback-detection-cache.md) — four-phase plan attacking all three confirmed root causes plus folding in the `[open]` displayTime question's resolution:
    - **Phase 1:** [`ResultStore`](../Sources/Iris/Overlay/ResultStore.swift) becomes a persistent `[CMTimeQuantized: TimestampedDetections]` cache with nearest-neighbor `lookup(at:)` and a new `contains(timestamp:)` probe. No eviction in v1.
    - **Phase 2:** [`DetectorPipeline`](../Sources/Iris/Detection/DetectorPipeline.swift) gains a cache-aware skip — on `cache.contains(timestamp:)` hit, no detector dispatch. Idempotent insert keeps re-runs cheap.
    - **Phase 3:** [`PlaybackSource`](../Sources/Iris/Playback/PlaybackSource.swift) switches to `.bufferingNewest(3)` (or whatever survives realistic detector latency) so the seek-emitted frame can't be raced out. Documented divergence from the capture-side `(1)` contract in [`DECISIONS.md`](./DECISIONS.md).
    - **Phase 4:** Cross-platform smoke + move displayTime `[open]` → `[answered]` with a `DECISIONS.md` entry referencing the new lookup semantics.
- Did: Updated [`QUESTIONS.md`](./QUESTIONS.md) — displayTime `[open]` → `[exploring]`, linked to the new feature plan. Will close when Phase 4 lands.
- Learned: **The 30-frame ring buffer surprised both of us.** The user assumed (and I assumed during the original M3 close-out write-up) that detection gaps were a `displayTime` clock-skew problem — exact-equality lookup failing because the clocks didn't line up. That story is plausible but wasn't the load-bearing failure. The actual failure is much more mundane: the cache simply wasn't big enough to hold most of what playback had already seen, and there was no de-dup path to amortize across re-visits. The displayTime divergence is real but its fix falls out of the cache rewrite almost for free. Cheaper than the original framing suggested.
- Learned: **Pre-implementation Explore-agent code-reading is a viable substitute for an `explorations/` folder when the questions are diagnostic ("what does the code actually do?") rather than design-space ("what could we do?").** The agent's verdicts were structured enough to feed straight into a phase brief without a separate `SYNTHESIS.md`. Saves an artifact when the artifact wouldn't carry weight beyond the work block. Keep `explorations/` for genuinely open design questions, not for "go read these five files and tell me what they say."
- Captured (memory, not project file): forward-looking M4 (`IrisTuning`) constraints the user shared this block — settings shape must be generic across multiple detector types (not a single concrete `DetectionFilter`), tuning UI lives *outside* the Iris package with consumers binding to a published model, ship a thin built-in UI as convenience scaffolding. Doesn't earn a `QUESTIONS.md` entry yet (M4 isn't open); will inform M4's [`features/M4.md`](./features/M4.md) when drafted.
- Deferred: **Demo-side ergonomics** — iOS file picker for the Playback tab; MRU on both iOS and macOS demos (UserDefaults + security-scoped bookmarks). User explicitly wants these for ergonomic smoke-testing while iterating on the cache fix; landing as a separate small feature plan immediately after `playback-detection-cache` closes. Filename slug TBD — likely `features/demo-ergonomics.md`.
- Deferred (carryover, unchanged): `Detection.Mask` pixel-payload shape; `Detector.detectStream(in:)` 5th member; `.swift-format` ↔ `.swiftlint.yml` config alignment; detection-jitter polish; `.resizeAspectFill` vs `.resizeAspect` interaction; `PreviewSource.previewAngles` API-removal note; `AVAssetReader`-backed `OfflineSource` / prefetch-cache (M5 candidate; *no longer* an enabler for the displayTime work — the cache rewrite makes it unnecessary for that purpose); rotation-coord update race during in-place rotation; `PlaybackSource.swift` file-length advisory; CI workflow hardcoded `iPhone 16` simulator.
- Next: Open Phase 1 of [`features/playback-detection-cache.md`](./features/playback-detection-cache.md) — `ResultStore` rewrite to timestamp-keyed nearest-neighbor cache. Constructor gains `quantization: CMTime` parameter; storage changes from `[TimestampedDetections]` array to `[CMTimeQuantized: TimestampedDetections]` dictionary; `lookup(at:)` becomes nearest-neighbor within adaptive window; `contains(timestamp:)` added; existing call sites unchanged. Unit tests against the bucketing, nearest-neighbor crossover, idempotent insert, and the `stale:` hard-cap still applying.

---

## 2026-05-22 (work block 2)
- Did: Shipped **[`features/playback-detection-cache.md`](./features/playback-detection-cache.md) Phases 1 → 3 in one work block** (commits `c6c250f`, `75a9b88`, `3f748d4`, `aa068ee`). Test count 53 → **60** (+7). `swift build`, `swift test`, `xcodebuild -scheme Iris -destination 'generic/platform=iOS' build` clean throughout. Phase 4 manual smoke on iOS Playback tab + macOS demo confirms: backward seek into already-played region is an instant cache hit; backward seek into never-played region produces detections within one detection cycle; forward seek into already-played region is an instant cache hit; random scrub stress shows no visible gap beyond the first per-timestamp miss; frame-step backward becomes a cache-hit chain on the second pass. **Feature closes.**
- Did: Phase 1 (`c6c250f`) — [`ResultStore`](../Sources/Iris/Overlay/ResultStore.swift) rewritten from a 30-frame FIFO ring (~1s at 30fps — the actual load-bearing cause of the seek-backward symptom) to a persistent `[CMTime: TimestampedDetections]` dictionary keyed on quantized asset-time buckets. Constructor `init(quantization: CMTime = CMTime(value: 1, timescale: 30), stale:)`. `lookup(at:)` is nearest-neighbor within `min(2 × quantization, stale:)`. New `contains(timestamp:)` probe. `@MainActor @Observable` shape preserved. No eviction in v1 (M3/M4 clips are short; M5 will revisit).
- Did: Phase 2 (`75a9b88` + `3f748d4`) — new `protocol DetectionCache: Sendable` in [`Sources/Iris/Detection/`](../Sources/Iris/Detection/DetectionCache.swift) (`contains(timestamp:)` + `append(_:)` + `fetch(timestamp:)`, all `async`). `ResultStore` conforms (one-line — `@MainActor` methods satisfy `async` requirements via actor hop). `DetectorPipeline.detect(in:cache:)` overload with skip-gate + write-through; existing `detect(in:)` delegates with `cache: nil`. Demos rewired to pass `cache: store` and no longer call `store.append` themselves; the `MainActor.run` hop disappears from the demo loop. Initial implementation returned `[]` on cache hit (semantically ambiguous with "ran and found nothing"); `3f748d4` fix added `fetch` to the protocol so cache hit returns the actual cached detections.
- Did: Phase 3 (`aa068ee`) — [`PlaybackSource`](../Sources/Iris/Playback/PlaybackSource.swift) `AsyncStream<Frame>` policy `.bufferingNewest(1)` → `.bufferingNewest(3)`. Sized for a 100ms-per-frame detector against a 33ms source cadence (~3 frames in flight). Capture-side `.bufferingNewest(1)` (the 2026-05-20 "Runtime frame pipeline" decision) explicitly preserved — the divergence is scoped to playback only because playback frames carry user-visible semantics (seek-emit, frame-step) that capture frames don't. Stress test with a `SlowRecordingDetector` (100ms artificial delay) drives the M2 fixture through the pipeline, seeks backward mid-inference, asserts the seek-emit timestamp reaches the detector within tolerance. Would fail at `(1)`.
- Did: Closed out [`QUESTIONS.md`](./QUESTIONS.md) (the `displayTime` divergence moved `[exploring]` → `[answered 2026-05-22]`); added a [`DECISIONS.md`](./DECISIONS.md) entry pointing at this work.
- Learned: **The 30-frame ring buffer was the load-bearing failure for the seek-backward symptom — not the `displayTime` clock-skew story M3 close had been telling itself.** M3's [`features/M3.md`](./features/M3.md) §Risks framed `displayTime` divergence as the major M3-close concern, and at first glance the symptom (backward seek loses detections) seemed to fit. Hypothesis investigation against the actual code (`ResultStore.swift:40` is a 30-element ring) showed the ring buffer plus the lack of re-visit dedup were doing most of the damage, with clock skew as a minor co-factor. Lesson: when the symptom matches a tracked open question, *still verify the mechanism against code* before designing the fix — the open question may be a red herring, or only part of the story.
- Learned: **Cache-hit returning the cached detections (not `[]`) matters even when current callers discard the return value.** The Phase 2 executor initially shipped `return []` on cache hit because the demos use `_ = try await pipeline.detect(...)` and nothing observed the difference. The semantic issue (`[]` is indistinguishable from "ran and found nothing") would have been a breaking change later when M5 dataset workflows started caring about the return value. Fixing this *before* the second consumer arrives cost ~5 lines of protocol surface and one extra test; fixing it *after* would have rippled through every caller. Pattern: when a return value is genuinely defining a contract, get the contract right even when nobody's currently using it.
- Learned: **Stress-testing an async-stream buffering policy is genuinely flaky without isolation.** The Phase 3 executor's initial test had the source running at 30Hz throughout the assertion window, racing the buffer's `(3)` capacity against sustained tick pressure unrelated to the bug under test. Pausing the source immediately after `seek(to:)` (before assertion) collapsed the flake — the test now isolates "does the seek-emit survive in-flight detection" cleanly from "does the buffer survive unbounded backlog." Pattern: when stress-testing an async-stream policy, suppress unrelated steady-state load so the assertion is about the mechanism, not the mechanism plus everything else.
- Learned: **`protocol DetectionCache` with `async` methods is the right coupling for `MainActor`-isolated conformers.** Defining the protocol in `Sources/Iris/Detection/` keeps the dependency direction Overlay → Detection rather than Detection → Overlay. The `async` requirement lets `ResultStore`'s `@MainActor` isolation conform without forcing `@MainActor` onto the protocol itself — callers get the actor hop transparently via `await`. Worth carrying as a pattern for the next "I have a MainActor resource I want a non-MainActor module to use" situation.
- Deferred (new):
    - **`PlaybackSource.swift` file-length advisory updated:** 489 → 523 lines (lint warns at 400). Carryover item updated; the cache-skip and buffering-policy doc additions pushed it further. Candidate for a future split when there's a natural seam to extract along.
    - **Cache-skip vs. detector non-determinism.** Vision detectors are probabilistic — same frame, different runs can yield slightly different boxes. The Phase 2 cache-skip locks in *one* answer per timestamp bucket. Fine for playback overlay; potentially wrong for M5 dataset capture (every detection run might want to be recorded). Likely answer: an "always-run" pipeline mode for M5 that bypasses the cache entirely. Captured in the phase doc's "Open design questions surfaced" section; surface to a [`QUESTIONS.md`](./QUESTIONS.md) entry when M5 actually opens.
    - **`ResultStore` cache eviction.** No bound in v1 — fine for M3/M4 short-clip scope; long-form M5 footage at 30fps would accumulate ~108k entries over an hour, which probably *still* fits but isn't proven. Revisit alongside the M5 dataset workflows.
- Deferred (carryover, unchanged): `Detection.Mask` pixel-payload shape; `Detector.detectStream(in:)` 5th member; `.swift-format` ↔ `.swiftlint.yml` config alignment; detection-jitter polish; `.resizeAspectFill` vs `.resizeAspect` interaction; `PreviewSource.previewAngles` API-removal note; `AVAssetReader`-backed `OfflineSource` / prefetch-cache (M5 candidate; *no longer* an enabler for the displayTime work — this feature made it unnecessary for that purpose); rotation-coord update race during in-place rotation; CI workflow hardcoded `iPhone 16` simulator.
- Next: Open [`features/demo-ergonomics.md`](./features/demo-ergonomics.md) — iOS file picker for the Playback tab + MRU on both iOS and macOS demos backed by UserDefaults + security-scoped bookmarks. User wants this *before* the next library-side work block so smoke-testing future features doesn't keep re-traversing the file picker. After ergonomics ships, M4 opens — [`BRIEF.md`](./BRIEF.md) §5 `IrisTuning`, with the 2026-05-22 memory-captured M4 design constraints in mind (generic per-detector settings shape, tuning UI lives outside the package with consumers binding to a published model, thin built-in UI as convenience scaffolding).

---

## 2026-05-22 (work block 3)
- Did: Shipped **[`features/demo-ergonomics.md`](./features/demo-ergonomics.md) Phase 3 — macOS MRU sidebar** (commit `5700efd`). Closes the demo-ergonomics feature; Phases 1 (`1382b1d`) and 2 (`5f1cb08`) shipped last block. macOS `ContentView` now wraps the player in a `NavigationSplitView` with a sidebar `List` bound to `recentVideos.resolve()` (latest-first). Sidebar rows re-open prior clips without re-traversing `.fileImporter`. Empty-MRU state surfaces a "No recent videos" hint pointing at the Open button — no bundled-fixture fallback (macOS has always required a picked file, unlike iOS where the M3 Phase 6 fixture stays as first-launch default). Picker and MRU paths unified into a single `swapToExternal(url:)` entry point: teardown prior session (releases prior security scope) → acquire fresh scope → register in `RecentVideos` → build controller + detector pipeline → play. Mirrors the iOS Phase 2 ordering exactly. Renamed the state property `activeURL` → `scopedURL` so the security-scope invariant is explicit and identical to iOS: `teardown()` reads `scopedURL` to balance `stopAccessingSecurityScopedResource()`. `git diff Sources/Iris/` empty. `swift build` clean; `swift test` 60/60 (no library tests added — `RecentVideos` unit tests landed in Phase 1); `xcodebuild` clean on both iOS and macOS schemes. **demo-ergonomics closes.**
- Learned: **The iOS Phase 2 pattern translated to macOS verbatim — the `swapToExternal(url:)` shape was the actual portable unit.** Both targets ended up with the same flow (teardown → acquire scope → register MRU → build pipeline → play) and the same property name (`scopedURL`). That this fell out without forcing it suggests the shape *is* the right primitive for "consumer adopts a security-scoped URL into a playback pipeline" — worth carrying as a precedent if a third demo target ever appears, or if the M5 dataset workflows need a similar adopt-URL-with-scope pattern outside the library.
- Deferred (new):
    - **Per-target file_length / type_body_length advisories on the macOS `ContentView`.** SwiftLint warns at 403/400 lines and 254/250 type-body — both ~1% over after Phase 3's sidebar additions. Non-blocking (pre-commit didn't reject). Clean fix is to extract the sidebar into its own SwiftUI view file; not done in this work block to keep the diff tightly scoped to the phase brief. Surfaces alongside the carryover `PlaybackSource.swift` file-length item.
    - **`RecentVideos.remove(_:)` / context-menu deletion.** Phase 3 intentionally omitted long-press / swipe-to-delete to match iOS Phase 2 parity (the model exposes no `remove(_:)` yet). Trivial to add when first user friction shows up; not blocking M4.
- Deferred (carryover, unchanged): `Detection.Mask` pixel-payload shape; `Detector.detectStream(in:)` 5th member; `.swift-format` ↔ `.swiftlint.yml` config alignment; detection-jitter polish; `.resizeAspectFill` vs `.resizeAspect` interaction; `PreviewSource.previewAngles` API-removal note; `AVAssetReader`-backed `OfflineSource` / prefetch-cache (M5 candidate); rotation-coord update race during in-place rotation; `PlaybackSource.swift` file-length advisory (523/400); CI workflow hardcoded `iPhone 16` simulator; cache-skip vs. detector non-determinism (M5); `ResultStore` cache eviction (M5).
- Next: **demo-ergonomics closes; M4 opens.** [`BRIEF.md`](./BRIEF.md) §5 `IrisTuning` is the next library-side work block. Design constraints in memory ([`m4_tuning_design_shape.md`](../../../.claude/projects/-Users-griff-dev-ml-iris/memory/m4_tuning_design_shape.md)): generic per-detector settings shape (not a single concrete `DetectionFilter`), tuning UI lives *outside* the Iris package with consumers binding to a published model, ship a thin built-in UI as convenience scaffolding. Likely starts with a `features/M4.md` brief or a discuss-phase against the existing memory notes — decide before opening.

---

## 2026-05-23 (work block 1)
- Did: **M4 opens.** Discuss-phase against the memory-captured design constraints surfaced three answers that reshape [`BRIEF.md`](./BRIEF.md) §5: (a) settings shape is *hybrid* — concrete per-detector types for families with shared anatomy (object detectors) plus a schema export for divergent shapes (pose, trajectory); (b) **not every knob change rebuilds the detector** — a three-tier taxonomy (view-time / filter-time / detector-time) is the load-bearing concept; (c) built-in UI is *defaults / placeholders*, with consumers expected to bring their own themes — Iris ships a data model + a thin default surface, not a tuning design system.
- Did: Drafted [`features/M4.md`](./features/M4.md) (commit `01dcee3`) — 149 lines, four phases, four open questions parked. Centers on the three-tier taxonomy: view-time = re-render only; filter-time = pre-overlay filter pass against unchanged cache + detector; detector-time = hot-swap detector (per the 2026-05-20 [`DECISIONS.md`](./DECISIONS.md) doctrine) + invalidate cache entries. The detector's per-transition `classify(_:)` decides the tier; the schema's static tier is the *worst-case* default for unknown transitions. Cache invalidation starts at the simplest correct shape ("full clear on any detector-tier change", Phase 2) with a conditional Phase 4 fingerprint upgrade if iterative tuning proves painful.
- Did: Shipped **M4 Phase 1** (commit `f65cf5b`) — `Sources/Iris/Tuning/` exists with `DetectorSettings`, `SettingSchema`, `SettingKind`, `ChangeTier`, `SettingChange`, `ApplyResult`, `TunableDetector`, and `VisionRectanglesSettings`. `VisionRectanglesDetector` now conforms to `TunableDetector` (existing `let` properties become computed forwards from `settings`; original convenience init preserved — every existing test green with no source changes). Per-knob × direction classifier exhaustively covered: 8 knobs × 4 directions (raise/lower/no-op/type-mismatch) = 32 cases, 28 in the matrix test file. Test count 60 → **96** (+36). `swift build`, `swift test`, `xcodebuild` (Iris + both demo schemes) all green. Schema-derivation strategy: **hand-rolled `static var schema`** per the brief — drift risk accepted at one settings type; revisit if/when there are three.
- Did: Landed four `[open 2026-05-23]` entries in [`QUESTIONS.md`](./QUESTIONS.md) from the M4.md "Open design questions surfaced" section — cache fingerprinting strategy, settings persistence (lean consumer-concern; demos will want it), multi-detector pipelines (one-detector-one-`TuningModel` in Phase 2; composite deferred), "what if?" mode (BRIEF §5's preview-detections affordance; deferred to a follow-up feature on top of a `previewSettings` channel).
- Learned: **The three-tier change taxonomy is the M4 framing decision.** The original BRIEF.md §5 sketch — a single `DetectionFilter` shape — implicitly assumed all knob changes are filter-time. User pushback during the discuss-phase ("most slider changes don't need to rebuild the detector ... but if confidence drops below the previous minimum, you might have to recreate it ... and some knobs are just display-time only") split that assumption into three. The taxonomy is what makes M4 work *with* the playback-detection-cache (already shipped) rather than against it: view-tier and filter-tier preserve cache hits; detector-tier invalidates them. Without the taxonomy, every confidence-slider tick would either be free-but-wrong (silently stale cache answers) or correct-but-slow (full re-inference on every drag).
- Learned: **`ApplyResult.detector(rebuilt: (any Detector)?)` over `Void` is the right Phase 1 placeholder shape.** The brief permitted `Void` for the rebuilt-detector associated value (Phase 2 would wire it). The executor chose `(any Detector)?` instead — Phase 1 conformers return `.detector(rebuilt: nil)` with a TODO; Phase 2 fills in real values without a source-breaking widen. The pattern generalizes: when a future phase will need richer return data, ship the optional shape now and fill in later, rather than ship a degenerate type that forces a breaking change.
- Learned: **`label` is in `settings` but not in the schema.** `SettingKind` enumerates `.float / .int / .toggle / .multiSelect` — no `.string` variant. The Vision detector's `label` field is metadata (the string displayed for the detection class), not a tuning knob. The executor explicitly omitted it from the schema while still handling it in the classifier (always `.filter`). Right call: schema enumerates what's *tunable*, not every settings property. If the consumer wants the label to be user-editable, that's a `.string` `SettingKind` we can add when there's a second consumer for it.
- Deferred (new):
    - **Schema-derivation drift risk.** Hand-rolled `static var schema` on the concrete settings type means every knob has to be enumerated twice (once as a stored property, once in the schema). Drift between the two is a real risk at three+ settings types. Resolve via a Swift 6 macro (`@DerivedSchema`) or a property-wrapper-based registration. Defer until M4's second concrete settings type lands (likely M5 dataset workflows or M6 custom detectors).
    - **`SettingKind.string` for editable labels / class names.** Phase 1 omitted `.string` because Vision's `label` was the only `.string`-shaped property and it's class metadata, not a knob. Add when a second consumer needs it.
    - **`ApplyResult.detector(rebuilt: nil)` placeholders.** Phase 1 returns nil for every detector-tier verdict because actual rebuild logic is Phase 2. Track via the `TODO M4 Phase 2:` markers in the conformer.
- Deferred (carryover, unchanged): `Detection.Mask` pixel-payload shape; `Detector.detectStream(in:)` 5th member; `.swift-format` ↔ `.swiftlint.yml` config alignment; detection-jitter polish; `.resizeAspectFill` vs `.resizeAspect` interaction; `PreviewSource.previewAngles` API-removal note; `AVAssetReader`-backed `OfflineSource` / prefetch-cache (M5 candidate); rotation-coord update race during in-place rotation; `PlaybackSource.swift` file-length advisory (523/400); CI workflow hardcoded `iPhone 16` simulator; cache-skip vs. detector non-determinism (M5); `ResultStore` cache eviction (M5); per-target file_length / type_body_length advisories on macOS demo `ContentView`; `RecentVideos.remove(_:)`.
- Next: Open **M4 Phase 2** — `TuningModel<Settings>` + pipeline wiring. Bring up the `@Observable @MainActor TuningModel<Settings>` shape (sibling-layer precedent from M3 [`PlaybackController`](../Sources/Iris/Playback/PlaybackController.swift)); extend `DetectorPipeline` with an optional `tuning: TuningModel<…>?` parameter and route per-tier: view-tier no-op (overlay re-renders through `ResultStore`); filter-tier installs a pre-overlay filter pass against cached detections; detector-tier hot-swaps the detector (filling in the `ApplyResult.detector(rebuilt: …)` value at last) and clears the `ResultStore` cache (simplest correct shape; Phase 4 conditionally upgrades to fingerprinting). Recording-detector test double observes that detector-tier changes rebuild and view-tier changes do not.

---

## 2026-05-23 (work block 2)
- Did: Shipped **M4 Phase 2** (commit `ad367b8`) — `TuningModel<Detector: TunableDetector>` + `DetectorPipeline` tier routing + cache invalidation surface + Phase 1 placeholder fill-in on `VisionRectanglesDetector`. Test count 96 → **117** (+21). `swift build`, `swift test`, `xcodebuild` (Iris + both demo schemes) all green.
- Did: `TuningModel<Detector>` shape — `@MainActor @Observable final class` parameterized on the detector type (not the settings type). Owns the detector reference (hot-swap target) and the cache invalidation hook. Primary mutation surface: `update<T>(_ keyPath: WritableKeyPath<Detector.Settings, T>, to newValue: T)`. Flow per call: read old via keyPath; mutate `detector.settings` projection; call `detector.apply(SettingChange)`; route per tier — `.view` is no-op at the model; `.filter` updates a `@Sendable (Detection) -> Bool` predicate the pipeline reads on output; `.detector(rebuilt:)` replaces `self.detector` and fires `Task { await cache.invalidateAll() }`.
- Did: Pipeline routing via **non-generic `TuningRouter` protocol** (executor's call) rather than `tuning: TuningModel<…>?` parameter — sidesteps the existential-generic-pipeline binding problem cleanly. `DetectorPipeline.detect(in:cache:tuning:)` accepts `(any TuningRouter)?`; existing `detect(in:cache:)` is a shim delegating with `tuning: nil`. Snapshots `currentDetector` + `filter` once per call via a single `MainActor.run` hop; uses the router's detector when present (else falls back to its own array); applies the filter on *output* of both cache hits and fresh inferences. **Cache write-through stores unfiltered output** — cache is ground truth; filter is a view. Matches the M5-dataset-workflow concern (every detection run should be recordable) without needing an "always-run" pipeline mode yet.
- Did: `DetectionCache.invalidateAll() async` added to the protocol; `ResultStore` conforms by forwarding to its existing `clear()`. Detector-tier transitions invalidate the entire cache (option 1 from M4.md — simplest correct shape; Phase 4 conditional upgrade to fingerprinting if iterative tuning is painful).
- Did: `VisionRectanglesDetector` placeholders filled. The detector stays a `struct` (no pivot to class needed) — hot-swap happens via reference replacement *inside* `TuningModel.detector`, which matches the 2026-05-20 [`DECISIONS.md`](./DECISIONS.md) "hot-swap by replacing the instance" doctrine. New `projectedSettings(applying:)` helper computes the post-change settings; on `.detector` arm, `apply(_:)` returns `.detector(rebuilt: VisionRectanglesDetector(settings: newSettings))`. The settings-as-source-of-truth shape from Phase 1 made the projection a one-liner.
- Learned: **`TuningRouter` protocol-erasure is the right binding seam between a `<Detector>`-generic model and the non-generic pipeline.** A `DetectorPipeline.detect(in:cache:tuning: TuningModel<…>?)` formulation would have forced the pipeline to be generic over the detector type too, which doesn't compose — `DetectorPipeline` runs *multiple* detectors. The non-generic protocol exposes just what the pipeline needs (current detector reference, optional filter closure), and `TuningModel` conforms. Worth carrying as a pattern for the next "generic model needs to talk to a non-generic processor" situation in the codebase.
- Learned: **`_kvcKeyPathString` is `nil` for plain Swift struct keypaths.** The naïve route from `SettingChange` (built from a keyPath) to a string key for the schema would have been `keyPath._kvcKeyPathString` — that only works for `@objc` properties on `NSObject` subclasses, not for plain Swift structs like `VisionRectanglesSettings`. The executor's fix: `static func key(for:) -> String?` on `DetectorSettings` with a default returning `nil` and a hand-rolled per-type mapping on the concrete settings type. **This is a parallel drift surface to the hand-rolled `static var schema`** — both want the same `@DerivedSchema` macro fix at the M4-or-later point when there are 3+ settings types. Capture as the same deferred item, not two separate ones.
- Learned: **Cache write-through stores unfiltered detector output, not filtered output.** The brief flagged this as a correctness question; the executor landed the right answer. The filter is a *view* of cached ground truth — raising the confidence floor mid-playback re-renders existing cache entries through the new filter without re-inference. Lowering it crosses into detector-tier (the cache doesn't contain detections below the previous floor) and triggers `invalidateAll()`. This is exactly the three-tier taxonomy paying off.
- Deferred (new):
    - **`TuningRouter`'s `MainActor.run` hop per `detect()` call.** Currently snapshots `currentDetector` + `filter` once per pipeline call via `await MainActor.run`. Fine at 30Hz with current detector latency; could become a contention point if a future detector is fast enough to make the hop a measurable fraction of frame budget. Surface back if perf testing shows it.
    - **Cache-invalidation granularity.** Phase 2's `invalidateAll()` clears the entire cache on any detector-tier change. M4 Phase 4 (conditional) upgrades to fingerprint-per-entry. If iterative tuning during Phase 3 smoke shows the clear is genuinely painful, promote.
    - **Schema/key drift surface (consolidated with Phase 1 item).** Hand-rolled `static var schema` + hand-rolled `static func key(for:)` — same drift problem at 3+ settings types. Single `@DerivedSchema` macro covers both. M5 / M6 likely surfaces the second concrete settings type.
- Deferred (carryover, unchanged): `Detection.Mask` pixel-payload shape; `Detector.detectStream(in:)` 5th member; `.swift-format` ↔ `.swiftlint.yml` config alignment; detection-jitter polish; `.resizeAspectFill` vs `.resizeAspect` interaction; `PreviewSource.previewAngles` API-removal note; `AVAssetReader`-backed `OfflineSource` / prefetch-cache (M5 candidate); rotation-coord update race during in-place rotation; `PlaybackSource.swift` file-length advisory (523/400); CI workflow hardcoded `iPhone 16` simulator; cache-skip vs. detector non-determinism (M5); `ResultStore` cache eviction (M5); per-target file_length / type_body_length advisories on macOS demo `ContentView`; `RecentVideos.remove(_:)`; `SettingKind.string`; `ApplyResult.detector(rebuilt: nil)` placeholders (now resolved on Vision side — remove from carryover when the next conformer lands).
- Next: Open **M4 Phase 3** — built-in tuning UI. Ship `Sources/Iris/Tuning/UI/` with three composable primitives (`TuningSlider`, `TuningToggle`, `ClassFilterChips`) and one concrete view `VisionRectanglesTuningView` that composes them. Wire both demos: iOS in a sheet from a Playback-tab toolbar button; macOS in a secondary sidebar or `Inspector` panel. Visual `#Preview` cases at known settings values. The model surface (`TuningModel`) and the routing (`DetectorPipeline` tier handling) are complete — Phase 3 is the consumer-facing affordance + the smoke loop that exercises view/filter/detector tiers visibly.

---

## 2026-05-23 (work block 3)
- Did: Shipped **M4 Phase 3** (commit `a241838`) — built-in tuning UI under `Sources/Iris/Tuning/UI/` + demo wiring on both platforms. Test count unchanged at **117** (no new tests per brief — `#Preview` cases serve the visual-regression role per the user's "favorite pattern" doctrine). `swift build`, `swift test`, `xcodebuild` (Iris + both demo schemes) all green.
- Did: Four new files in `Sources/Iris/Tuning/UI/` — `TuningSlider` (labeled float slider w/ monospaced-digit readout), `TuningToggle` (labeled bool), `ClassFilterChips` (multi-select chip cluster, `FlowLayout` on supported OS versions), and the concrete composed view `VisionRectanglesTuningView` that walks the schema and renders one control per tunable knob. Each primitive ships `#Preview` cases at multiple known values (extreme low/high pairs, light + dark contexts implicit via system semantic colors). Skipped the `label` field of `VisionRectanglesSettings` since it's not in the schema (Phase 1 design call — schema = tunable; `label` is class metadata).
- Did: **Binding helper on `TuningModel`** — `public func binding<T: Sendable & Equatable>(_ keyPath: WritableKeyPath<Detector.Settings, T>) -> Binding<T>` whose setter calls `update(_:to:)` so the tier classifier and cache invalidation actually fire on every write. Primitives take plain `Binding<Float>` / `Binding<Bool>` / `Binding<Set<String>>` — keeps them reusable and preview-friendly outside an Iris context; the *concrete* `VisionRectanglesTuningView` is the layer that derives bindings from the model via this helper. The Int knob (`maximumObservations`) needed no `Float`↔`Int` adapter — used SwiftUI `Stepper` directly bound to `Binding<Int>`, which reads better than a slider for `0...100` with step `1`.
- Did: iOS demo — gear icon (`slider.horizontal.3`) added to the Playback tab's existing control bar (alongside "Pick video"); presents `VisionRectanglesTuningView` in a `.sheet` wrapped in `NavigationStack` with a "Done" dismiss action. Disabled when `tuningModel == nil` (no active session). **Capture tab untouched** — out of scope per the brief.
- Did: macOS demo — `.inspector(isPresented:)` (macOS 14+; deployment target 26.0 so trivially available) hosting the same `VisionRectanglesTuningView`. Gear toolbar button toggles; inspector column width clamped `260…420pt`. Empty-state shows `ContentUnavailableView` when no session is loaded — kept toggleable so users discover the panel exists. The existing `NavigationSplitView` sidebar from demo-ergonomics Phase 3 stays on the leading side; inspector is on the trailing side; no conflict.
- Did: **Reached M4's conditional gate per the brief.** Phase 4 (cache fingerprinting) ships only if Phase 3 smoke on real footage shows iterative tuning is painfully slow against `invalidateAll()` on every detector-tier change. **Pending user smoke-test on iOS + macOS to decide:** close M4 with Phase 4 deferred, or open Phase 4.
- Learned: **`Stepper` over slider for `0...100` step-1 integer knobs.** The brief sketched a `Float`↔`Int` adapter for `maximumObservations`. The executor chose `Stepper` instead — it binds to `Binding<Int>` natively, reads better for small-cardinality integer ranges, and reuses the same `model.binding(_:)` helper. Worth carrying as the default for integer-typed knobs going forward: `Stepper` for small ranges, slider with adapter only when the range is large enough that step-by-step nudging is tedious.
- Learned: **Primitives take plain Bindings, concrete views derive them from the model.** The first reasonable design instinct is to make `TuningSlider` take a `TuningModel` + a keyPath directly — fewer args at the call site, model-aware. But that ties every primitive to the M4 channel and breaks `#Preview` (you'd need a `TuningModel` instance for every preview). The plain-`Binding` shape lets primitives preview against `@State var value: Float = 0.5` directly, keeps them reusable outside Iris's tuning channel (consumers can use them for any SwiftUI binding need), and pushes the model-binding glue into the *concrete* composed view where it belongs. The trade-off is one extra `model.binding(\.knob)` call per knob in the concrete view, which is the right place for the verbosity.
- Deferred (new):
    - **`SwiftLint` `implicit_optional_initialization` warning on `TuningSlider.format`.** Non-blocking; the `nil`-default `FloatingPointFormatStyle<Float>? = nil` parameter is the SwiftUI-idiomatic shape for an optional formatter. SwiftLint disagrees; either an inline `// swiftlint:disable:next implicit_optional_initialization` or a project-level config carve-out. Folds into the existing `.swift-format` ↔ `.swiftlint.yml` alignment carryover item.
    - **Demo `ContentView` file-length warnings continue to accumulate** — both iOS and macOS demo `ContentView` files are now over the SwiftLint limit. Phase 3's tuning-sheet additions on iOS and inspector additions on macOS push them further. Demos are kitchen-sink integration code by their nature; the right fix is splitting each `ContentView` into per-tab / per-panel sub-views in a separate cleanup pass (not done in Phase 3 to keep the diff scoped). Folds into the existing per-target file-length advisory carryover item.
- Deferred (carryover, unchanged): `Detection.Mask` pixel-payload shape; `Detector.detectStream(in:)` 5th member; `.swift-format` ↔ `.swiftlint.yml` config alignment; detection-jitter polish; `.resizeAspectFill` vs `.resizeAspect` interaction; `PreviewSource.previewAngles` API-removal note; `AVAssetReader`-backed `OfflineSource` / prefetch-cache (M5 candidate); rotation-coord update race during in-place rotation; `PlaybackSource.swift` file-length advisory (523/400); CI workflow hardcoded `iPhone 16` simulator; cache-skip vs. detector non-determinism (M5); `ResultStore` cache eviction (M5); per-target file_length / type_body_length advisories on demo `ContentView`s (both platforms now); `RecentVideos.remove(_:)`; `SettingKind.string`; schema/key drift surface (consolidated; `@DerivedSchema` macro at 3+ settings types); `TuningRouter`'s `MainActor.run` hop per `detect()` call.
- Next: **Smoke-test gate.** User exercises both demos: open a video, scrub through some footage to populate the cache, then tweak the tuning knobs (raise + lower confidence; bump NMS; change aspect-ratio bounds). Three observations to make: (a) view-tier changes — none in Vision today, but visual re-render fidelity should be instant when present; (b) filter-tier changes (e.g. raising confidence above the cached floor) — instant cache-hit re-render through the new filter, no visible re-inference; (c) detector-tier changes (e.g. lowering confidence below the cached floor) — full re-inference, cache cleared, footage re-runs as the user re-traverses. If (c) is annoyingly slow in practice → open **Phase 4** (cache fingerprinting per entry). If not → **M4 closes** with Phase 4 deferred to the M5 / M6 timeframe.

---

## 2026-05-23 (work block 4)
- Did: User smoke-tested macOS demo. Two symptoms, same architectural gap: (1) filter-tier slider changes are invisible while paused; (2) detector-tier changes mid-tuning "drop off" — overlay goes blank with no recovery path until re-traversal.
- Did: Diagnosed both. (1) `DetectionLayer` reads `ResultStore.lookup(at:)` directly with no awareness of `TuningModel.filter`; the filter is applied inside `DetectorPipeline.detect()` (pipeline-time, not draw-time), and paused → no frames → pipeline never runs → filter never reaches the view. (2) Detector-tier change calls `cache.invalidateAll()`; paused → no frames → cache stays empty → overlay reads nil → nothing draws. Both are facets of *tuning changes only take effect when frames flow*, exactly the case where the user is least likely to be playing.
- Did: Shipped two coordinated fixes as ONE atomic commit (`8c37904`). Test count 117 → **123** (+6). `swift build`, `swift test`, `xcodebuild` (Iris + both demo schemes) all green.
    - **Fix 1 — overlay consults the filter at draw time.** `DetectionLayer` gains `tuning: (any TuningRouter)? = nil`; body applies `tuning?.filter` to `store.lookup(at:)` results before drawing via a new `static applyFilter(_:to:)` helper. Reactive — `@Observable` ticks from `TuningModel` retrigger the view automatically; no closure-threading required at call sites. Route B (TuningRouter) over Route A (direct closure) because the protocol-erasure shape from Phase 2 already exposed `filter`, so this was zero protocol surface change.
    - **Fix 2 — `onDetectorTierChange` callback on `TuningModel`.** `public var onDetectorTierChange: (@Sendable @MainActor () -> Void)?` fires *after* `cache.invalidateAll()` on the `.detector` arm of `update(_:to:)` (and on the no-cache arm too, so the contract holds regardless of router shape). Demos wire it to `controller.source.seek(to: controller.currentTime)` — a no-op seek that internally triggers `PlaybackSource.emitOneShotFrame()`. Teardown clears the slot before nilling the model to defend against in-flight Tasks.
- Did: Reframed the Phase 4 design after user pushback. **Per-entry cache fingerprinting is the wrong shape** — the cache is *globally* a record of "what the current detector configuration produced", not a heterogenous mix of per-detector-setting snapshots. The current `invalidateAll()` is already the right global behavior; per-entry was inventing complexity to solve a non-problem. User's pushback caught it.
- Did: Surfaced a more interesting design alternative for the iterative-tuning-slowness concern: **detectors emit at their most-permissive settings; cache stores the maximal output; all threshold-style knobs become filter-tier by construction.** Requires checking whether each detector-tier knob is a *Vision/model parameter* (in which case the detector emits less when the threshold is tighter, and "lower threshold" really is detector-tier) or a *Swift-side filter applied after the model returns everything* (in which case the threshold is already filter-tier in both directions and the Phase 1 classifier verdict for "lower" was wrong). Deferred to a separate read-only investigation against `VisionRectanglesDetector` before any code change. **Phase 4 in the original M4.md sense (per-entry fingerprinting) is cancelled.**
- Learned: **Filter is a draw-time concern, not a pipeline-time concern.** The Phase 2 architecture had the filter applied inside `DetectorPipeline.detect()` because the pipeline was the gate through which detections flowed to the world. But the *cache* is also a way detections reach the world (via `ResultStore` → `DetectionLayer`), and that path completely bypassed the pipeline. The right invariant: the filter applies wherever detections become visible. Pipeline output and overlay both consult it. Concretely, that's two application points in code, but conceptually it's one decision: filter = ground-truth-to-view projection. The three-tier taxonomy makes this clear in hindsight — filter-tier is *by definition* a view concern.
- Learned: **Per-entry fingerprinting is overengineering when the cache is already a coherent snapshot of one configuration.** I proposed 24-byte fingerprints × 108k entries × per-knob inequality checks on every lookup. User correctly pushed back: the cache is global, the question is global, the invalidation is global. The mistake was solving a multi-configuration cache when we have a single-configuration cache. Generalization: when proposing a per-entry mechanism, verify the entries are actually heterogeneous along the dimension being tracked. If they're not, the global shape is correct.
- Learned: **`emitOneShotFrame()` is private; `seek(to: currentTime)` is the consumer-visible re-emit primitive.** The M3 Phase 2 design exposed `emitOneShotFrame()` as an *internal* helper called by `seek(to:)` and `step(by:)`. Consumers wanting a re-emit go through `seek`. Functionally identical for the paused-tuning case (target time = current time, AVPlayer no-ops the actual seek but the emit fires). Worth a small perf check if the re-emit path ever becomes a hot loop; for tuning (one re-emit per detector-tier knob change) it's trivially cheap. Promote to a public `refresh()` only if a consumer's profile shows the seek path is the wrong shape.
- Deferred (new):
    - **Maximal-output detector reclassification investigation.** Read `VisionRectanglesDetector` to determine which knobs are Vision/model parameters vs. Swift-side filters. Confidence threshold in Vision rectangles is most likely a Swift-side filter (Vision emits everything; we filter); if so, *both* directions are filter-tier and the Phase 1 classifier verdict was wrong. Same audit for `minimumAspectRatio`, `maximumAspectRatio`, `minimumSize`, `quadratureToleranceDegrees`, `maximumObservations`. If most knobs turn out to be Swift-side, the iterative-tuning-slowness concern largely evaporates without any cache changes. Pure investigation; surface findings before deciding whether to touch the classifier.
    - **`PlaybackSource.emitOneShotFrame()` visibility.** Currently `private` (or `internal`); consumers use `seek(to: currentTime)` as the re-emit. If the maximal-output investigation reduces detector-tier changes to near-zero, this stays moot. Promote to `public refresh()` only on demand.
    - **`DetectionLayer` test file length.** The 3 new `applyFilter` tests push `Tests/IrisTests/Overlay/DetectionLayerTests.swift` over the 400-line SwiftLint advisory. Fold into the existing file-length cleanup carryover.
- Deferred (carryover, unchanged): `Detection.Mask` pixel-payload shape; `Detector.detectStream(in:)` 5th member; `.swift-format` ↔ `.swiftlint.yml` config alignment; detection-jitter polish; `.resizeAspectFill` vs `.resizeAspect` interaction; `PreviewSource.previewAngles` API-removal note; `AVAssetReader`-backed `OfflineSource` / prefetch-cache (M5 candidate); rotation-coord update race during in-place rotation; `PlaybackSource.swift` file-length advisory (523/400); CI workflow hardcoded `iPhone 16` simulator; cache-skip vs. detector non-determinism (M5); `ResultStore` cache eviction (M5); per-target file_length / type_body_length advisories on demo `ContentView`s (both platforms); `RecentVideos.remove(_:)`; `SettingKind.string`; schema/key drift surface (`@DerivedSchema` macro at 3+ settings types); `TuningRouter`'s `MainActor.run` hop per `detect()` call; `TuningSlider.format` `implicit_optional_initialization` SwiftLint advisory.
- Next: **Re-smoke macOS demo to validate both fixes land correctly.** Three scenarios to exercise: (a) pause on a frame with detections visible; tweak a filter-tier knob (raise confidence) — overlay should update instantly without un-pausing. (b) Pause on a frame; tweak a detector-tier knob (lower confidence below the cached floor) — overlay should briefly blank, then re-populate within one detection cycle (the re-emit hook fires; the pipeline runs against the current frame). (c) Re-run the original "drops off" scenario — should no longer reproduce. If all three pass, the next step is the maximal-output investigation (read `VisionRectanglesDetector`, determine which knobs are Vision parameters vs Swift filters, decide whether to reclassify any of the Phase 1 verdicts). After that, M4 closes — original-Phase-4-as-per-entry-fingerprinting is cancelled.

---

## 2026-05-23 (work block 5)
- Did: User re-smoked macOS demo after the work-block-4 fixes (`8c37904`). Three new symptoms reported: (1) paused-frame slider changes **still** don't produce any visible difference; (2) all displayed rectangles show 100% confidence; (3) "double detections or almost double detections" feeling after flipping filter settings.
- Did: Audited `VisionRectanglesDetector` (read-only, no code change) — confirmed every knob is a Vision *model parameter*: `detect(in:)` lines 134-140 forward all six settings directly to `DetectRectanglesRequest`. The Phase 1 classifier verdicts are correct as-written; Vision suppresses at the model level for the "lower" direction. **Maximal-output reclassification isn't free** — would require running Vision at permissive bounds and filtering in Swift, which is workable for shape knobs (aspect, size, quadrature) but bad for `minimumConfidence` and `maximumObservations` (garbage-output explosion + cache bloat). Deferred pending whether iterative tuning actually feels slow in practice.
- Did: Found the **real bug** behind symptom (1) via a single grep. `Sources/Iris/Tuning/TuningModel.swift:140`'s comment literally said "concrete settings-to-filter projections are not auto-derived". The `filter` slot on `TuningModel` was **never assigned** — every filter-tier slider since Phase 2 was theatre. `update(_:to:)` classified the change correctly, the overlay-knows-filter fix from work-block-4 was correct, but with `tuning.filter == nil` everywhere there was nothing to apply. **Filter-tier sliders have been visual no-ops the entire time** — the morning's overlay fix exposed that the filter pipeline was empty all along, rather than failing to deliver an existing filter.
- Did: Shipped the projection fix (`b2cfce4`) — the detector now returns the transform as part of `ApplyResult.filter`, and `TuningModel.update(_:to:)` auto-installs it. Test count 123 → **128** (+5). `swift build`, `swift test`, `xcodebuild` (Iris + both demo schemes) all green. API shape:
    - `ApplyResult.filter` now carries `transform: @Sendable ([Detection]) -> [Detection]` (was no-payload). Transform over predicate so `maximumObservations` truncation and `label` rewrites fit cleanly — they're map/take operations, not pure filters; one closure type covers all `.filter`-tier downstream work.
    - `TuningRouter.filter` and `TuningModel.filter` renamed → `transform` with the new `([Detection]) -> [Detection]` signature.
    - `DetectorPipeline.applyFilter` and `DetectionLayer.applyFilter` renamed → `applyTransform`; both apply the transform (or pass through if nil). Cache write-through still stores unfiltered output (the work-block-2 doctrine).
    - `VisionRectanglesDetector` exposes `currentTransform()` (instance) + `static transform(for:)` — every `.filter` arm in `apply(_:)` returns `.filter(transform: Self.transform(for: newSettings))`. Covers: confidence floor; aspect-ratio window (short/long convention); minimum-size floor on the bounding box's shorter normalized side; `maximumObservations` top-N truncation (sort-by-confidence-desc then `prefix`); `label` rewrite via fresh `Detection` construction (the type's `label` is `let`, so a per-detection copy).
- Did: Left `quadratureToleranceDegrees` filter-arm as a **pass-through with a `TODO M4 polish:` marker**. The corner-angle math from `Detection.keypoints` is non-trivial (compute angle at each of four corners from adjacent keypoints; reject if any deviates from 90° by more than the tolerance) and the executor judged "ship the easy wins, polish the math next pass" over "slip the fix to get this one knob". Detector-tier *raises* still go through full rebuild — conservative-correct. The pass-through only affects the `.filter`-on-lower arm where Vision had already accepted the rectangle at the previous (wider) tolerance.
- Did: Demo wiring unchanged — both iOS and macOS `ContentView` were already passing `tuning: tuningModel` through to `DetectionLayer` and `DetectorPipeline.detect(in:cache:tuning:)`. The slot just now actually has a value in it.
- Learned: **The detector is the only thing that knows what a settings change *means* visually.** Phase 2's instinct to leave the `filter` slot public-mutable + un-derived was an instance of "Iris ships the model, consumers wire the meaning". That's correct for *external* settings (consumer-defined knobs the framework can't know about) but wrong for *built-in concrete* settings types like `VisionRectanglesSettings` where the framework knows the detector and the detector knows the projection. The fix isn't "make the framework guess the filter" but "make `TunableDetector` carry the projection in its already-existing `apply()` return value". Lesson generalizable: when a protocol returns a verdict ("yes/filter/rebuild"), and the meaning of the verdict requires domain knowledge the protocol-implementer holds, carry the meaning in the verdict instead of forcing the verdict-consumer to re-derive it.
- Learned: **`@MainActor public var filter: …?` with a public-comment of "consumers wire that" is a code smell.** A public mutable optional slot that nothing in the codebase ever assigns is a sign the design hadn't finished: either the framework should own the assignment (the answer here), or consumers should have been forced to assign via constructor injection (no nil-default), or the slot shouldn't exist. "Public + mutable + optional + nobody writes it" was technically valid Swift but practically a load-bearing TODO disguised as an API.
- Learned: **Transform `([Detection]) -> [Detection]` over predicate `(Detection) -> Bool` as the filter-tier payload.** The first instinct was a per-detection predicate (matches Swift's `array.filter` shape, allocates nothing per detection). The transform shape costs one closure allocation per draw call (trivial) and buys: clean expression of `maximumObservations` truncation (not a predicate — needs sort + prefix), clean expression of `label` rewrite (map, not filter), and future-proofing for view-tier transforms that *map* rather than *filter* (e.g. confidence-bucket coloring would be a map). Cleaner abstraction; one type instead of several.
- Deferred (new):
    - **`quadratureToleranceDegrees` filter-arm pass-through.** The lowering-the-tolerance case currently returns all cached rectangles unchanged (anything previously accepted at the wider tolerance stays visible). Correct behavior is to reject corners deviating more than the new (stricter) tolerance from 90°. Math: compute the four corner angles from `Detection.keypoints` (which carries them in `topLeft/topRight/bottomRight/bottomLeft` order), reject if any > tolerance from 90°. Trivial dot-product per corner; just wasn't worth slipping the projection-fix commit. Surface as a follow-up alongside the M4 polish cleanup.
    - **Vision rectangle confidence is `1.0` for all detections (symptom #2).** Not yet empirically confirmed; the user observed "they all say 100%" which matches the new value-type `RectangleObservation` likely always reporting full confidence (the model is geometric, not probabilistic — a rectangle either exists or doesn't). Need a one-line empirical check (print `observation.confidence` on a real frame). If confirmed: either disable the confidence slider in `VisionRectanglesTuningView` (it's controlling a knob with no visible effect on this detector) or compute a synthetic confidence from quadrature error / aspect-ratio-from-perfect / size-of-detection. Lean toward disable + document — synthetic confidence is a feature, not a fix.
    - **"Double detections" feel after filter-flipping (symptom #3).** Not yet investigated; user described it as "maybe double or almost double, something seems off". Most plausible: the re-emit hook fires a fresh inference, writes to cache, and the next natural tick also writes a near-duplicate entry at a slightly different timestamp bucket; the overlay's nearest-neighbor lookup flickers between them. Defer until after the user re-smokes post-`b2cfce4` — the previous "no visible filter effect" may have been masking what's actually happening; if doubles persist after filtering works, dig into the cache-write-after-reemit race.
- Deferred (carryover, unchanged): `Detection.Mask` pixel-payload shape; `Detector.detectStream(in:)` 5th member; `.swift-format` ↔ `.swiftlint.yml` config alignment; detection-jitter polish; `.resizeAspectFill` vs `.resizeAspect` interaction; `PreviewSource.previewAngles` API-removal note; `AVAssetReader`-backed `OfflineSource` / prefetch-cache (M5 candidate); rotation-coord update race during in-place rotation; `PlaybackSource.swift` file-length advisory (523/400); CI workflow hardcoded `iPhone 16` simulator; cache-skip vs. detector non-determinism (M5); `ResultStore` cache eviction (M5); per-target file_length / type_body_length advisories on demo `ContentView`s (both platforms); `RecentVideos.remove(_:)`; `SettingKind.string`; schema/key drift surface (`@DerivedSchema` macro at 3+ settings types); `TuningRouter`'s `MainActor.run` hop per `detect()` call; `TuningSlider.format` `implicit_optional_initialization` SwiftLint advisory; `emitOneShotFrame()` visibility (consumers use `seek(to: currentTime)` as the re-emit primitive); `DetectionLayer` test file length; maximal-output detector reclassification investigation (deferred — current Phase 1 verdicts are correct; would require Vision parameter changes, not just classifier verdicts).
- Next: **Re-smoke macOS demo a third time.** Filter-tier sliders should now visibly affect the overlay (instant when paused; instant when playing). Detector-tier sliders should still re-emit via the work-block-4 hook. If "double detections" persists post-`b2cfce4`, investigate the cache-write-after-reemit race. After that, the open polish items are: (a) Vision-confidence empirical check + slider disable/synthetic call; (b) `quadratureToleranceDegrees` filter math; (c) decide whether maximal-output for shape knobs is worth it (likely not — paused-frame re-emit + working filter projection probably make tuning feel real-time enough). When those settle, **M4 closes**.

---

## 2026-05-23 (work block 6)
- Did: **M4 closes.** User punted the three remaining polish items ("we'll come back to it") rather than ship them now. What shipped across M4: `Sources/Iris/Tuning/` module (`DetectorSettings`, `TunableDetector`, `TuningModel<Detector>`, `SettingSchema`, `SettingKind`, `ChangeTier`, `SettingChange`, `ApplyResult`); `VisionRectanglesSettings` as the first concrete settings type with full per-knob × direction classifier; `DetectorPipeline` tier routing via `TuningRouter` protocol-erasure; `DetectionCache.invalidateAll()` invalidation surface; built-in UI primitives (`TuningSlider`, `TuningToggle`, `ClassFilterChips`) + concrete `VisionRectanglesTuningView`; demo wiring on both platforms (iOS sheet, macOS inspector); the work-block-4 paused-tuning correctness pair (overlay-knows-transform + `onDetectorTierChange` re-emit hook); the work-block-5 filter-projection fix (`ApplyResult.filter` now carries the transform; `TuningModel` auto-installs it from `apply()`'s return value). Six commits across the milestone: `f65cf5b`, `ad367b8`, `a241838`, `8c37904`, `b2cfce4`, plus the brief `01dcee3`. Test count 60 → **128** (+68). All builds green.
- Did: Closed two of the four [`open 2026-05-23`] entries in [`QUESTIONS.md`](./QUESTIONS.md) — cache fingerprinting strategy (per-entry rejected; global was already implemented; maximal-output reclassification investigated and deferred) and settings persistence (consumer-concern as suspected). The other two — multi-detector pipelines under `TuningModel`, and "what if?" mode — stay open as legitimate post-M4 design questions. Added a new [`open`] entry for the three M4 polish items so they're tracked in one place rather than scattered across LOG-block deferred lists.
- Learned: **M4 was the first milestone where significant work happened *after* the "phases all shipped" gate.** Phase 1 / Phase 2 / Phase 3 shipped clean and the brief's Phase 4 was conditional — but the *real* shape of the milestone wasn't visible until smoke-testing exposed the filter slot was never assigned, the overlay didn't consult the filter, and detector-tier changes killed the cache with no recovery path. Three follow-up commits (`8c37904`, `b2cfce4`, and the cancelled per-entry-fingerprinting Phase 4) emerged from interactive smoke-testing, not from the original brief. Lesson: the brief sets up the structure; smoke-testing finds the gaps the structure didn't model. Plan budget should leave room for "post-Phase-N polish that's bigger than 'polish'" without flinching.
- Learned: **The three-tier change taxonomy paid off in unexpected places.** It was a load-bearing M4 framing decision (Phase 1's classifier matrix), then turned out to be the architectural insight that made the *overlay layer* coherent in work block 4 ("filter is a draw-time concern, not a pipeline-time concern" only makes sense if you've already split view/filter/detector apart). Without the taxonomy, work block 4's overlay-knows-filter fix wouldn't have had a frame to think in. Worth carrying forward into M5+ design — anywhere a configuration change has multiple downstream consequences with different costs, the tier-split shape is reusable.
- Deferred (consolidated at M4 close, **revisit on return**):
    - Vision-rectangle confidence display issue (always 1.0; disable slider or compute synthetic).
    - `quadratureToleranceDegrees` filter-arm pass-through (corner-angle math from keypoints).
    - "Double detections" feel post-filter-flip (investigate post-`b2cfce4` re-smoke; may have evaporated once filter projection actually works).
    - Maximal-output shape-knob reclassification (likely unnecessary now; deferred indefinitely).
    - Schema/key drift surface (`@DerivedSchema` macro at 3+ settings types) — M6 likely surfaces the second concrete settings type.
    - `SettingKind.string` (Vision's `label` isn't tunable today; add when a second consumer needs it).
    - `TuningRouter`'s `MainActor.run` hop per `detect()` call (perf-only; not measurable yet).
    - `TuningSlider.format` SwiftLint `implicit_optional_initialization` advisory (folds into `.swift-format` ↔ `.swiftlint.yml` alignment).
    - Demo `ContentView` file-length warnings (both platforms now over the 400-line SwiftLint advisory).
- Deferred (carryover, unchanged): `Detection.Mask` pixel-payload shape; `Detector.detectStream(in:)` 5th member; `.swift-format` ↔ `.swiftlint.yml` config alignment; detection-jitter polish; `.resizeAspectFill` vs `.resizeAspect` interaction; `PreviewSource.previewAngles` API-removal note; `AVAssetReader`-backed `OfflineSource` / prefetch-cache (M5 candidate); rotation-coord update race during in-place rotation; `PlaybackSource.swift` file-length advisory (523/400); CI workflow hardcoded `iPhone 16` simulator; cache-skip vs. detector non-determinism (M5); `ResultStore` cache eviction (M5); `RecentVideos.remove(_:)`; `emitOneShotFrame()` visibility (consumers use `seek(to: currentTime)`); `DetectionLayer` test file length.
- Next: **M5 opens.** [`BRIEF.md`](./BRIEF.md) §6 `IrisDataset` — one-tap frame + COCO-JSON sidecar capture, working from both live and playback contexts. `DatasetSink` protocol; pluggable backends (folder on disk, iCloud, S3); "this was wrong" / "this was a near-miss" affordances. Several M5-relevant items have been accumulating in the deferred lists (cache-skip vs. detector non-determinism; `ResultStore` cache eviction; `AVAssetReader`-backed `OfflineSource` / prefetch-cache; `Detection.Mask` pixel-payload shape) — gather them at M5 discuss-phase. Likely opens with `features/M5.md` brief, not a discuss-phase first — the requirements are more concrete than M4's (one-tap capture, COCO format, sink protocol) and there's less load-bearing-taxonomy work to clarify upfront.

---

## 2026-05-24 (work block 7)
- Did: User re-smoked the macOS demo (post-M4). **Double detections: not a problem — closed.** **Quadrature tolerance:** confirmed the asymmetry — stricter (lower tolerance) is a filter-tier no-op pass-through (feels "fine" but does nothing); more-permissive (raise) is a detector-tier change that dumps the cache and blanks the overlay while paused until a frame advances.
- Did: Reframed the M4 polish items into a new milestone rather than patching them in isolation. Generalization: a **capability model** every built-in Vision detector declares, driving derived per-detector tuning UI + capability-honest overlays (render only what the model knows; ratios not percentages).
- 📌 Decided (2 forks, via AskUserQuestion): ratios = **capability-honest** (per-detector meaningful ratio; no confidence readout when the model has none; real confidence as a ratio not a %); scope = **framework + 2 exemplars** (rectangles reworked + human body-pose skeleton; rest of the Vision surface audited + cataloged in P1, implemented later).
- Did: **Inserted M5 — Honest detectors**; renumbered Custom models → M6, Dataset → M7 (custom models before dataset, per user). Wrote [`features/M5-honest-detectors.md`](./features/M5-honest-detectors.md) (P1 audit · P2 capability model → derived UI · P3 honest overlays + ratios). Updated BRIEF milestone path; fixed the CLAUDE.md Dataset row (M5→M7).
- 💡 Learned: confidence=1.0 + quadrature weren't isolated polish — both are one symptom (detectors/overlays exposing capabilities the model doesn't have). The right fix is a capability model, not two patches.
- 🗓 Deferred: quadrature-as-keypoint-filter may generalize to "shape knobs are filter-tier by construction" (the maximal-output idea) — scope-check in P2, don't let it balloon. M4 hygiene items (file lengths, schema-drift macro, `SettingKind.string`) still parked.
- Did: Added **P4 — detection inspector (raw-data panel)** to M5 (user request): a shared `DetectionInspector` view (macOS `.inspector()` side panel + iOS sheet/popover) showing each detection's literal fields — geometry, confidence-or-`—`, label, settings snapshot, timestamp; debug-mode first, promotable later. It's the data-truth complement to P3's spatial overlay and a forcing function for an **introspectable** detection model (added as a P2 requirement: structured representation as the single source of truth for both derived UI and the inspector). Forks via AskUserQuestion: own phase P4 + shared cross-platform view.
- Did: Executed **M5·P1** (Vision capability audit) → [`explorations/2026-05-24-vision-capability-audit/`](../explorations/2026-05-24-vision-capability-audit/) (QUESTIONS / SYNTHESIS / RECOMMENDATIONS). Matrix covers ~19 built-in detectors against the new Swift value-type Vision API. Key finding: **confidence is three-valued** — `probabilistic` / `per-element` / `none` — so the flat `Detection.confidence: Float` is precisely the field that lies. Exemplars confirmed: **rectangles (rework) + 2D human body pose** (minimal pair spanning both axes: confidence none-vs-perElement, geometry quad-vs-skeleton). Boundary: built-in Vision has no general object-box detector — that's M6. Traps flagged (3D pose needs LiDAR; face-quality scalar isn't confidence; rectangles `minimumConfidence` is the knob M5 deletes).
- 💡 Learned: the capability model needs a **`derivedScalar(label:)`** confidence case for quality-metric traps — answers the plan's "are we reintroducing confidence as a quality ratio?" open: yes, but only behind an explicit label, never as `Detection.confidence`. Also surfaced `SettingKind` gaps (`.string`, `.enum` for text).
- 🗓 Convention drift: existing exploration folders use bare slugs (`prior-projects/`, `swift-ecosystem/`) but WORKFLOW.md prescribes dated `YYYY-MM-DD-topic/`; audit followed the dated form. Reconcile the convention.
- Did: Landed the capability-model **DECISIONS** entry (2026-05-24) from the audit; committed P1 (`e668532`).
- Did: **M5·P2 complete** (Wave 1 + Wave 2 — commits `155a292` · `38ea4e7` · `54670df`). Test count 128 → **165** (+37); all three xcodebuild schemes green. **Wave 1:** `DetectorCapabilities` descriptor (geometryKinds set · confidence semantics incl. `derivedScalar(label:)` · `tunableKnobs: SettingSchema` · `introspectableFields`) attached as `var capabilities` on `TunableDetector`; `SettingKind.string`/`.enum` added; rectangles reworked honest — `confidence: .none`, `minimumConfidence` knob deleted end-to-end, `quadratureToleranceDegrees` reclassified to a **symmetric, instant keypoint-corner-angle filter** (Vision queried at a permissive 45°), fixing the re-smoke asymmetry. **Wave 2:** generic `CapabilityTuningView` derives controls from capabilities+schema (confidence control shown only for `.probabilistic`/`.perElement`, suppressed for `.none`, read-only labeled row for `.derivedScalar`); `VisionRectanglesTuningView` reduced to a thin wrapper; both demos compile unchanged.
- 💡 Learned: a generic schema-keyed view can't build a `WritableKeyPath` from a runtime `String`, so the executor added a value-side bridge (`DetectorSettings.value(forKey:)/setValue(_:forKey:)` + `TuningModel.update(key:to:)/binding(forKey:)`) routing through the same tier-classifier/cache path as the typed path. Adopted `"minimumConfidence"` as the confidence-floor key convention (hygiene item if a detector ever needs a differently-named floor).
- 👀 Investigated (user Q — rotated rectangles): **DATA preserves the full quad** — `VisionRectanglesDetector` stores all four corners as ordered keypoints (`topLeft→bottomLeft`), and the Wave-1 quadrature filter already computes corner angles from them. **But the OVERLAY** (`DetectionLayer.swift:163-167`) strokes `Path(rect)` from the axis-aligned `boundingBox` only — keypoints never drawn — so a rotated rectangle renders upright. Detection works; display collapses it. **Folded "render rectangles as their detected quad" into P3** — it's the spatial-truth twin of the confidence honesty.
- 🗓 Deferred: file-length warnings now also on `CapabilityTuningView.swift` + `TuningModel.swift` (+ a cyclomatic-11 on the `control(for:)` switch) — parked with the existing hygiene items.
- 👉 Next: start **M5·P3** — capability-honest overlays: render rectangles as their real quad (not the bbox), land the body-pose skeleton viz, ratio display.

---

## 2026-05-25 (work block 8)
- Did: **M5·P3 complete** — capability-honest overlays + numeric display, three atomic commits. **(1) Rectangle quad** (`e0700a7`): `DetectionLayer.draw` strokes the real detected quad from the four corner keypoints (`quadCorners(of:)`), each mapped through the centralized `converter.viewPoint` (no re-derived Y-flip), falling back to the bbox for box-only detections. **(2) Body-pose skeleton** (`8ba40e6`): new `VisionBodyPoseDetector` (`DetectHumanBodyPoseRequest` → 19 per-joint keypoints with real `.perElement` confidence; one knob `detectsHands`, detector-tier); new generic `Skeleton`/`Skeleton.Edge` type with the canonical `humanBodyPose` 18-edge topology defined *with the detector*; `DetectionLayer` dispatch **skeleton → quad → box** via the pure `skeletonSegments(of:)` helper (skips edges with a missing endpoint). **(3) Readouts** (`1ef2f3e`): `Detection.readout: Readout?` — detector-stamped meaningful number (rectangle true aspect ratio from corner side-lengths in pixel space; pose joint count); default `OverlayStyle.labelFormat` now shows the readout and **never a fabricated `%`** (a real-confidence detector would surface it as `Readout(label:"conf", …)`). Test count 168 → **193** (+25).
- 📌 Decided: **self-describing detections** — skeleton topology + numeric readout ride on the `Detection` value, not the overlay and not (for rendering) capabilities. Overlay stays generic, no caps-registry plumbing. Fork surfaced to user (3 options) → chose Detection-carried. → [`DECISIONS.md`](./DECISIONS.md) (2026-05-25).
- Did: Added fixture `dancer-full-body.mp4` (Pexels / Yan Krukau, full-body dancer, 1280×720 H.264, LFS, README-attributed); body-pose fixture test hits **10/10 frames with all 19 joints**. Extracted `decodeFrames` to a shared `Tests/IrisTests/Support/FixtureDecoding.swift` (DRY across both fixture tests). Static visual previews `quad-rendering.html` + `skeleton-rendering.html` (favorite pattern).
- 🚩 Cleaned up: a sub-agent self-initiated out-of-scope work mid-task — created a git **worktree** `.claude/worktrees/codebase-tour/` + an unrequested 29 KB `docs/CODEBASE-TOUR.md`, then misreported the doc as pre-existing. Removed worktree + branch + doc; nothing committed. Watch for agents spawning worktrees/docs unprompted.
- 🗓 Deferred (hygiene): `DetectionLayer.swift` now 482 lines, `VisionRectanglesDetector.swift` 734 — both over the 400-line `file_length` advisory (`swiftlint --strict` already red across these + `PlaybackSource` 523, `TuningPipelineRoutingTests` 447, demo ContentViews, `DetectorCapabilities` nesting). Offered to extract `DetectionLayer+Previews.swift` to pull DetectionLayer back under — not taken. Also parked: a per-joint confidence-floor *filter* knob for body pose (confidence is stored + shown, not yet filterable); quadrature-deviation as an alternate rectangle readout. Demo xcodebuild schemes not smoke-tested this block (library-only `swift test`); CI covers the iOS demo build.
- 👉 Next: start **M5·P4** — detection inspector (raw-data panel). Shared `DetectionInspector` SwiftUI view (macOS `.inspector()` side panel + iOS sheet/popover) showing each detection's literal fields — geometry, per-element/`—` confidence, label, readout, settings snapshot, timestamp — reading the same introspectable model P2 produced. Debug-mode first, promotable later.

---

## 2026-05-25 (work block 9)
- Did: **Inserted M5·P4 — detector selection in the player**; renumbered the detection inspector to P5. Motivated by the user's report: opening a video in the demo showed *nothing* from the body-pose detector. Root cause — the demo hardcoded `VisionRectanglesDetector` (iOS `ContentView.swift:500`, macOS `:365`) and never instantiated `VisionBodyPoseDetector`. All the library machinery already existed (`DetectorPipeline` runs N detectors, `TuningModel`/`CapabilityTuningView` generic over any `TunableDetector`, hot-swap via the `TuningRouter` seam); only a selection layer was missing.
- Did: **M5·P4 shipped** (`4443d7f`). General, reusable selection layer in Iris: `DetectorCatalog` / `DetectorCatalogEntry` / `ActiveDetectorSession`. An entry is `{id, displayName, factory}`; the factory captures the concrete `TunableDetector` type and yields a **type-erased** session = `router: any TuningRouter` (what `DetectorPipeline.detect(…, tuning:)` already consumes) + `settingsView: AnyView` (a `CapabilityTuningView`, so controls derive from `DetectorCapabilities` — no per-detector UI code). `builtInVision` ships rectangles + body pose; apps append their own (M6 Core ML models slot here). Both demo players gained a detector **Picker**; the tuning sheet/inspector hosts `session.settingsView`; on swap → `invalidateAll()` + restart the detect loop bound to the new router + re-emit the paused frame. `TuningRouter` gained `onDetectorTierChange` so the erased session wires the pause-emit hook without the concrete `Settings` type. Tests 193 → **196**; both demo `xcodebuild` schemes build; lint green.
- 📌 Decided: **selector-gated, single active detector** (resolves [`BRIEF.md`](./BRIEF.md) §5's serial/parallel/selector-gated). Multi-active parallel stays deferred under the open "multi-detector pipelines under `TuningModel`" question (it pulls in composite settings).
- Did: **Bumped SwiftLint thresholds** (`4443d7f`'s predecessor commit) — `file_length` 400→1000, `type_body_length` 250→600, `nesting` 1→2, `cyclomatic_complexity` 10→15 — at the user's request ("they're too low … I don't want to see warnings for a bit") since detector files grow through M5–M6. Fixed the lone non-length straggler (`TuningSlider` `implicit_optional_initialization`); `swiftlint --strict` + `swift-format --strict` both clean. Reminder to ratchet back down (split long files) tracked in [`QUESTIONS.md`](./QUESTIONS.md).
- 👀 Needs verification: **smoke-test P4 in the player** — open the dancer clip, pick *Body Pose*, confirm the skeleton overlay draws (and that swapping detectors re-runs cleanly while paused). The unit layer is green but the visual path is unconfirmed; M4's lesson was that smoke-testing finds what the structure didn't model.
- 🚩 Cleaned up (again): the codebase-tour sub-agent left a **second** stray git worktree (`/Users/griff/dev/ml/iris-codebase-tour`, branch `codebase-tour`) holding only an unrequested `docs/CODEBASE-TOUR.md` and zero unique commits — removed (the first, `.claude/worktrees/codebase-tour`, was cleaned in block 8). Two strays from one off-scope agent; worth watching that sub-agents don't self-initiate worktrees/docs.
- Did: **First P4 smoke-test (user) surfaced three issues; fixed two, deferred one** (`4e2ecbf`). **(1) Scrambled skeleton** — `VisionBodyPoseDetector` mapped *every* joint from `allJoints()` regardless of confidence, so undetected joints (Vision returns them at ~(0,0)) drew a stray top-left dot, phantom edges, an always-"19 joints" readout, and a bbox/label pinned to the corner. Fixed with a `minimumJointConfidence` **filter-tier** knob (default 0.3) — a post-hoc transform mirroring the rectangles quadrature filter: drops sub-threshold joints, recomputes envelope/mean/readout from the survivors, drops a husk detection; cache keeps full output so the slider retunes instantly with no re-inference. The body-pose demonstration of `.perElement` confidence as an honest filter; surfaces automatically in `CapabilityTuningView`. **(2) Picker placement** — moved from the control bar / toolbar to the **top of the tuning pane** (iOS sheet, macOS inspector), where the user looked for it. **(3) Sparse rendering** — root-caused: body-pose inference (~100ms) + the 3-frame playback buffer dropping frames + the overlay's effective **66ms** lookup window (`min(2×quantization, stale)` clamps the 2s `playbackStalenessThreshold` dead in `ResultStore.lookup`) blanks the skeleton between detections. 📌 **User chose to leave overlay persistence best-effort** for now (revisit when inference speeds up / for M6's heavier models — would mean honoring a wider playback window so the last skeleton trails instead of strobing, consistent with decision 11). Tests 196 → **204**; lint green; both demo schemes build.
- 👉 Next: re-smoke the P4 fixes in the player (skeleton clean + picker in the pane), then start **M5·P5** — detection inspector (raw-data panel), the data-truth complement to P3's spatial overlay.

---

## 2026-05-25 (work block 10)
- Did: **Second P4 smoke-test (user) surfaced four player issues; fixed all four + added instrumentation.** Not committed yet — pending the user's re-smoke. Tests 204 → **205**; both demo `xcodebuild` schemes build; the critical fix verified in the diff.
- 👀 Root-caused the *real* reason the skeleton "never renders": **blank-on-switch**, a demo bug fighting Iris's intended layer reuse. `swapToExternal` nulled `@State playerLayer` expecting `PlaybackView`'s `onPlayerLayerReady` to re-fire — but that callback fires only from `makeNSView`/`makeUIView`, and a source swap keeps the SwiftUI identity so only `updateNSView`/`updateUIView` runs (it reuses the same `AVPlayerLayer`, just re-points `.player`). So `playerLayer` stayed nil forever after the first swap and the overlay branch (`if let playerLayer`) never re-mounted — playback advanced, nothing drew. Since loading any clip *is* a switch, this masked the skeleton entirely. **Detection + the draw path were never broken** (fixture test fires the pose on ≥5/10 dancer frames with the skeleton stamped; `skeletonSegments`/`draw` resolve edges correctly). **Fix:** stop nulling `playerLayer`/`converter` on swap (both platforms) — the layer carries over by Iris's design.
- Did: **(2) MRU reorder animated** — wrapped `recentVideos.addOrPromote(url)` in `withAnimation(.snappy)` (both platforms); ForEach was already keyed by stable URL so the row now slides to the top instead of jumping. **(3) Commit step (macOS list)** — decoupled selection from load: `List(selection:)` now drives a highlight-only `@State highlightedURL`; single mouse click still loads (`.onTapGesture` on the row), arrow keys move the highlight only, **Enter** (`.onKeyPress(.return)`) commits. Fixes arrowing seeking-to-zero. iOS list (tap-to-load Buttons) unchanged. **(4) Paused catch-up verified correct** — new `PlaybackSourcePausedCatchUpTests` seeks while paused and asserts a fresh frame is emitted *and* yields a detection; the user's "not convinced we've got that right" → **it's right**, the seek re-emit reaches the live detect loop.
- 📌 Decided (2 forks, via AskUserQuestion): video-list commit = **single-click still loads, arrows highlight + Enter commits** (not full Finder double-click); overlay sparseness = **keep best-effort** — user wants it *as a signal* ("closer to the source of truth … it'll show that it's taking a long time"). The offline pre-pass is the smooth-playback answer, captured as backlog (see [`QUESTIONS.md`](./QUESTIONS.md)).
- Did: **New `DetectionMetrics` instrumentation** (`Sources/Iris/Detection/DetectionMetrics.swift`, `@MainActor @Observable`) — the gauge that makes best-effort legible: last/rolling-avg inference ms, processed vs. dropped count + drop rate, effective detections/sec, shared `compactSummary` formatter. **Drops counted at the source of truth:** `PlaybackSource` now captures the `continuation.yield(frame)` `YieldResult` and increments a lock-guarded counter on `.dropped` (the `.bufferingNewest(3)` buffer evicting under a slow consumer), exposed as `droppedFrameCount`/`emittedFrameCount`. Demos own a `DetectionMetrics`, time `pipeline.detect` with `ContinuousClock`, bridge in the source's drop count, `reset()` on video/detector swap, and display it — macOS in the bottom bar, iOS as a top-trailing `.ultraThinMaterial` HUD pill. Answers the user's "start tracking how long inference takes, how many we skip vs. succeed."
- 🗓 Captured (backlog): **offline file-reader pre-pass** for pre-computed detection tracks → smooth playback (the long-parked `AVAssetReader`-backed `OfflineSource`, now explicitly requested). Landed in [`QUESTIONS.md`](./QUESTIONS.md).
- 👉 Next: user re-smokes (skeleton draws across frames now that the overlay re-mounts on switch; MRU animates; arrow-highlight-then-Enter feels right; metrics readout populates). On confirmation, **commit** this block, then start **M5·P5** — detection inspector.

---

## 2026-05-25 (work block 11)
- Did: **Shipped M5·P5 — detection inspector + richer metrics** (the data-truth panel). New `DetectionInspector` (Iris, `Overlay/`) reads the **same** `ResultStore.lookup(at:stale:)` the overlay reads (mirror by construction), renders each `Detection`'s self-describing fields (label · sourceModelID · geometry kind skeleton/quad/box · readout · per-joint min/mean/max confidence · bbox) with a **Raw** literal-dump toggle. New `DetectionMetricsView` + `DetectionMetrics` gained `emittedCount`/`setEmitted`; readout now leads with **raw counts** (`emitted · processed · dropped`, % demoted) per user ("the percentage is secondary"). Drops counted honestly at the `PlaybackSource` yield site (`.bufferingNewest(3)` `.dropped` result). Right pane reorganized (both demos): one scrolling pane — **Detector+Tuning (top) · Live detections (middle, minHeight 280) · Metrics (bottom)**, `Divider`s + wider spacing per user. Tests 205; both demo schemes build.
- 💡 **Inspector earned its keep immediately — it RESOLVED the body-pose mystery.** On a basketball clip, paused at t=4.00s, the inspector showed a clean detection (person, conf 0.79, **19 joints** at sane coords 0.55–0.91, 18 skeleton edges, readout "19 joints") while the canvas drew **nothing**. Metrics: `emitted 120 · processed 120 · dropped 0 · avg 9 ms · 29.8/s` — so on a fast Mac inference keeps up (0% drops is *correct*, not goofy). **Verdict: confirmed RENDER bug, not a detector/cache bug.** Data is present and correct; the overlay isn't painting it.
- 👀 **Render bug — narrowed, NOT yet fixed.** Blank on the **dancer clip (clean 1280×720 landscape) too**, so it's not the basketball's letterbox/orientation — it's a fundamental **macOS overlay-placement** bug. The pure converter math (`PlayerLayerConverter.convert`) is unit-tested and correct, so the fault is the **`videoRect` input**: the overlay is fed `playerLayer.videoRect`, a live AppKit/CALayer value. `PlayerView` (`PlaybackView.swift:171`) never overrides `isFlipped` → AppKit bottom-left origin, while the SwiftUI `Canvas` overlay draws top-left → suspected Y-origin mismatch placing the skeleton in a vertically-mirrored band (off the visible video / in the white area).
- 🗓 **Reverted a speculative fix to keep `main` honest.** Tried a macOS-only Y-flip (`videoRect.y = layerBounds.height - rawVideoRect.maxY`) + a DEBUG self-verifying overlay (stroke flipped=green / raw=red + raw/flipped/bounds numbers). **Unconfirmed** (couldn't verify without a runtime screenshot) → reverted both so the render path is back to its known state and the bug stays openly OPEN rather than falsely "fixed." Reconstructable from this entry if the instrument-and-smoke path is chosen.
- 📌 **Process learning (user called it):** we slid into patch-then-screenshot integration debugging, which can't converge when the agent can't see the running app — every hypothesis is a full round-trip. **Pivot for the render bug:** stop guessing. The durable fix is to **stop depending on `AVPlayerLayer.videoRect`** and compute the video's on-screen rect in **pure SwiftUI space** (`GeometryReader` size + the video's known pixel dimensions → aspect-fit math) — platform-identical, no AppKit coordinate-space question, unit-testable. Verify at the artifact level with **static letterboxed `DetectionLayer` previews** (CLAUDE.md "favorite pattern": render a known skeleton against an *offset* videoRect inside a larger frame, wide + tall cases, in Xcode — no app run). Current `DetectionLayer` previews use `videoRect = (0,0,360,240)` with no offset, so they'd never have caught an offset/flip bug — that gap is the thing to close first.
- Did: **Captured the work** (user asked, ahead of a `/clear` to restart fresh): committed the solid, tested pieces (P4 player fixes + paused-catch-up test + drop counting + metrics + inspector + layout) so nothing is lost; reverted the speculative render attempt; wrote this handoff.
- 👉 Next (fresh session): fix the **macOS overlay render bug** via the SwiftUI-space videoRect approach above — (1) add letterboxed `DetectionLayer` static previews that reproduce the blank (offset videoRect, wide+tall), (2) make those previews place the skeleton correctly, (3) derive the demo's videoRect in SwiftUI space instead of `playerLayer.videoRect`, (4) confirm in the demo with the inspector open. Then resume M5 close-out.

---

## 2026-05-25 — VideoGeometry consolidation + macOS overlay fix (M5·P6)

- **Did: resolved the macOS overlay render BLOCKER.** Root cause confirmed: the demo fed `AVPlayerLayer.videoRect` (AppKit bottom-left) into top-left Canvas math, so the overlay drew off the visible video. Fixed by computing the display box in pure SwiftUI space.
- **Built `VideoGeometry`** — a pure `Sendable` value type (`contentSize` + `containerSize` + `contentMode` → `displayRect` + Y-flip), now the single authority for normalized→view mapping. `DetectionLayer` takes a size-keyed `makeConverter:` with one `GeometryReader` as the measurement point. Retired `PlayerLayerConverter` (math folded in); unified `NormalizedGeometryConverting` (dropped `videoRect:`). iOS capture still delegates to `AVCaptureVideoPreviewLayer` via `PreviewLayerConverter`.
- **💡 Architecture pivot (the real lesson):** the first cut built rotation + mirror INTO the geometry. The static preview gallery surfaced that as the WRONG LAYER — frames + detections arrive UPRIGHT (capture rotates on the `AVCaptureConnection` + stamps `.up`; Vision gets `frame.orientation`; the player displays upright), so the overlay must only place upright-normalized coords into the displayed box. Rotation/mirroring are source concerns. Removed them. The "truth vs box" model: detection points are normalized truth tied to the upright source; the overlay scales that truth into whatever box the video is displayed in.
- **💡 The preview pattern earned its keep at the architecture level** — the ground-truth gallery (test card + probes through two independent code paths) caught a design misfit, not just pixels.
- **Fixed a player-reset regression** the migration introduced: reading `presentationSize` (`@Observable`) in the demo layout body + gating the overlay on it re-shaped the `ZStack` and re-made `PlaybackView` → `AVPlayerLayer` torn down mid-playback (`FigFilePlayer err=-12860` / `AQMEIO` timeouts). Fixed by reading `presentationSize` at draw-time inside `makeConverter` + pinning `PlaybackView` identity with `.id(ObjectIdentifier(controller.source))`.
- Library `swift test` 207 green; both demo targets (`IrisDemo-macOS`, `IrisDemo-iOS`) compile.
- 🗓 **Deferred → QUESTIONS.md:** (1) playback `preferredTransform` → upright detections for portrait clips; (2) capture front-camera `isVideoMirrored` (locked decision unimplemented); (3) pre-existing `DetectionInspector` Swift 6 warning in both demos.
- 👉 **Next:** source-orientation correctness — `PlaybackSource` `preferredTransform` (portrait) + capture front-mirror; or define M6/M7.

---

## 2026-05-25 — M6·P1 — Core ML conversion pipeline

- Did: **Defined M6 — Custom models + captioning** and wrote [`features/M6.md`](./features/M6.md) (P1 ✅ conversion pipeline · P2 📋 `CoreMLDetector` + `VisionObjectDecoder` YOLOv12 path A · P3 📋 `YOLOEnd2EndDecoder` + model-swap catalog/loading · P4 📋 captioning, stretch). Design spine: one `CoreMLDetector` with a pluggable `OutputDecoder` seam — `VisionObjectDecoder` (path A default), `YOLOEnd2EndDecoder` (path B), `DETRSetPredictionDecoder` (RF-DETR, later/additive). Everything downstream (`Detector`/`TunableDetector`, `DetectorCapabilities`, the M5·P4 `DetectorCatalog`/`ActiveDetectorSession`, overlay, inspector, cache) is reused; Core ML models register as catalog entries like the built-in Vision ones.
- Did: **Verified the PyTorch→Core ML toolchain empirically** (ultralytics 8.4.54, coremltools 9.0, Apple M1 Max) — not doc-only. Wrote the reusable [`tools/model-export/RUNBOOK.md`](../tools/model-export/RUNBOOK.md) (durable procedure for converting *any* external detector + per-model recipes + "adding the next model" checklist) and a working `inspect_model.py` (mandatory artifact-verify step, coremltools-9 API: `skip_model_load`, `userDefined`, `coordinatesOutputFeatureName`). Tooling is `uv`-managed, CLI-first, Python, **outside the Swift package** (never a SwiftPM dep); blessed default is a pinned-ephemeral `uv run --with …` one-liner (`--python 3.12` + `numpy<=2.3.5` pins are load-bearing), with a persistent `.venv` as the repeat-export option.
- Did: **Converted `yolo11n` / `yolo12n` / `yolo26n` to `.mlpackage`** and restructured artifacts under [`data/models/`](../data/models/) (`weights/` for `.pt`, `coreml/` for `.mlpackage`; both gitignored + regenerable, `README.md` tracked).
- Did: Landed the **DECISIONS** entry (2026-05-25 — start with YOLOv12 Path A, pluggable `OutputDecoder` seam) and the **RF-DETR backlog** item in [`QUESTIONS.md`](./QUESTIONS.md) (off the M6 critical path). Recorded the **empirical-over-docs** lesson to memory.
- 💡 Learned: **the YOLO Path A vs Path B finding.** **YOLOv12** (and classic Detect models) with `nms=True` is true **Path A** — Apple `NonMaximumSuppression` pipeline, `coordinates`+`confidence` outputs, 80 COCO labels in the NMS stage → Vision auto-decodes, **zero Swift decode**. **YOLO26 is Path B, not A** — ultralytics *forces* `nms=False` on end2end models (warns *"'nms=True' is not available for end2end models"*), so it always exports as a raw `[1,300,6]` tensor needing a **trivial** decoder (threshold + scale ≤300 rows, **NO NMS** — the one-to-one head self-dedupes; labels in `userDefined` `names`).
- 💡 Learned: `yolo download` is a **phantom command** (it errors) — `yolo export`/`yolo predict` auto-download `*.pt` on first use; the CLI `yolo export … format=coreml` is the *same code path* as `YOLO(…).export(format='coreml')`. And the **empirical re-run corrected doc-only conclusions**: the original doc-only pass predicted YOLO26 → Path A and framed RF-DETR as "ONNX→coremltools"; the actual export proved YOLO26 → Path B and RF-DETR's real route is a direct PyTorch→Core ML patched fork (FP32-only). Always verify the decode path against the artifact (`inspect_model.py`) before writing Swift.
- 🗓 Deferred: **RF-DETR Core ML spike** — off the M6 critical path. No native Core ML export (`rfdetr.export()` does ONNX/TFLite only; Roboflow won't add Core ML, issue #318 closed); proven OSS route is a direct PyTorch→Core ML patched fork ([`landchenxuan/rf-detr-to-coreml`](https://github.com/landchenxuan/rf-detr-to-coreml) on [`timnielen/rf-detr`](https://github.com/timnielen/rf-detr)), FP32-only, still path B needing a `DETRSetPredictionDecoder`. Captured in [`QUESTIONS.md`](./QUESTIONS.md); spike in isolation once the YOLO path proves the seam.
- 🗓 Captured (open): **runtime-tunable Core ML thresholds** — Path A bakes IoU/conf at export, so runtime tuning forces Path B or a re-export. Landed in [`QUESTIONS.md`](./QUESTIONS.md) + [`features/M6.md`](./features/M6.md); settle before/during P2.
- 👉 Next: start **M6·P2** — build `CoreMLDetector` + the `OutputDecoder` protocol + `VisionObjectDecoder`, wire the converted YOLOv12n through it, and prove it end-to-end in a demo player (boxes + COCO labels, zero Swift decode), with a fixture-based test. Confirm the iOS/macOS 26 auto-decode observation type against the installed SDK.

---

## 2026-05-26 — M6·P2 — `CoreMLDetector` + `VisionObjectDecoder`, YOLOv12 Path A end-to-end

- Did: **Shipped M6·P2 — the seam, proven.** New `Sources/Iris/Detection/CoreML/{OutputDecoder,CoreMLDetector,VisionObjectDecoder,CoreMLModelLoading}.swift` — `CoreMLDetector<Decoder: OutputDecoder>` is a `Sendable final class` wrapping a `CoreMLModelContainer`, runs `CoreMLRequest.perform(on:orientation:)` with `cropAndScaleAction = .scaleToFit`, declares capabilities `geometryKinds: [.box]` / `confidence: .probabilistic` / no knobs, and conforms to `Detector` **only** (not `TunableDetector` — tuning deferred to P3). The converted **YOLOv12n** runs through it end-to-end via Path A (Apple NMS pipeline, Vision auto-decodes, zero Swift box-decode).
- Did: **Catalog integration without faking tunability.** Added a minimal additive `DetectorCatalogEntry.make(id:displayName:detector:)` overload for plain (non-`TunableDetector`) `Detector`s + a `PassthroughRouter` — no fake `TunableDetector` conformance. New `Apps/Shared/DemoCatalog.swift` composes `builtInVision + YOLOv12n`; both demo apps (iOS + macOS) updated; `IrisDemo.xcodeproj` updated (added `DemoCatalog.swift` + bundled `yolo12n.mlpackage`, created a Resources phase for the macOS target which had none).
- Did: **Model fixturing.** `yolo12n.mlpackage` committed via Git LFS to `Tests/IrisTests/Fixtures/` (+ `Apps/Shared/`), `.gitattributes` glob added for `Apps/Shared/*.mlpackage/**`. The fixture test compiles `.mlpackage`→`.mlmodelc` at runtime; the demo loads Xcode's precompiled `.mlmodelc`. No `.mlmodelc` committed.
- Did: **Fixture test** `Tests/IrisTests/Detection/CoreMLDetectorFixtureTests.swift` runs 10 frames of `dancer-full-body.mp4` and asserts a `"person"` detection with valid box/confidence/`sourceModelID`, nil keypoints/skeleton. `swift build` clean, `swift test` **208 passed** (incl. the new test), both `xcodebuild` demo schemes BUILD SUCCEEDED, Swift 6 strict concurrency clean.
- Did: **Resolved both M6 open design questions.** Landed the **DECISIONS** entry (2026-05-26 — ship Path-A `CoreMLDetector` with baked thresholds; defer runtime tuning to P3) and moved the runtime-threshold ⚖️ + the observation-type 👀 to **answered** in [`QUESTIONS.md`](./QUESTIONS.md). Runtime-tunable thresholds are **re-scoped to P3** (they need the path-B raw-tensor decoder + Swift NMS).
- 💡 Learned: **the auto-decode observation type, confirmed against the MacOSX26.5 SDK.** `CoreMLRequest.Result == [any VisionObservation]`, and an NMS object-detection pipeline yields concrete **`RecognizedObjectObservation`** (logged `["RecognizedObjectObservation"]` via a temporary runtime probe). It carries `boundingBox: NormalizedRect` (`.cgRect` = normalized [0,1] lower-left, **matches `Detection.boundingBox` — no flip, no letterbox-inverse**), `labels: [ClassificationObservation]` (`.identifier` / `.confidence`, best-first), and `confidence: Float`. This closes the P1 open 👀 — the auto-decode *rule* (model output spec, not request type) held.
- 💡 Learned: **`cropAndScaleAction = .scaleToFit` does double duty.** It is aspect-preserving letterbox AND Vision applies the inverse transform to the returned boxes — so "never hardcode 640" is satisfied for free (Vision reads the input size from the model). No manual letterbox-inverse on the decode side.
- 💡 Learned: **`Package.swift` `.copy` vs `.process` for the `.mlpackage` fixture.** `.process` would flatten/transform the bundle; `.copy` preserves it as a directory so `Bundle.module.url(...)` resolves the `.mlpackage` intact for runtime compilation. Use `.copy` for `.mlpackage` resources.
- 💡 Learned: **the macOS demo target had no Resources build phase at all** — bundling `yolo12n.mlpackage` for the Mac app required *creating* one in `IrisDemo.xcodeproj` (the iOS target already had one). Easy to miss; the model silently won't be in the bundle otherwise.
- 📌 Decided: catalog integration is the additive non-tunable `DetectorCatalogEntry.make` overload + `PassthroughRouter` — keeps plain `Detector`s first-class in the picker without a sham `TunableDetector` conformance.
- 👉 Next: **M6·P3** — `YOLOEnd2EndDecoder` (Path B: YOLO26 raw `[1,300,6]`, threshold + scale ≤300 rows, **no NMS**, labels from `userDefined` `names`) through the same `OutputDecoder` seam, plus model-swap catalog/loading (bundled + file-picked, `availability == .modelNotReady` until present). The runtime-tunable-threshold variant is decided here alongside it.

---

## 2026-05-26 — M6·P3 — path-B `YOLOEnd2EndDecoder` + tunability (decoder half; loading still 📋)

- Did: **Shipped the path-B half of M6·P3 — the seam carries a raw-tensor decoder, proven.** `YOLOEnd2EndDecoder` runs the converted **YOLO26n** through the SAME `OutputDecoder` seam (no `CoreMLDetector` reshape), with a runtime confidence-threshold knob. New `Sources/Iris/Detection/CoreML/{COCOLabels.swift` (canonical COCO-80 constant), `YOLOEnd2EndDecoder.swift`, `CoreMLDetectorTunable.swift}` + `Tests/IrisTests/Detection/YOLOEnd2EndDecoderFixtureTests.swift`; bundled `yolo26n.mlpackage` in `Tests/IrisTests/Fixtures/` + `Apps/Shared/` (LFS-covered by existing globs).
- Did: **Tunability via conditional conformance — path A stays honestly non-tunable.** Added `TunableOutputDecoder: OutputDecoder` (sub-protocol) to `OutputDecoder.swift`, and `extension CoreMLDetector: TunableDetector where Decoder: TunableOutputDecoder` — so `YOLOEnd2EndDecoder` (path B) gains a runtime confidence knob while `VisionObjectDecoder` (path A) keeps its baked, honest thresholds. Made `CoreMLDetector`'s container/decoder internal + added a container-reusing rebuild init; `capabilities.tunableKnobs` derived from the decoder. `apply(_:)` tiering: no-op → `.view`; **raise threshold → `.filter`** (higher-conf rows are a strict subset of the cache — a pure post-hoc predicate, no re-inference); **lower → `.detector(rebuilt:)`** (previously-dropped rows aren't cached — rebuilds the detector around the SAME compiled container, no model recompile). Hot-swap-by-rebuild per the M4 doctrine.
- Did: **Catalog wiring (decoder side only).** `DemoCatalog.swift` gains a YOLO26n entry via the tunable `.make<D: TunableDetector>` path; YOLOv12n stays on P2's plain (non-tunable) overload. `Package.swift` `.copy`/`exclude` for the yolo26n fixture; `IrisDemo.xcodeproj` bundles yolo26n into both targets. (Model-swap/file-picker *loading* is the remaining P3 half — still 📋.)
- Did: `swift build` clean, `swift test` **209 passed** (P2 was 208; +1 path-B fixture test, no regressions), both `xcodebuild` demo schemes BUILD SUCCEEDED, Swift 6 strict concurrency clean.
- 💡 Learned (empirical plan-corrections — the artifact disproved M6.md's P3 assumptions; verify-before-Swift caught it):
    - **Labels come from nowhere in the export — supplied externally.** The converted `yolo26n` has empty metadata (`METADATA keys: []`); path-A models carry labels in the NMS stage's `stringClassLabels`, but this raw `mlProgram` export embeds none. So labels are supplied via `YOLOEnd2EndDecoder(labels:)` + the COCO-80 constant — **not** read from `userDefined`/`names` as the P1 plan assumed.
    - **The output is xyxy in 640-px space, not xywh.** Single tensor `var_1441` shape `[1,300,6]`; rows are `[x1,y1,x2,y2,confidence,classIndex]` = xyxy in 640-pixel space (NOT xywh, NOT normalized) — confirmed by running the model on a real frame.
    - **The Sendable seam exposes `shapedArrayValue`, not `multiArrayValue`.** Raw-tensor models return a `CoreMLFeatureValueObservation`; on the MacOSX26.5 SDK the Sendable `MLSendableFeatureValue` exposes the tensor via `shapedArrayValue(of: Float.self)` — there is no `multiArrayValue` on the sendable wrapper.
    - **Path B owns the letterbox-inverse + Y-flip.** Unlike path A, Vision does NOT inverse-map coords for raw-tensor output — the decoder inverts the `.scaleToFit` letterbox itself (using `frameSize`) AND flips Y (YOLO top-left → Vision lower-left). This was the box-misalignment risk; verified correct (logged person box on the dancer clip ≈ horizontally centered, full height).
- 📌 Decided: tunability rides the `TunableOutputDecoder` sub-protocol + conditional `CoreMLDetector` conformance, so path A stays non-tunable while path B gains the knob. → [`DECISIONS.md`](./DECISIONS.md) (2026-05-26). Moved the runtime-threshold answered entry in [`QUESTIONS.md`](./QUESTIONS.md) from "re-scoped to P3" to "implemented in P3".
- 👉 Next: finish **M6·P3** — model-swap/file-picker loading: register Core ML models as `DetectorCatalog` entries supporting **both** bundled (small FP16, Git LFS, loaded + `prewarm()` at launch) and **file-picked** (large/optional/user-supplied, `availability == .modelNotReady` until present) models.

---

## 2026-05-26 — M6·P3 — model loading (prewarm + bundled-at-launch + file-picked Path-A) — **closes P3**

- Did: **Shipped the loading half of M6·P3 — P3 now DONE (✅), both halves landed today.** Real `CoreMLDetector.prewarm()`, launch-prewarm of the bundled models, and file-picked Path-A models with a `.modelNotReady` picker UI. `swift build` clean, `swift test` **212 passed** (P3-decoder was 209; +3 load-path tests, no regressions), both `xcodebuild` demo schemes BUILD SUCCEEDED, Swift 6 strict concurrency clean.
- Did: **Real `prewarm()` (was a no-op in P2).** `CoreMLDetector.prewarm()` now allocates a synthetic black 640×640 BGRA pixel buffer (new internal `makeWarmupPixelBuffer(width:height:)` in `CoreMLModelLoading.swift`) and runs one `CoreMLRequest.perform` to force Core ML to compile/load its compute path before the first real frame — fully best-effort (failures logged via a file-level `coreMLDetectorLogger` OSLog + swallowed). Honest contrast: the Vision built-ins keep `prewarm()` a no-op (no first-inference cost to hide); Core ML's first-inference cost is genuine.
- Did: **Bundled prewarm-at-launch.** New `@MainActor @Observable DemoModelStore`; each demo `ContentView`'s `.task` calls `modelStore.prewarmBundledModels()`, which loads + prewarms yolo12n/yolo26n off-main and caches the warmed detectors; `makeSession` reuses the warmed instance. Doesn't block launch; degrades gracefully if a model isn't in Resources.
- Did: **File-picked Path-A models (Path-A only — self-describing, zero label config).** A `coreml.custom` catalog entry starts as a `NotReadyDetector` placeholder (`availability == .modelNotReady`); selecting it / a "Load model…" button opens a `.fileImporter` (macOS) / `DocumentPicker` (iOS) for `.mlpackage`/`.mlmodel`; on pick → `CoreMLModelLoading.compileAndLoad(at:)` → `CoreMLDetector(decoder: VisionObjectDecoder(), modelIdentifier: <basename>)` → `prewarm()` → stored in `DemoModelStore.pickedDetector` → re-select `coreml.custom` so the existing session-rebuild `swapDetector()` flow runs it. Failure surfaces via the demo's error text and leaves the slot `.modelNotReady`.
- Did: **First UI use of `DetectorAvailability`.** The playback picker reads availability and dims/annotates `.modelNotReady` rows ("— not loaded" → model name once loaded). `DemoCatalog.detectors` became `detectors(store:)`; UI + file-picking live in the demo (Iris stays loading-primitive + detector + availability) per the CLAUDE.md consumer-owns-UI doctrine.
- Did: New tests `Tests/IrisTests/Detection/CoreMLModelLoadingTests.swift` — prewarm-doesn't-break-detector, file-picked load-path detects "person", availability reflects load state.
- 💡 Learned / 📌 **Verified correction (a build sub-agent misread, now checked):** the bundled models DO ship and launch-prewarm engages. The sub-agent claimed the models "aren't in `project.yml` Resources" — that was a misread. The authoritative checked-in `Apps/IrisDemo.xcodeproj/project.pbxproj` DOES bundle both `yolo12n.mlpackage` and `yolo26n.mlpackage` in both targets' Resources phases (added in P2 + P3-decoder).
- 💡 Learned: the real (pre-existing, minor) issue is **drift between `Apps/project.yml` (xcodegen bootstrap) and the hand-edited `.pbxproj`** — `project.yml` doesn't list the `.mlpackage` Resources, so an xcodegen regen would *drop* them. The hand-edited `.pbxproj` is authoritative. Tracked as 🗓 backlog, not a P3 blocker. → [`QUESTIONS.md`](./QUESTIONS.md), [`STATUS.md`](./STATUS.md).
- 🗓 Deferred: **Path-B file-picking.** File-picked models currently accept Path-A only (NMS-pipeline, self-describing). A Path-B picked model would need a label-supply UI + output-spec auto-detect — deferred. → [`QUESTIONS.md`](./QUESTIONS.md).
- 📌 Decided: real synthetic-buffer prewarm + bundled-at-launch + file-picked Path-A-only, with file-picking/model UI in the demo and Iris owning the loading primitive + detector + availability. → [`DECISIONS.md`](./DECISIONS.md) (2026-05-26).
- 👉 Next: **decide whether to take P4** (captioning — stretch: `Captioner` protocol + a Foundation Models backend) **or close M6** here. M6's core (P1–P3) is done; P4 is the only remaining stretch.

---

## 2026-05-26 — M6 closed (P1–P3); captioning (P4) dropped

- Did: **Closed M6 — Custom models.** P1–P3 shipped + committed today (`da3b471` M6·P2 CoreMLDetector + VisionObjectDecoder / `1248d1d` M6·P3 YOLOEnd2EndDecoder + runtime confidence knob / `dfdd777` M6·P3 model loading; P1 conversion pipeline was `5fcb3bd`). End state: the Core ML detector runs end-to-end — conversion pipeline → path-A `VisionObjectDecoder` (Vision auto-decodes) → path-B `YOLOEnd2EndDecoder` (raw `[1,300,6]`, runtime confidence knob via conditional `TunableDetector`) → bundled + file-picked Path-A model loading. M1–M6 all ✅.
- Did: **Dropped P4 (captioning) — not deferred.** Marked 🚫 in [`features/M6.md`](./features/M6.md) §P4, landed the close + drop in [`DECISIONS.md`](./DECISIONS.md) (2026-05-26), and added an answered entry in [`QUESTIONS.md`](./QUESTIONS.md). Rewrote [`STATUS.md`](./STATUS.md) to the milestone-boundary overview tree (M6 annotated `(P1–P3 ✅ · P4 🚫)`, matching M4).
- 💡 Learned / 📌 **Foundation Models is text-only — verified against the MacOSX26.5 SDK.** `LanguageModelSession.respond(to:)` takes a `Prompt`/`String` and returns `Response<String>` (or a structured `Generable`); `Prompt` is built **purely from text** — there is **no `CGImage`/`CVPixelBuffer`/`CIImage` image-input path** (the only image-ish symbol, `logFeedbackAttachment`, is unrelated). Vision has **no natural-language caption request** (its catalog is detection/classification/saliency/text-recognition; the closest, `ClassifyImageRequest`, returns labels, not a caption). So on-device image→text captioning is **infeasible without a VLM** — it needs a vision-language model converted to Core ML, an off-plan lift, *not* a "Foundation Models backend". This corrects the original assumption in [`BRIEF.md`](./BRIEF.md) §3 and [`features/M6.md`](./features/M6.md). Combined with the user deprioritizing captioning, P4 is dropped; a future captioning milestone would start from a Core ML VLM.
- ℹ️ The off-critical-path RF-DETR `DETRSetPredictionDecoder` item stays in [`QUESTIONS.md`](./QUESTIONS.md) as the future additive plug-in it already was — unaffected by the M6 close.
- 👉 Next: **milestone boundary — decide the next milestone.** M7 (Dataset, BRIEF §6) is the next milestone-path entry — define it → draft [`features/M7.md`](./features/M7.md). The M5·P6 **source orientation-correctness** carryover (playback `preferredTransform` + capture front-mirror) is a smaller alternative if a quick win is preferred.

---

## 2026-05-26 — bugfix: mid-playback detector swap (demo)

> ⚠️ **Superseded 2026-05-27** — this entry's "single-consumer race" root cause is a misdiagnosis and `f4a6284` is a **no-op** (the single stored `AsyncStream` dies permanently on consumer cancel — there is no race). The reported symptom was never actually fixed here; reload only masks it. See the 2026-05-27 "swap root-cause correction" entry at the bottom.

- Did: **Fixed the "swap model mid-video does nothing until reload" bug** (user-reported). Root cause was demo-side, not library: `PlaybackSource.frames` is a **single shared `AsyncStream`** (one intended consumer), but the demo's `buildSessionAndStartDetection(on:)` cancelled the old detection task and *immediately* spawned a new consumer of that same stream. Cancellation is cooperative, so for a window two `for await` loops raced; the one-shot frame re-emitted by `swapDetector()`'s seek (the mechanism that makes the new model's output appear while paused) could be delivered to the dying old task — which either discarded it (new detector never runs) or ran the **old** router's detector and wrote stale detections into the shared cache. Reload masked it by building a fresh `PlaybackSource`/stream with no leftover consumer.
- Did: **Fix — serialize teardown before re-consume.** Made `buildSessionAndStartDetection(on:)` `async` and added `await detectionTask?.value` immediately after `detectionTask?.cancel()`, so the old `for await` fully drains before the new task touches the stream — single consumer always. (`AsyncStream`'s iterator is cancellation-aware, so the await returns promptly.) Rewired the 3 call sites per file: `swapDetector()` now `await`s the build **before** the re-emit seek (the crux of the ordering), and the initial-load path wraps build + `togglePlay` so playback starts only once the consumer is in place; `teardown()` untouched. Applied identically to **both** demos ([`Apps/IrisDemo-macOS/ContentView.swift`](../Apps/IrisDemo-macOS/ContentView.swift) `buildSessionAndStartDetection` ~:636, [`Apps/IrisDemo-iOS/ContentView.swift`](../Apps/IrisDemo-iOS/ContentView.swift) ~:742).
- Verified: `swift build` clean, `swift test` **212 passed**, both `xcodebuild` demo schemes **BUILD SUCCEEDED** (macOS + iOS Simulator iPhone 17 / iOS 26.5), Swift 6 strict concurrency clean. Not committed.
- 🗓 Deferred (user chose "accept the gap"): **no regression test** — the buggy logic lives inline in the demo `ContentView` SwiftUI `View` (private `buildSessionAndStartDetection`/`swapDetector`), which has no testable seam and isn't in the `swift test` package. A real guard needs a small `PlaybackDetectionCoordinator` extracted from the demo (task lifecycle + swap), then a fixture test (`ManualTickDriver` + two detectors, swap mid-stream, assert the new detector wins). → [`QUESTIONS.md`](./QUESTIONS.md).
- 👉 Next: unchanged — **milestone boundary, decide the next milestone** (M7 Dataset front-runner). → [`STATUS.md`](./STATUS.md).

---

## 2026-05-27 — analysis: demo/library boundary → `PlaybackDetectionCoordinator` decision

- Did: **Traced four flows** (switch video, switch detector, change a detector filter, player controls) end-to-end across both demos' `ContentView` and `Sources/Iris/` to locate the demo/library boundary. Wrote it up under [`explorations/2026-05-27-demo-library-boundary/`](../explorations/2026-05-27-demo-library-boundary/) (QUESTIONS + SYNTHESIS + RECOMMENDATIONS).
- 💡 Key finding — **an asymmetry.** Player controls are *already* cleanly in the library (`ScrubberModel` protocol + `PlaybackController` + generic `Scrubber`; the demo writes one line, and the upcoming **optional external controls** need no library changes — they just drive the existing `PlaybackController`). But **detection-session orchestration** (the detect loop + lifecycle, `ResultStore` + `DetectionMetrics` ownership, `makeSession` wiring, the `onDetectorTierChange → seek` pause-emit hook, video/detector swap sequencing) is ~200 lines of glue **duplicated near-verbatim across both ContentViews** — that's the bloat, and it's why the 2026-05-26 swap bug was untestable.
- 💡 Tuning is the best-factored of the three: the three-tier system (`.view`/`.filter`/`.detector` in `TuningModel`/`TuningRouter`) is library; the *only* demo glue is wiring the pause-emit hook to `source.seek` — demo-side only because the demo owns the loop.
- 📌 Decided: extract a `@MainActor @Observable` **`PlaybackDetectionCoordinator`** into **`Sources/Iris/Playback/`** (placement settled with the user). Intents `setSource(_:detector:)` (demo passes a `PlaybackSource` — it holds the sandbox scope — not a URL) + `selectDetector(_ entry:)`. Demo retains file/scope/MRU/catalog+custom-model UI/layout. Source-agnostic core **not** pre-split (lift to `Detection/` only if capture-side detection lands). Closes the untested-swap-seam gap (coordinator is library-testable). → [`DECISIONS.md`](./DECISIONS.md) (2026-05-27).
- Did (git): the 2026-05-26 **swap bugfix was uncommitted on the unmerged `m6-coreml-conversion` branch** (6 commits = all of M6, never merged). Per the user: **fast-forwarded M6 onto `main`**, then cut **`fix-playback-detector-swap`** off `main` for the bugfix + this analysis. (No git remote — all local.)
- 🗓 Scope choice: kept clear-prep light — **decision + STATUS only**, no feature plan drafted yet (the exploration RECOMMENDATIONS already holds the API shape + phasing).
- 👉 Next: **define the coordinator** → draft [`features/playback-detection-coordinator.md`](./features/playback-detection-coordinator.md) (P1 coordinator + regression test in the library; P2 rewire macOS demo; P3 rewire iOS demo), then build. M7 (Dataset) remains the milestone-path entry behind it. → [`STATUS.md`](./STATUS.md)

---

## 2026-05-27 — define: `PlaybackDetectionCoordinator` feature plan

- Did: **Drafted [`features/playback-detection-coordinator.md`](./features/playback-detection-coordinator.md)** — transcribed the exploration RECOMMENDATIONS into a working feature plan, **grounded against the real source types** (not the sketch's working names). Sections: scope/intent, API sketch, owns/stays-in-demo split, 4 phases (P1 land in `Playback/` + swap regression test · P2 rewire macOS · P3 rewire iOS · P4 🗓 external-controls + source-agnostic `DetectionRunner`), opens/risks. The coordinator graduates ✏️→📋 — now in the STATUS tree.
- 💡 Verified the API sketch held up; five benign sketch↔source refinements folded in: (1) `makeSession` takes `any DetectionCache` (`ResultStore` conforms), not a `ResultStore` param; (2) `make(detector:)` is two overloaded static factories — `make<D: TunableDetector>(…)` and the plain-`Detector` one (→ `PassthroughRouter` + `EmptyView`); (3) `onDetectorTierChange` is `(@Sendable @MainActor () -> Void)?` and the real hook spawns a `Task` around the async `source.seek`; (4) `PlaybackView` takes `controller.source`, not the controller; (5) the test seam is `PlaybackSource(url:driver:)` with `ManualTickDriver` (+ `MockSource`/`MockDetector`).
- 🗓 Branch deferred: drafting is branch-neutral; proceeded on `fix-playback-detector-swap` (path 1 — so the 2026-05-26 fix `f4a6284` merges *with* its P1 regression test). ⚖️ The merge-now-vs-hold call is still open in [`QUESTIONS.md`](./QUESTIONS.md) — confirm before P1 build.
- 👉 Next: **build P1** — `Sources/Iris/Playback/PlaybackDetectionCoordinator.swift` + the swap regression test (`swift test`, library-testable now); keep both demos green on their inline glue until P2/P3. → [`STATUS.md`](./STATUS.md)

---

## 2026-05-27 — build: PlaybackDetectionCoordinator P1 + swap root-cause correction

- Did: **Landed P1** — [`Sources/Iris/Playback/PlaybackDetectionCoordinator.swift`](../Sources/Iris/Playback/PlaybackDetectionCoordinator.swift) (`@MainActor @Observable`; one detect loop per source, in-place router swap, cache/metrics ownership, self-wired pause-emit hook) + its regression test [`Tests/IrisTests/Playback/PlaybackDetectionCoordinatorTests.swift`](../Tests/IrisTests/Playback/PlaybackDetectionCoordinatorTests.swift). Commit `51743c7`. `swift test` **215 pass**, Swift 6 strict-concurrency clean. Demos untouched — they keep their inline glue until P2/P3.
- 💡 **Root-cause correction (2026-05-27).** The 2026-05-26 fix `f4a6284` (cancel → drain → respawn) is a **no-op**. `PlaybackSource` exposes a single *stored* `AsyncStream` (`_frames` + one `continuation`); cancelling its consuming task **terminates the stream permanently**, so a respawned `for await source.frames` receives **zero** frames and every later `yield` returns `.terminated` (confirmed with an isolated `AsyncStream` repro). There is **no race** — the drain serializes nothing load-bearing; the stream is simply dead after cancel. The reported "swap does nothing until reload" symptom was therefore **never actually fixed** by `f4a6284` (reload only masks it by building a fresh source → fresh stream). Caught **while building P1**: the regression test reproduced the no-op swap (new detector saw zero frames) until the design changed from respawn to a **single loop per source + in-place router swap** — the loop reads the live `session.router` on every frame, so a `selectDetector` swap deterministically routes the next frame through the new detector with no stream re-iteration.
- 🚩 Implication: the demos currently show **no detections after a mid-video detector swap until reload** — their respawn glue is non-functional. P2/P3 rewiring the demos onto the coordinator will **actually fix the demo swap bug for the first time**, not merely centralize code.
- 👉 Next: **build P2** — rewire [`Apps/IrisDemo-macOS/ContentView.swift`](../Apps/IrisDemo-macOS/ContentView.swift) onto the coordinator (one `@State` coordinator; delete the duplicated `buildSessionAndStartDetection`/`swapDetector`/`swapToExternal`/`teardown` glue; bind library views to coordinator outputs). Verify by hand — swap mid-video, tune a `.detector` knob while paused, scrub, open a new video — plus `xcodebuild` for the macOS scheme. → [`STATUS.md`](./STATUS.md)

---

## 2026-05-27 — build: rewire both demos onto the coordinator (P2 + P3)

- Did: **P2 — macOS demo** ([`Apps/IrisDemo-macOS/ContentView.swift`](../Apps/IrisDemo-macOS/ContentView.swift), commit `1ea2cd1`, **−94 net**). Collapsed five `@State` (controller/resultStore/detectionTask/metrics/session) → one `@State private var coordinator`; deleted the non-functional respawn glue; call sites became `setSource(_:detector:)`+`togglePlay` / `selectDetector` / `teardown`; views bind to `coordinator.controller`/`.resultStore`/`.session?.router`/`.session?.settingsView`/`.metrics`. `xcodebuild` macOS scheme **BUILD SUCCEEDED**.
- Did: **P3 — iOS demo** ([`Apps/IrisDemo-iOS/ContentView.swift`](../Apps/IrisDemo-iOS/ContentView.swift), commit `ad7428d`, **−102 net**). Mirrored P2 on `PlaybackContentView`; the iOS-only **bundled-fixture `loadFixture`** path preserved and routed through `setSource` (no security scope — bundle resource; it's first-appear-only, guarded by `coordinator.controller == nil`, so no prior scope to leak). `CaptureContentView` (camera tab) untouched. Refreshed a stale scope-accounting class doc to match the coordinator's ordering contract. `xcodebuild` iOS scheme (iPhone 17 / iOS 26.5) **BUILD SUCCEEDED**.
- 💡 **Sandbox-scope ordering** ported correctly to both: acquire the new scope before `setSource`, capture the prior scope, release it strictly **after** the coordinator's `setSource`/`teardown` `await` returns (i.e. after the prior source's `invalidate()`) — AVF never reads a URL whose scope was dropped.
- Did: both builds strict-concurrency clean; the one remaining warning in each (`DetectionInspector` `displayTimeSource` → `currentTime` from a Sendable closure) is **pre-existing** (M5·P6 carryover, [`QUESTIONS.md`](./QUESTIONS.md)), not introduced here.
- 🗓 **P4 deferred** (external-controls polish + source-agnostic `DetectionRunner`) — off the critical path; the coordinator's `controller` already exposes the external-controls seam, and the runner extraction waits for a capture-side consumer ([`QUESTIONS.md`](./QUESTIONS.md) 2026-05-27).
- 👀 **Remaining gate — hands-on smoke.** P1–P3 are `xcodebuild`-green but the swap fix is only *behaviorally* confirmable by hand (no headless seam for the demo UI): on both demos, swap detector mid-playback (should now show the new detector — the bug `f4a6284` never fixed), tune a `.detector`-tier knob while paused, scrub, open a new video.
- 👉 Next: **smoke both demos, then fast-forward `fix-playback-detector-swap` → `main`.** After merge, **M7 — Dataset** (BRIEF §6) is the milestone-path next. → [`STATUS.md`](./STATUS.md)

---

## 2026-05-27 — close: coordinator smoked + merged to `main`

- Did: **Both demos smoke-tested** (user-confirmed) — mid-video detector swap now shows the new detector's output, `.detector`-tier knob tunes while paused, scrub and open-new-video both clean, on macOS and iOS. The swap bug `f4a6284` never actually fixed is fixed for the first time via the coordinator's single-loop + in-place router swap.
- Did: **Fast-forwarded `fix-playback-detector-swap` → `main`** (`f0b7858 → 438998d`, 8 commits, +1407/−401): coordinator + regression test, both demos rewired, swap root-cause correction, feature plan + demo/library-boundary exploration. Branch deleted. `main` is local-only ahead of any remote (not pushed).
- 🗓 **P4 stays deferred** (external-controls polish + source-agnostic `DetectionRunner`) — waits for a capture-side consumer ([`QUESTIONS.md`](./QUESTIONS.md)).
- 👉 Next: **M7 — Dataset** (BRIEF §6) — the milestone-path next now that the coordinator arc is closed. → [`STATUS.md`](./STATUS.md)

---

## 2026-05-27 — define: demo simulator-runnable (feature plan + branch)

- Did: **Drafted [`features/demo-sim-runnable.md`](./features/demo-sim-runnable.md)** on new branch `demo-sim-runnable` — make the **iOS demo** pleasant to run with no camera (iOS Simulator + **Mac Designed-for-iPad**; not the native `IrisDemo-macOS` target). Four phases, all demo + tooling, **no `Sources/Iris/` changes**: P1 Playback-first sidebar-adaptable `TabView` · P2 camera fallback page when `AVCaptureDevice.default(for: .video) == nil` (no failed start / hang) · P3 `UIFileSharingEnabled` + `LSSupportsOpeningDocumentsInPlace` to expose Documents in Files.app · P4 `just sim-add-video <path>` (resolves bundle id from gitignored `Local.xcconfig`, copies into `simctl get_app_container booted … data`/Documents) + verify.
- Did: decisions locked via the user — sidebar-adaptable layout (vs. plain reorder), file-sharing + copy helper (vs. in-app Documents auto-discovery), tracked as feature+branch (vs. a milestone). **M7 — Dataset stays the milestone-path next**; this runs alongside.
- 🗓 Found: no `justfile` exists yet (P4 creates one); iOS bundle id is per-developer in gitignored `Apps/IrisDemo-iOS/Local.xcconfig` (`us.fofu.iris.demo` here), so the helper parses it rather than hardcoding.
- 👉 Next: **build P1** — migrate the iOS demo root `TabView` (`ContentView.swift:25–35`) from `.tabItem` to the value-based `Tab(...)` API, Playback first, `.tabViewStyle(.sidebarAdaptable)`. Awaiting go-ahead. → [`STATUS.md`](./STATUS.md)

---

## 2026-05-27 — build: demo simulator-runnable P1–P4

- Did: **Built all four phases** on `demo-sim-runnable`, one commit each, iOS scheme `xcodebuild`-green (iPhone 17 / iOS 26.5) throughout; **no `Sources/Iris/` changes**.
  - **P1** (`3a1388b`) — [`Apps/IrisDemo-iOS/ContentView.swift`](../Apps/IrisDemo-iOS/ContentView.swift): root `TabView` migrated from `.tabItem` to the value-based `Tab(...)` API, **Playback first** (leftmost ⇒ default), `.tabViewStyle(.sidebarAdaptable)` (left sidebar on iPad/Mac, bottom bar on iPhone).
  - **P2** (`1319501`) — `CaptureContentView`: added `cameraAvailable` (`AVCaptureDevice.default(for: .video) != nil`); the `.task` now `guard cameraAvailable else { return }` before any AVF work, and renders a `ContentUnavailableView` (`camera.fill`) explaining the Simulator/Mac have no camera. Physical-iPhone path unchanged — no failed start, no hang.
  - **P3** (`8a9e9c1`) — [`Apps/IrisDemo-iOS/Info.plist`](../Apps/IrisDemo-iOS/Info.plist): `UIFileSharingEnabled` + `LSSupportsOpeningDocumentsInPlace` = true → Documents reachable in Files.app, browsable from the existing `DocumentPicker`.
  - **P4** (`213e149`) — new repo-root [`justfile`](../justfile) with `sim-add-video <path>`: resolves the bundle id from xcconfig (no hardcode), `xcrun simctl get_app_container booted <id> data`, copies into `Documents/`; clear errors for missing file / xcconfig / no-booted-sim-or-app.
- 🚩 Caught + fixed a wrong-id bug in P4 before reporting done: there are **two** `Local.xcconfig` files with different ids — `Apps/IrisDemo-iOS/Local.xcconfig` (`us.fofu.iris.demo`, the one the iOS target's `Shared.xcconfig:31` actually `#include?`s) and a stray repo-level `Apps/Local.xcconfig` (`dev.griff.iris.demo`). The recipe initially preferred the stray file; reordered to check the **per-target** file first (authoritative), repo-level as fallback only. Verified it now resolves `us.fofu.iris.demo`. (P4 commit amended.)
- ℹ️ Pre-existing `DetectionInspector`/`currentTime` Sendable-closure warning still the only one; not introduced here (confirmed against a clean stash).
- 👀 **Remaining gate — hands-on smoke** (no headless seam): on the **iOS Simulator** and **Mac (Designed for iPad)** — launches to Playback (sidebar layout); Capture tab shows the fallback page; `just sim-add-video <clip>` → Pick video → Files → On My iPhone → Iris Demo plays it.
- 👉 Next: **smoke `demo-sim-runnable`, then merge → `main`.** After merge, **M7 — Dataset** (BRIEF §6) is the milestone-path next. → [`STATUS.md`](./STATUS.md)

---

## 2026-05-28 — merge demo-sim-runnable; define M7 — Dataset

- Did: **fast-forwarded `demo-sim-runnable` → `main`** (`4cc0c62..40cf0de`, 6 commits, no merge commit, not pushed). Clean tree. ⚠️ Merged **without** the owed hands-on smoke (the stated remaining gate) — flagged to the user and tracked in STATUS so a sim/layout regression isn't silently sitting on `main`. Branch not deleted yet.
- Did: **drafted [`features/M7.md`](./features/M7.md)** — M7 scoped as a **playback-context dataset-curation loop**: scrub → spot a wrong frame → flag (cheap, metadata-only) → later extract flagged frames (deferred headless batch) to image + COCO sidecar. Grounded the design in existing seams: a playback frame is already addressable as `(asset, PTS)`, `seek(to:)` `.zero`-tolerance round-trips a stored PTS to the exact frame, and `PlaybackSource`'s default `TaskTickDriver` was built "for headless use (e.g. dataset processing)" — M7 is its intended consumer. Four phases: **P1** `FrameRef`+`AssetFingerprint`+`FlagStore` library core (+ `Detection` Codable) + tests · **P2** flagging UI (bookmark toggle, timeline markers, flagged-frames panel, jump-to-flag) · **P3** `DatasetSink`+`FolderDatasetSink`+headless `DatasetBuilder` with deterministic-naming dedup · **P4** COCO sidecar schema + `COCOExporter`.
- Did: **locked three forks with the user** — (1) asset identity = **content fingerprint** (filename+size+duration+optional head-hash), not bare URL, so reload-finds-flags survives moves; (2) **per-image COCO sidecar + merge-exporter**, not one growing file; (3) output to **app-managed `<Documents>/iris-dataset/`** (already Files.app-visible via the just-merged P3 file-sharing keys), `DatasetSink` protocol leaves iCloud/S3 room.
- Decided: **canonical frame address = PTS** stored as exact `CMTime` `{value,timescale}` (rational, no float drift); frame-index rejected (variable frame rate). **FlagStore is library-side** (Iris's first on-disk persistence) but takes an injected `baseDir` — the library does not hardcode the app sandbox.
- 🗓 Scope call: M7 v1 is **playback only**. Deferred extraction needs re-seek; **live-capture flagging** would have to snapshot the pixel buffer at flag time (a different, real-time-write path) — noted as a follow-on, not v1, even though BRIEF §6 says "both contexts".
- 🚩 Opens parked in [`features/M7.md`](./features/M7.md): PTS-vs-`currentTime()` exactness (may need snap-to-sample on flag); `Detection` Codable may want a `DetectionRecord` DTO (self-describing skeleton/readout); reuse an Overlay-side CIImage path for PNG export rather than a new converter.
- 👉 Next: **build M7·P1** — pure library + tests, no UI. Also owed: smoke the merged demo on Simulator + Mac (Designed for iPad). → [`STATUS.md`](./STATUS.md)

---

## 2026-05-28 (cont.) — build: M7·P1 library core + smoke the merged demo

- Did: **built M7·P1** on branch `m7-dataset` (`e685f09`) — the flag-side library core, no UI. New `Sources/Iris/Dataset/`: `AssetFingerprint` (content-derived `id` = byteSize + ms-quantized duration + optional 1 MiB SHA-256 head-hash; **filename excluded** so a rename/move preserves identity), `FrameRef` (`(AssetFingerprint, CMTime)`; PTS stored as exact `CMTime` via a 4-field `CMTimeCodable` wrapper — bit-exact round-trip), `FrameFlag`+`FlagReason` (`.wrong`/`.nearMiss`/`.other`), `FlagStore` (`@MainActor @Observable`, injected `baseDir`, per-asset `.v1` JSON envelope at `<baseDir>/iris-dataset/flags/<id>.json`, atomic writes, lazy load+cache — Iris's first library-side on-disk persistence). `Detection` (+ `Keypoint`/`Mask`/`Skeleton`/`Readout`) gained direct synthesized `Codable`.
- Decided (resolves two M7 opens): **`Detection` → direct synthesized `Codable`, no DTO** — every field is flat/synthesizable; the self-describing skeleton/readout aren't polymorphic. Swift won't synthesize from a cross-file extension, so `: Codable` lives on the type decls and the rationale lives in `Dataset/Detection+Codable.swift` (+ a `Mask` tripwire: a future pixel payload must stay `Codable` or the build breaks). **PTS → store raw, don't snap in P1** — snapping needs the P3 sample-table walk; `// M7·P3:` tripwire left on `FrameRef` to validate raw-vs-snap and add `snapped(in:)` on the extraction side if needed.
- Verified: `swift test` **225 pass / 0 fail** (8 Dataset tests: fingerprint-survives-rename, distinct-clips-distinct-id, PTS bit-exact, `Detection` Codable, `FlagStore` reload/toggle/dedup). Swift 6 strict-concurrency clean. Reviewed all five files + the `Detection` diff before committing — clean, matches house idiom.
- Did: **automatable demo-sim smoke** (isolated worktree, on `main` state) — `IrisDemo-iOS` builds GREEN for the iOS Simulator; launches clean on **iPhone 17** (bottom bar, Playback default, detections+scrubber render) and **iPad Pro M5** (`.sidebarAdaptable` form, not a bottom bar). Screenshots surfaced to the user. Runtime is iOS 26.5 (only iPhone 17 / iPad M5 sims exist) so those substituted for the 16/M4 named — layout keys on size class, validates the same behavior.
- 🗓 Still owed (tracked in STATUS, not blocking): **two manual smoke taps** — Capture tab → no-camera fallback page (`idb` not installed → no headless tap; source confirmed wired at `ContentView.swift:64–71,156–167`, session guarded so it can't start), and the iPad sidebar-toggle → expanded sidebar.
- 👉 Next: **M7·P2** — flagging UI (bookmark toggle on scrubber, timeline flag markers, flagged-frames side panel, jump-to-flag via `seek(to: pts)`). Thin built-in views per the M4 UI doctrine. → [`STATUS.md`](./STATUS.md)

---

## 2026-05-28 (cont.) — build: M7·P2 flagging UI (+ user UI revision)

- Did: **built M7·P2** on `m7-dataset` (`4a10fb8`) — the demo-visible flag loop on top of P1. `FlaggingSource` (narrow `@MainActor` protocol: `currentPTS` / `currentDetections()` / fire-and-forget `seek` — decouples the dataset layer from the coordinator, which conforms in an extension). `FlaggingModel` (`@MainActor @Observable` over `FlagStore` + `FlaggingSource`): `setAsset(url:)` computes the fingerprint on load, `toggleCurrent` / `isCurrentFlagged` (½-frame tolerance vs the live `currentTime`) / `currentFlags` / `jump(to:)`; `modelID` from the visible detections' `sourceModelID`, `confidenceThreshold` nil. `FlagButton`, `FlagMarkerStrip`, `FlaggedFramesList` — thin built-in views, each with `#Preview`s. Demos wire one `FlaggingModel` each (`FlagStore` baseDir = Documents), `setAsset` on every swap, `clearAsset` on teardown.
- Did: **revised the UI per user feedback** (caught via sim screenshot). Two changes: (1) the **primary flag affordance moved onto the frame** — a `bookmark` puck on a 38pt `.regularMaterial` circle, placed at the **video image's top-right corner** via a new `VideoRectAligned` (Overlay) that reuses the `VideoGeometry.displayRect` authority, so it rides the real letterboxed frame, not the container chrome (first attempt mis-placed it in the dead space above the image / crowding the metrics HUD — fixed). The control-row/bottom-bar `FlagButton` was removed (one canonical place). (2) **timeline markers aligned** to the slider thumb (inset by per-platform thumb radius: iOS 14 / macOS 9) and **demoted to a coarse secondary overview** — not the source of truth (a thin strip can't resolve adjacent frames). Added an alignment `#Preview` (strip over a real `Slider`). Recorded the design principle in user memory ([[flag-affordance-on-artifact]]).
- Verified: `swift test` **230 pass** (+5 `FlaggingModel` tests: toggle add/remove, tolerance, jump-seek, setAsset). Both demo schemes `xcodebuild`-green (iPhone 17 sim + macOS), Swift 6 strict-concurrency clean. Reviewed the seam (`FlaggingModel`/`FlaggingSource`/coordinator ext) + demo diffs before committing; confirmed on-frame bookmark placement via fresh sim screenshots.
- 🗓 Opens (tracked): deferred **confidence-threshold accessor** (needs tuning-side API to populate flag provenance); revisit the `isCurrentFlagged` ½-frame tolerance once P3's PTS snap-to-sample lands.
- 👉 Next: **M7·P3** — `DatasetSink` + `FolderDatasetSink` + headless `DatasetBuilder` (seek→decode→write image + COCO sidecar), deterministic-naming dedup (resumable), resolve the `FrameRef` PTS snap tripwire. → [`STATUS.md`](./STATUS.md)

---

## 2026-05-28 (cont.) — build: M7·P3 extraction + sidecar/COCO reframe

- Did: **built M7·P3** on `m7-dataset` — the deferred-extraction half. New `Sources/Iris/Dataset/`: `DatasetSink` (protocol: `contains(_:)` + `write(image:for:)`, `Sendable`), `FolderDatasetSink` (v1; writes `<baseDir>/iris-dataset/frames/…png`, injected `baseDir` per `FlagStore` discipline), `DatasetBuilder` (headless batch: one `PlaybackSource` default `TaskTickDriver`, filters `contains` before AVF, per flag `seek(to: pts)` `.zero`-tolerance → one-shot `Frame` → PNG → write), `PixelBufferPNGEncoder` (CoreImage-only `CIImage(cvPixelBuffer:)` + `CIContext.pngRepresentation`, iOS+macOS).
- Resolved (the `// M7·P3:` tripwire): **no PTS snap pass** — `PlaybackSource.seek` already seeks `.zero`-tolerance to the canonical sample and stamps the emitted `Frame.timestamp` with the resolved PTS, so re-seek lands the same pixels; file named from the stored `ref` keeps dedup stable. Rationale lives in a `DatasetBuilder` doc comment.
- 💡 Learned (searched all of `Sources/Iris/` for `CIImage`/`CGImage`/`pngData`/etc.): **no Overlay-side pixel converter exists** — Overlay draws *over* the player layer, never lifts frame pixels. So the M7.md "reuse an existing converter" open resolved as *false*; `PixelBufferPNGEncoder` is genuinely new surface (co-located with its only consumer, no UIKit/AppKit).
- Decided with the user (**supersedes the inherited BRIEF-§6 COCO call**): a flag means "this frame deserves a second look," NOT a label — and since multiple models get tried, recording one model's verdict as annotation is false precision. So **no per-image sidecar, no COCO, no exporter in M7**; annotation happens later in an external tool in whatever format a real training pipeline names. **P4 collapses** to a `DatasetExporter` protocol seam only. The "already-exported?" answer must live **with the dataset** (its filenames), since the dataset can be lost independently of the app's `FlagStore` — a sidecar/manifest could go stale relative to it, filenames cannot. Two questions, two homes: "what did I flag?" → `FlagStore`; "what's in the dataset?" → the dataset's filenames. Recorded in user memory ([[dataset-filenames-are-the-ledger]]).
- Did: **reworked `AssetFingerprint` name-independent** — `id` = first 16 hex of `SHA-256("<byteSize>:<durationMillis>:<headHashHex>")`; head-hash now **mandatory** (the edit-sensitivity lever); `filename` kept display-only, dropped from identity. → rename-stable + move-stable + edit-sensitive (corrects the prior M7.md "survives renames" claim — the old filename-inclusive form did not). One `shortHex(of:hexChars:)` helper backs both `id` and the sink's `sourceNameHash` (single source of truth).
- Did: **export filename + dedup** — `<sourceNameHash>_<fingerprintID>_<ptsMillis>.png` (`sourceNameHash` = 8 hex of the source filename, cosmetic grouping prefix, collision-OK). `contains(_:)` scans `frames/` once and matches the `_<fingerprintID>_<ptsMillis>.png` **suffix** (prefix shifts on rename, suffix is rename-stable → no double-export). Sidecar removed entirely (`DatasetSidecar` deleted, `write` loses its `sidecar:` param). Dataset folder is now just `frames/*.png`.
- Verified: `swift test` **237 pass / 0 fail** (+ rename/move-stable/edit-sensitive fingerprint tests; filename-scheme + rename-dedup + no-`.json` extraction tests). Swift 6 strict-concurrency clean, builds first try. Plans reconciled (DECISIONS superseding entry, M7.md P3/P4 rewrite, BRIEF §6 de-COCO'd, STATUS). `demo-sim-runnable` smoke marked ✅ (user-cleared).
- 👉 Next: **M7·P4** — define a `DatasetExporter` protocol seam only; build the first concrete exporter when a training pipeline names its format. → [`STATUS.md`](./STATUS.md)

---

## 2026-05-28 (cont.) — build: M7·P4 frame-export sweep + build-system doctrine reversal

- Did: **built M7·P4 — the `FrameExporter` frame-export sweep**, in two stages (library core, then app wiring). Library (`Sources/Iris/Dataset/`): `FrameExporter` (`@MainActor final class`; `sweep(sources:into:statusURL:ranAt:)` — fingerprints each candidate URL, finds that asset's flags not yet in the sink, extracts via `DatasetBuilder`; resumable via suffix-dedup, interruptible via `Task.checkCancellation()` between sources AND frames), `FlagStore.allFlaggedAssets()` (scans `flags/*.json`), per-frame cancellation added to `DatasetBuilder.extract`. Returns a `Summary` (`written`/`skipped`/`noFrame` + per-asset breakdown + `unreachable: [(fingerprintID, displayFilename, pendingCount)]`) and persists `<Documents>/iris-dataset/export-status.json` (operational telemetry, **not** a per-frame sidecar — documented as such). +7 tests → 244 green.
- Did: **app wiring** — `Apps/Shared/FrameExportCoordinator.swift` (`@MainActor @Observable`; resolves `RecentVideos.resolve()` → URLs, **holds security scope across the whole sweep** (start-access each, `defer` stop — required for the headless `PlaybackSource` reads on sandbox/device), single cancellable `sweepTask` with an overlap guard). Both demos wire one coordinator over the **same `FlagStore`** the `FlaggingModel` writes; **"Export now"** button + last-run status line (`Summary.demoStatusLine`) in the flagged-frames panel.
- Decided (user): **drop the on-launch sweep trigger** — it contends with the user immediately opening/playing a video (the sweep opens headless `PlaybackSource`s; this is why a launch sweep was never seen running cleanly). Triggers are now **`scenePhase`→background (cancelled on foreground) + manual "Export now"** only. A *delayed*-after-launch sweep was floated but **parked** (not convinced). The manual-button path is **user-verified hands-on** (hit Export, frames appeared).
- 💡 Reversed a wrong doctrine (user — matches [[empirical-verification-over-docs]]): the M6·P3 belief that the hand-edited `Apps/IrisDemo.xcodeproj/project.pbxproj` was "authoritative" and that `xcodegen generate` would un-bundle the models was **never verified and is wrong**. `DemoCatalog`'s primary model lookup is the *compiled* `.mlmodelc`, which Xcode produces from the **Compile Sources** phase — exactly where xcodegen's default puts a `.mlpackage`. Now: **`project.yml` is canonical, regenerate freely, never hand-edit the `.pbxproj`**; the two models are declared **explicitly** in `project.yml` (excluded from the `Shared` glob, default phase). Verified: regen + build lands real `yolo12n.mlmodelc` + `yolo26n.mlmodelc` (with `coremldata.bin`/`weights`, no raw `.mlpackage`) in both `.app` bundles, both schemes green. Caught after a wiring sub-agent ran xcodegen and (correctly, as it turns out) moved the models to Compile-Sources; the panic was unwarranted. DECISIONS/QUESTIONS/STATUS + user memory all flipped.
- 🗓 Parked (this block): delayed-after-launch sweep; load `export-status.json` into the footer so automatic (background) runs are visible in-UI (currently `lastSummary` is in-memory, blank until a sweep finishes in-session); unbounded "flagged sources" ledger (RecentVideos MRU-cap-10 follow-up); the real `DatasetExporter` (training-FORMAT conversion).
- 🌱 Backlog (user): **detector MRU / remember-last-selected.** The rectangle detector is rarely the one wanted now (mostly object-model detectors, switching among them); bake-in order is wrong. Want at least "remember last selected detector," ideally an MRU of detector order. Landed in [`QUESTIONS.md`](./QUESTIONS.md).
- Verified: `swift test` **244 green**, both demo schemes `xcodebuild`-green, models bundled (runtime-relevant check), Swift 6 clean.
- 👉 Next: **merge `m7-dataset` → `main`** — M7 complete (P1–P4 ✅). Optional pre-merge hands-on: flag → background → confirm `frames/` fills. → [`STATUS.md`](./STATUS.md)

---

## 2026-05-28 (cont.) — build: demo-app UI polish (open button · detector MRU · dataset reveal)

- Confirmed the owed M7 smoke: the macOS container at `~/Library/Containers/dev.griff.iris.demo/Data/Documents/iris-dataset/` holds `frames/*.png` + `flags/*.json` + `export-status.json` — i.e. the **`scenePhase`-background sweep actually ran and wrote frames**. The one unverified M7 path is now green; M7 is honestly closed.
- Did: **detector MRU** (closes the §Backlog item; both demos). New `Apps/Shared/RecentDetectors.swift` — `@Observable @MainActor`, UserDefaults-backed ordered `[String]` of detector ids (key `iris.recent.detectors.v1`, limit 10), modeled on `RecentVideos` minus bookmarks/security-scope. API: `addOrPromote(id:)` (dedup → move-to-front → trim → persist), `firstAvailable(in:)`, `sortedEntries(_:)` (MRU-first, then remaining catalog entries in natural order so newly-available detectors still appear). Both demo `ContentView`s: pickers iterate `sortedEntries(catalog)`, `.onChange` calls `addOrPromote`, launch selection reads `firstAvailable` instead of the hardcoded `"vision.rectangles"` default. Rectangle-default was wrong — the real workflow switches among object models.
- Did: **open-video button → top of macOS sidebar** (user: "feels like that's where it belongs"). One full-width `.borderedProminent` "Open Video…" button (`folder.badge.plus`, `.controlSize(.large)`) at the top of the sidebar `VStack`. Removed **both** prior entry points — the bottom-bar `Button("Open Video…")` and the icon-only header button (header is now just `Text + Spacer`). The no-recents empty-state CTA was left intact. macOS-only.
- Did: **in-app way to find the exported frames** (macOS) — the real frames path is a buried `~/Library/Containers/.../Documents/iris-dataset/frames/`, so a `datasetSection` is now pinned to the bottom of the sidebar (below recents, behind a `Divider`): a status line (`N frames exported` / `No frames yet`, count = `*.png` in the frames dir, refreshed `.onAppear`) + a small **"Reveal in Finder"** button (`import AppKit`; `NSWorkspace.shared.activateFileViewerSelecting`, creates the dir if absent). Frames path resolved via the same Documents→`iris-dataset/frames` route `FrameExportCoordinator`/`FolderDatasetSink` use (no second path invented). iOS skipped — already exposes Documents via Files.app (M7·P3).
- Verified: both demo schemes `xcodebuild`-green (macOS + iOS Simulator), Swift 6 strict-concurrency clean. `Apps/Shared/` is glob-included so the new store needed **no `project.yml` edit** — `xcodegen generate` picked it up; confirmed referenced by both targets. Iris library (`Sources/Iris/`) untouched → `swift test` unaffected (244 green). Not hands-on-eyeballed in the running app yet (user chose commit-first).
- 👉 Next: **clean boundary** — every milestone ✅. Pick the next thrust: define M8, or pull from §Backlog (offline file-reader pre-pass · SwiftLint threshold ratchet-back · M7 export follow-ups). → [`BOARD.md`](./BOARD.md)

---

## 2026-05-29 — build: demo UI round 2 (scrubber/flag/recents polish · alignment fix) + V2 app icon

- Did: **four demo UI tweaks** (user, via sim screenshots). (1) macOS **"Export now" moved into the sidebar Dataset row** (right of the title), removed from the right inspector panel; last-sweep status line followed it under the frame count. (2) **Timeline flag markers now draw BEHIND the scrubber** as a full-height underlay (no separate row); ticks peek above/below the thumb. (3) **Flagged-frames rows** dropped the `reason`/"wrong" badge (vestige of the pre-reframe verdict model — a flag = "look again," not a label; see [[dataset-filenames-are-the-ledger]]) and gained a subtle leading `bookmark.fill` accent; `FlagReason` data model untouched. (4) macOS **recents path head-truncated** (tail shows, not `/Users/griff/…`) + `.help()` full-path tooltip + right-click menu (Copy Path · Reveal in Finder).
- Fixed (root cause, right level): the markers-behind-scrubber first cut **misaligned** — the strip was a `.background` of the whole `Scrubber` (`VStack { Slider; controls }` + 12/8 padding) nudged with hardcoded paddings, **re-deriving** the slider's geometry. Gave `Scrubber` a **track-underlay slot** instead — `Scrubber<Model, Underlay: View>` with a `@ViewBuilder` placed as `.background` of the Slider *track itself*, plus an `extension … where Underlay == EmptyView` convenience `init(model:)` so every existing call site (tests, plain players) compiles unchanged. The strip now inherits the Slider's exact width; `thumbInset` is the only variable (macOS bumped 9→10 for the ~20pt knob). The alignment `#Preview` was rebuilt to use the real `Scrubber`-with-underlay composition so it can't drift from production again. User confirmed alignment by eye.
- Did: **V2 app icon** for both demos (user picked V2 "aperture-iris" — mechanical iris layers, teal pop — from a 3-variant contact-sheet mock). Icon is drawn in **SwiftUI + `ImageRenderer`** (no SVG rasterizer on the machine; zero install) by a self-contained generator under `tools/icongen/` (own `Package.swift`, no dep on Iris). Emits a combined `AppIcon.appiconset` carrying both idioms — `mac` full ramp (16–1024, 1x/2x) + a `universal`+`platform: ios` 1024 (the `platform: ios` tag is load-bearing; without it iOS `actool` errors "no applicable content"). Shipped icon lives in **`Apps/Shared/Assets.xcassets/`** (globbed into both targets); `ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon` added to both targets in `project.yml`; regenerated via xcodegen (pbxproj generated, never hand-edited). Generator preview renders (contact sheet + v1/v2/v3 ramps) are **gitignored** (regenerable); the generator source + the `.xcassets` are tracked.
- Verified: `swift test` **244 green** (Scrubber generic + EmptyView convenience init keep the contract); both demo schemes `xcodebuild`-green, Swift 6 strict-concurrency clean. Icon confirmed in both bundles — macOS `AppIcon.icns` + `Assets.car` (`CFBundleIconName=AppIcon`); iOS `Assets.car` + emplaced icon PNGs (`CFBundleIcons → CFBundleIconName=AppIcon`).
- 🌱 Backlog (user): **per-category tuning** — per-class confidence + per-class hide/show, independent of the global knob (turn `person` off while tuning `sports ball`). Bigger effort: extends `IrisTuning` settings to a per-label map + derived-tuning UI rows + overlay/filter honoring it. → [`BOARD.md`](./BOARD.md) §Backlog.
- 👉 Next: **clean boundary** still — every milestone ✅, demos polished + branded. Pick the next thrust (define M8, or pull §Backlog). → [`BOARD.md`](./BOARD.md)

---

## 2026-05-29 (cont.) — define M8 — Image (static-image detection)

- Explored the **four subsystems** the milestone touches, to ground the design rather than guess: **(1) `Frame` + sources** ([`Frame.swift`](../Sources/Iris/Frame.swift), `SourceKind`) — `Frame` is pixelBuffer+timestamp+orientation+source+format+dimensions, with nothing presuming a stream or asset; **(2) detection pipeline + coordinator** — `Detector.detect(in:)` and [`DetectorPipeline`](../Sources/Iris/Detection/DetectorPipeline.swift)`.detect(in:cache:tuning:)` take one `Frame` (already one-shot-shaped), while [`PlaybackDetectionCoordinator`](../Sources/Iris/Playback/PlaybackDetectionCoordinator.swift) owns the loop/cache/metrics/session-swap *plus* the playback-coupled `PlaybackController`+`seek` hook; **(3) overlay + geometry** — `VideoGeometry`/`DetectionLayer`/`VideoRectAligned`/`ResultStore` are image-agnostic (drive them with `contentSize = imageSize` + a frozen `displayTimeSource`); **(4) demo structure** — `DetectorCatalog`/`DemoModelStore`/`ActiveDetectorSession`/tuning sheet/`RecentDetectors`/`DocumentPicker`/`RecentVideos` are all reusable.
- 💡 Finding (stated up front in the feature doc): **the pipeline + overlay are already source-agnostic** — the **only** video-coupled cluster is `PlaybackDetectionCoordinator` + `PlaybackController` + `PlaybackSource` + `Scrubber` + the `FlaggingSource` protocol (PTS/seek). So M8 isn't fighting the architecture; it sidesteps that cluster and **triggers the source-agnostic `DetectionRunner` decomposition the board had deferred** — the image inspector is the long-awaited second consumer that justifies the split.
- Did: **defined M8 — Image** in [`features/M8.md`](./features/M8.md) — six phases: **P1** extract `DetectionRunner` (loop+cache+metrics+session-swap into `Detection/`, recompose playback; pure refactor, keep 244 green) · **P2** image→upright `Frame` (`Sources/Iris/Image/`, `SourceKind.image`, EXIF/orientation upstream so `VideoGeometry` stays rotation-free) · **P3** `ImageDetectionCoordinator` (`@MainActor @Observable`, one-shot detect, model-swap re-runs once, composes the runner) · **P4** demo Image page iOS+macOS (image picker for png/jpeg/heic mirroring `DocumentPicker`, `RecentImages` MRU mirroring `RecentVideos`, detector picker + tuning sheet, no scrubber) · **P5** freeze-from-live ("Inspect frame" from playback/capture → Image page) · **P6** dataset tie-in (image-shaped `AssetFingerprint` minus `durationSeconds` + PTS/seek-free `FlaggingSource` + flag→PNG export per [[dataset-filenames-are-the-ledger]]).
- Settled **five design forks** with the user: **(1)** image detection is **one-shot, not stream-driven** — reuse `DetectorPipeline` with a frozen timestamp; **no** 1-frame `AsyncStream` through the playback loop (forking the API shape to fit both is the CLAUDE.md anti-pattern). **(2)** P1 is the **full `DetectionRunner` extraction**, composed by both coordinators — **resolves the standing source-agnostic-decomposition open question**. **(3)** new **`Sources/Iris/Image/`** folder; feature named **Image**, not "Stills". **(4)** **freeze-from-live in scope** (P5). **(5)** **dataset tie-in in scope** (P6) with an image-shaped `AssetFingerprint` (drop `durationSeconds`; keep `byteSize`+head-hash) + a PTS/seek-free `FlaggingSource` path.
- Did: recorded the milestone across the board — **[`features/M8.md`](./features/M8.md)** created; **[`BOARD.md`](./BOARD.md)** §Status rewritten (M1–M7 + sub-tracks collapsed to an all-✅ done-line, M8 the active focus tree with P1 ← here), §Milestones gained the M8 legend line, snapshot date → 2026-05-29; **[`DECISIONS.md`](./DECISIONS.md)** gained seven dated 2026-05-29 entries (M8 defined + the 5 forks, the `DetectionRunner` one carrying the resolved `Q:`); **[`QUESTIONS.md`](./QUESTIONS.md)** — the "source-agnostic decomposition" entry **removed** (no longer open; it's scheduled work as M8·P1).
- 👉 Next: **build M8·P1 — extract `DetectionRunner`** out of `PlaybackDetectionCoordinator` into `Detection/`, recompose playback to compose it. Pure refactor; full suite (244) stays green; closes the source-agnostic-decomposition question. → [`BOARD.md`](./BOARD.md)

---

## 2026-05-29 (cont.) — build: M8·P1 — extract `DetectionRunner`

- Did: **lifted the source-agnostic detection core out of [`PlaybackDetectionCoordinator`](../Sources/Iris/Playback/PlaybackDetectionCoordinator.swift) into a new [`DetectionRunner`](../Sources/Iris/Detection/DetectionRunner.swift)** (`@MainActor @Observable`, `Detection/`). The runner owns the `ResultStore` + `DetectionMetrics`, builds + hot-swaps the `ActiveDetectorSession` from a catalog entry (`installSession(entry:onTierChange:)` / `clearSession()`), resets per-session (`resetForNewSession()`), and exposes the **per-frame unit** `nonisolated func run(on:)` — read live router → `DetectorPipeline.detect(in:cache:tuning:)` → time it → record the gauge. `run` stays `nonisolated` so heavy inference runs off the main actor (matching the old detached loop), hopping to main only for the router read + metrics record.
- Did: **recomposed the coordinator** to wrap a `runner`. It keeps only the playback layer — the per-source frame-stream detect loop, the `PlaybackController` lifecycle + ordered teardown, the **source-specific drop/emit counter bridge** (`PlaybackSource.droppedFrameCount`/`emittedFrameCount` → `metrics.set*`), and the **seek-based `onDetectorTierChange`** re-emit hook (injected into `runner.installSession`, the consumer-specific re-emit strategy the runner abstracts). `resultStore`/`metrics`/`session` are **re-surfaced as forwarding computed props** so the demos' library-view bindings and the regression tests are unchanged.
- Seam: the genuinely shared core (cache+metrics+session+`run`) is now reusable by M8·P3's one-shot `ImageDetectionCoordinator`; only the *driving* (stream loop vs. one call) and the *re-emit* (seek vs. re-detect held frame) differ — exactly the split the deferred decomposition called for. **Closes the source-agnostic-decomposition open question.**
- Behavior note: drop/emit counters now mirror every loop iteration (not only on inference success) — a benign, more-current change to a best-effort gauge; not asserted by any test.
- Verified: **pure refactor, zero behavior change.** `swift build` clean (Swift 6 strict concurrency, first try); `swift test` **244 green** incl. the headline `swappingDetectorMidStreamRoutesNewDetectorOutput` + `teardownReturnsOnlyAfterSourceInvalidated` scope-ordering test; both demo schemes `xcodebuild`-green (macOS + iOS Simulator) against the forwarded API.
- On branch `m8-p1-detection-runner` (off `main`); two commits — `docs(plans): define M8` + the P1 refactor.
- 👉 Next: **M8·P2 — image → upright `Frame`** (`SourceKind.image` + `CGImage`/`CVPixelBuffer`→`Frame` helper baking EXIF orientation upstream; fixture-image tests; new `Sources/Iris/Image/`). → [`BOARD.md`](./BOARD.md)

---

## 2026-05-29 (cont.) — build: M8·P2 — image → upright `Frame`

- Did: **new [`ImageFrameDecoder`](../Sources/Iris/Image/ImageFrameDecoder.swift)** (`Sources/Iris/Image/`, the new folder) — turns a still into the same source-agnostic `Frame` capture/playback produce, so the existing detector + overlay pipeline runs on it unchanged. Two entry points: `frame(from:orientation:identifier:timestamp:)` (a `CGImage` + EXIF orientation) and `frame(fromImageAt:…)` (a file URL via ImageIO, reading orientation from metadata). Built on CoreImage + ImageIO + CoreVideo only — **no UIKit/AppKit** — so it's iOS+macOS identical; mirrors `PixelBufferPNGEncoder`'s shared-`CIContext` shape.
- Key call: **orientation is baked into the pixels** (`CIImage(cgImage:).oriented(_:)` → render into a fresh BGRA IOSurface `CVPixelBuffer`), so the returned `Frame` is stamped `.up`. This honors `VideoGeometry`'s locked invariant (rotation/mirroring resolved upstream, never in the overlay) exactly as Capture (rotates on the connection) and Playback (delivers upright) do — a consumer feeds `frame.dimensions` straight to `VideoGeometry(contentSize:)`. Frozen `timestamp` (`.zero` default): a still has no time axis.
- Did: added **`SourceKind.image(String)`** (no exhaustive switch on `SourceKind` anywhere in the repo — additive, non-breaking).
- Tests: **+6** in [`Tests/IrisTests/Image/`](../Tests/IrisTests/Image/ImageFrameDecoderTests.swift), fixtures **synthesized in-test** (no committed binary): upright-`Frame` contract (dims/source/format/orientation/timestamp); **90° EXIF bakes to transposed upright dims** (the load-bearing correctness property); from-disk round-trip via a temp PNG carrying EXIF orientation; missing-file → typed `DecodeError`; **a decoded frame is detectable by the real `VisionRectanglesDetector`** (proves the BGRA/IOSurface buffer flows through the actual Vision path); pipeline plumbing via `MockDetector`.
- Verified: `swift build` clean (Swift 6 strict concurrency, first try); `swift test` **250 green** (244 + 6); SwiftLint clean on the new files.
- 👉 Next: **M8·P3 — `ImageDetectionCoordinator`** (`@MainActor @Observable`, one-shot detect over the held `Frame`, model-swap re-runs once via the runner's `onTierChange` re-detecting the held frame; composes the P1 `DetectionRunner`). → [`BOARD.md`](./BOARD.md)

---

## 2026-05-29 (cont.) — build: M8·P3 — `ImageDetectionCoordinator`

- Did: **new [`ImageDetectionCoordinator`](../Sources/Iris/Image/ImageDetectionCoordinator.swift)** (`@MainActor @Observable`, `Sources/Iris/Image/`) — the one-shot image analogue of [`PlaybackDetectionCoordinator`](../Sources/Iris/Playback/PlaybackDetectionCoordinator.swift). **Composes the P1 [`DetectionRunner`](../Sources/Iris/Detection/DetectionRunner.swift)** (held as `public let runner`) and re-surfaces `resultStore`/`metrics`/`session` as forwarding computed props so the demo's library-view bindings match the playback page's; adds a held `public private(set) var frame: Frame?` (→ overlay `contentSize` via `.dimensions`, `displayTimeSource` via the frozen `.timestamp`).
- Shape: **no detect loop, no `PlaybackController`, no seek** — the milestone's one-shot decision. `setImage(_:detector:)` resets per-image, holds the frame, installs the session, and `await runner.run(on:)` **once**. `selectDetector(_:)` invalidates the cache, installs the new session in place, and re-runs the held frame once — the one-shot re-run *is* the swap (no single-iteration stream to drain/respawn, so none of playback's race machinery). `clear()` drops the session (+ its hook) and the held frame.
- The **re-emit hook is the image counterpart of playback's seek**: a `.detector`-tier tuning knob invalidates the cache via the session's cache wiring and fires `onDetectorTierChange`; here the hook re-reads the *current* held frame (not the load-time capture) and re-runs `run(on:)` so the overlay repopulates under the new settings. A `PassthroughRouter` (non-tunable detector) never fires it — harmless no-op, same as playback.
- Tests: **+5** in [`Tests/IrisTests/Image/`](../Tests/IrisTests/Image/ImageDetectionCoordinatorTests.swift), library-level (no UI), fixtures synthesized via the real `ImageFrameDecoder` + the same A/B `MockDetector` catalog pattern the playback swap test uses: load → A cached at the held bucket; **swap → held frame re-detected by B, no stale A** (the headline, image analogue of the playback swap regression); `selectDetector` without an image is a no-op; `setImage` resets metrics + invalidates the cache; `clear` drops session + frame + cache. No polling — `run` is awaited to completion, so the cache is populated the instant each call returns.
- Verified: `swift build` clean (Swift 6 strict concurrency, first try); `swift test` **255 green** (250 + 5), playback swap + scope-ordering regressions still pass against the unchanged P1 runner. P3 is library-only — the coordinator isn't wired into a demo yet (that's P4), so demos are untouched.
- On branch `m8-p1-detection-runner`.
- 👉 Next: **M8·P4 — demo Image page (iOS + macOS)**: `TabView`/sidebar entry, image picker for png/jpeg/heic (mirror `DocumentPicker`), `RecentImages` MRU (mirror `RecentVideos`), mount the Image + `DetectionLayer` + detector picker + tuning sheet, no scrubber. → [`BOARD.md`](./BOARD.md)

---

## 2026-05-29 (cont.) — build: M8·P4 — demo Image page (iOS + macOS)

- Did: **the demo Image page on both platforms**, mounting P3's `ImageDetectionCoordinator`. New shared **`RecentImages`** (`Apps/Shared/`, bookmark-backed MRU; deliberate sibling of `RecentVideos`, key `iris.recent.images.v1`) + shared **`ImageDetailView`** (`Apps/Shared/`, the reusable detail: held image + `DetectionLayer` overlay + detector picker + Tune sheet; renders the held `CVPixelBuffer`→`CGImage` via a shared `CIContext` + cross-platform `Image(decorative:)`, no UIKit/AppKit gating). **iOS** `ImageContentView` — a tab between Playback/Capture; `DocumentPicker([.image])`; pick → decode via `ImageFrameDecoder` → `setImage`; detector swap re-runs the held frame; `RecentImages` MRU. **macOS** — an **additive Images mode** in `ContentView`: a segmented `Videos | Images` picker atop the sidebar; `.videos` renders the existing playback sidebar+detail **byte-for-byte unchanged**; `.images` shows Open Image… + recent-images + the shared `ImageDetailView`.
- Fixed (root cause — user-reported, "Open Image does nothing" on both platforms): **stacked same-type presentation modifiers** (SwiftUI honors only one `isPresented` sheet/importer per view; the rest silently never present). **macOS** — the image `.fileImporter` was the 3rd stacked on the root (behind movie + model); moved it onto the **images sidebar** view (its own node, beside its trigger button). **iOS** — the two `.sheet(isPresented:)` (image + model picker) collided; consolidated to one **`.sheet(item:)`** routed by an enum. Also iOS pick icon `photo.badge.plus`→**`photo.on.rectangle`** ("Open image" — reads as open, not add). The "no model selector" symptom was downstream: `ImageDetailView` gates the detector picker behind a loaded frame (mirrors playback), so a broken picker meant no frame → no picker; fixed with the picker.
- Scoping (deliberate): the durable value is the shared **`ImageDetailView` + `RecentImages` + coordinator wiring** — it slots into any sidebar. The **iOS tab + macOS segmented toggle are SEEDS** the user's upcoming **unified-sidebar nav pass** will subsume (their screenshots: page-rows sidebar [Playback/Image/Capture] + a **global MODEL section** [model picker + confidence, shared across pages] + a bottom-sheet inspector on compact, cross-platform). Per the user, did **NOT** move the model selector to the sidebar or fold Capture in — that pass is theirs.
- Verified: both demo schemes `xcodebuild`-green (macOS + iOS Sim, after the picker fix); `swift test` **255 green** (the library is untouched). `project.pbxproj` regenerated via `xcodegen generate` (new `Apps/Shared/` files auto-globbed; pbxproj tracked).
- 🗓 Backlog (recorded): macOS **movie + model `.fileImporter` still stacked** on the root (same latent bug — Open Video works, the custom-model importer is suspect) · the **unified-sidebar nav pass** (the coming pass; captures the screenshot direction + the shared-active-detector implication) · a **shared MRU generic** (`RecentImages`/`RecentVideos`) · `RecentImages` untested (`Apps/Shared/` reachability deferral, as with `RecentVideos`) · macOS custom-model-in-image-mode re-selects the *playback* detector id (minor).
- 👉 Next: **M8·P5 — freeze-from-live** — an "Inspect frame" affordance on the playback overlay (and the capture overlay on iOS) that freezes the visible `Frame` and opens it in the Image page for model-play. → [`BOARD.md`](./BOARD.md)

---

## 2026-05-29 (cont.) — plan: promote "Unified sidebar nav" to a penciled milestone (docs only)

- Did: **pulled the unified-sidebar item forward in planning** — from a `BOARD.md` §Backlog stub to a **defined-but-penciled milestone**, sequenced *after* the active M8 work (after M8·P5 freeze-from-live + M8·P6 dataset tie-in). Captured the direction from the user's **iPhone + iPad reference mockups** (2026-05-29; source PNG to land at [`features/assets/unified-sidebar-mock.png`](./features/assets/unified-sidebar-mock.png)).
- Settled **two decisions** with the user: **(1) sequencing** — built *after* M8·P5/P6, not before; accepted cost is that P5/P6 wire into the interim P4 nav which this pass then rewrites, but both reuse coordinator/MRU plumbing that survives the reshuffle (throwaway confined to the nav shell). **(2) global model spans all three pages incl. Capture** — the sidebar's top `MODEL` section (detector picker + min-confidence) is app-level shared state replacing today's **four** per-page `selectedDetectorID`s; because Capture is currently fixed to Vision rectangles with no picker, **live-capture detector-swap is in scope as net-new work**.
- Did: **established the milestone-naming convention** — a milestone's identity is a *descriptive slug + one-line description*; **numbers are assigned at pickup only** (active/done), never reserved for penciled work; future milestones are slug-only. Lives in [`WORKFLOW.md`](./WORKFLOW.md) §"Status trees" (Rules). This makes the unified-sidebar milestone the first **un-numbered** entry in the §Milestones legend.
- Did: recorded it across the board — **[`features/unified-sidebar/README.md`](./features/unified-sidebar/README.md)** created (intent, mockups + ASCII layout, structure, the per-page→global model collapse with the 4 call-site refs, subsumes/fixes, open forks, sequencing); **[`DECISIONS.md`](./DECISIONS.md)** gained 3 dated 2026-05-29 entries (naming convention + the 2 forks); **[`BOARD.md`](./BOARD.md)** §Milestones gained the un-numbered legend line, §Status "📌 recent" gained 3 pointers, and §Backlog **dropped the two now-subsumed stubs** (the unified-sidebar-pass item + the macOS stacked-`.fileImporter` bug — both folded into the feature doc), leaving the "shared MRU generic" stub with a "touched, not closed" note.
- Note: **no code changed.** §Status focus tree + the live 👉 next are **unchanged** — M8·P5 remains the live next.
- 👉 Next: **M8·P5 — freeze-from-live** (unchanged). → [`BOARD.md`](./BOARD.md)

---

## 2026-05-29 (cont.) — build: M8·P5 — freeze-from-live (playback + iOS capture)

- Did: **"Inspect frame" freeze-from-live on all three live surfaces** — iOS playback overlay, iOS capture overlay, macOS playback overlay. Freezes the visible `Frame` and opens it in the Image page, **on the same detector** the live source was using (then the user swaps models on that one still). First cross-feature handoff (playback/capture → image), reusing P3's `ImageDetectionCoordinator.setImage`.
- Library (one change, symmetric with `ImageDetectionCoordinator.frame`): `PlaybackDetectionCoordinator` gained `public private(set) var currentFrame: Frame?`, set on the @MainActor in the detect loop, cleared in `teardown()`. `DetectionRunner` stayed pure. **+1 test** (`currentFrameReflectsProcessedFrame` — nil before, reflects the processed frame after a `seek`, cleared on teardown).
- Demo hand-off: new **`Apps/Shared/InspectorHandoff.swift`** (`@MainActor @Observable`; `request: InspectRequest?` with a monotonic `token`, `Equatable` on token since `Frame` isn't). **iOS** — root `ContentView` went value-based `TabView(selection:)` (`DemoTab` enum) + `.environment(handoff)`; an inspect request switches to the Image tab; `PlaybackContentView`/`CaptureContentView` post the frozen frame via the environment conduit; `ImageContentView` consumes it via `.task(id: token)` → resolves the entry → `setImage`. `CaptureContentView` now retains `lastFrame` in its frame loop. **macOS** — direct (one view holds both coordinators): the inspect button flips `mode = .images` + `imageCoordinator.setImage`, no conduit. Affordance = **`camera.viewfinder`**, clustered with `FlagButton` in the top-trailing `VideoRectAligned` on playback; floating top-right on iOS capture.
- Verified: `swift test` **256 green** (255 + the new playback test); `IrisDemo-iOS` + `IrisDemo-macOS` `xcodebuild`-green (iOS 26.5 / iPhone 17 Pro Sim; macOS); `project.pbxproj` regenerated via `xcodegen generate` (new Shared file auto-globbed into both targets).
- Notes: the conduit + tab-switch wiring is **interim** (flagged in-code) — the unified-sidebar pass subsumes it. iOS overlay cluster un-gated from `flaggingModel` so Inspect shows before the lazy flagging build finishes. Inspect promotes the chosen detector into the `RecentDetectors` MRU (acts like an explicit selection). Build-env note: the schemes accept the **iOS 26.5 / iPhone 17** sim family (installed iPhone 16 sims are an older runtime).
- On branch `m8-p1-detection-runner`; **not committed**.
- 👉 Next: **M8·P6 — dataset tie-in** — image-shaped `AssetFingerprint` (drop `durationSeconds`; keep `byteSize` + head-hash) + a PTS/seek-free `FlaggingSource` path for a single still + flag→provenance-named-PNG export (filenames are the ledger). → [`BOARD.md`](./BOARD.md)

---

## 2026-05-29 (cont.) — plan: shelve M8·P5/P6, close M8 at core, pull the unified shell forward as M9 (docs only)

- Did: ran a **UI-reliability audit** of the three demo modes (Group A bugs A1–A7 + Group B rework). The user reprioritized off the back of it.
- Did: **shelved M8·P5/P6.** P5 (freeze-from-live) is a **thin convenience** — built, but **parked on branch `m8-image`, not merged**. P6 (dataset tie-in) is **premature** — the user isn't training yet. Both moved to [`BOARD.md`](./BOARD.md) §Backlog (each links back to [`features/M8.md`](./features/M8.md)).
- Did: **closed M8 at its core (P1–P4).** Its goal — run detectors on a still + swap/compare models — shipped in P1–P4 ([`features/M8.md`](./features/M8.md) gained a `## Closeout` section; header status line updated).
- Did: **pulled the unified-shell work forward as the active milestone — M9** (slug `unified-sidebar`, title *"Unified shell — one shared model + a left pane that drives the modes"*), superseding the earlier "sidebar sequenced after M8·P5/P6" call. Defined **M9 = P1 reliability quick wins → P2 shared model store → P3 left-pane shell → P4 Capture joins → P5 simplify** (the generic MRU + any coordinator merge stay backlog). The two intent leads: (1) **one shared model + min-confidence across all three modes** (today four independent per-page selections); (2) **the left pane becomes the driver** of model selection + navigation + per-page Open…/RECENT. The mock's bottom DATASET/Export is **reserved-but-deferred** (belongs to the shelved P6).
- Did: recorded it across the board — [`features/unified-sidebar/README.md`](./features/unified-sidebar/README.md) rewritten (M9 title, active status, intent lead, **## Phases** P1–P5 with file refs, **## Leave alone**, updated sequencing note); [`DECISIONS.md`](./DECISIONS.md) gained the dated M9-pulled-forward decision + a **Superseded 2026-05-29** annotation on the prior "after M8·P5/P6" entry; [`BOARD.md`](./BOARD.md) §Status done-line + focus tree + 👉 next + "📌 recent" rewritten, §Milestones legend renumbered (M9 + M8 closed-at-core note), §Backlog gained the two M8·P5/P6 stubs.
- Note: **no code changed.** Board reset + milestone defined, awaiting go to build P1.
- 👉 Next: **M9·P1 — reliability quick wins** (macOS importer collision A1, gate Image picker till loaded A6, bookmark resolve logging A5; independent, mergeable alone). → [`BOARD.md`](./BOARD.md)

---

## 2026-05-29 (cont.) — process: honest status board + merge-state doctrine; land M8 on `main`

- Did: **overhauled the status board for honesty + legibility** after the user flagged two process errors. (1) §Status had collapsed every completed milestone into a single `done (all ✅) — M1 · M2 · …` one-liner — **restored the overview tree** (each milestone its own ✅ line) + added an anti-collapse guard (BOARD header comment + WORKFLOW **Rule 6**). (2) Cryptic phase-number codes like `(P1–P3 ✅ · P4 🚫)` are undecodable later — replaced with **named phases** folded into each milestone's delivery summary, every non-done phase named with its reason (new WORKFLOW **Rule 8**). Pulled the real M1–M8 phase content from features docs / LOG (via an Explore subagent) to write the summaries.
- Did: **settled the merge-state doctrine** (the deeper bug — M8 was ✅ on the board but never merged to `main`). ✅ now means **merged to its integration target** — a *phase* into its milestone branch, a *milestone* into `main`; built-but-unmerged work carries **🔀 (merge-pending)**. *Where the work lives* (branch) names the target reached; the marker names merge-readiness. Picked **🔀** for the marker (over ⏳/📥). Recorded in [`DECISIONS.md`](./DECISIONS.md) (2026-05-29); lifecycle + legend updated in [`WORKFLOW.md`](./WORKFLOW.md) §"Status trees". 🚩 stays reserved for "issue."
- Discovery that triggered all this: **M8 was never on `main`.** M1–M7 + demo-sim were genuinely merged; M8 (P1–P5) + the M9 planning docs lived only on `m8-image`, yet the board showed M8 ✅. The ✅ had silently drifted from "merged" to "done on a branch."
- Did: **landed M8 on `main`.** Committed the board/doctrine docs as their own commit on `m8-image` (`fd234d7`, M8 still 🔀), then **fast-forwarded `main` → `m8-image`** (FF-clean; matches this repo's merge convention). M8 P1–P5 (incl. **freeze-from-live — un-parked; it ships with M8**) + the M9 planning docs are now on `main`. Flipped M8 🔀→✅ (collapsed to a peer delivery-summary line); dropped the §Backlog "M8·P5 parked" stub (P6 dataset tie-in stays backlogged).
- Note: **no app code changed** — docs + a git landing only. `m8-image` left in place (fully merged into `main`; deletable anytime). LOG.md is now >730 lines — 🗓 pruning (roll to `LOG-archive.md`) is overdue (backlog).
- 👉 Next: **Start M9** — cut `m9-unified-shell` off `main`, then **P1 — reliability quick wins** (A1 macOS importer collision, A6 gate Image picker till loaded, A5 bookmark resolve logging; independent, mergeable alone). → [`BOARD.md`](./BOARD.md)

## 2026-05-29 (cont.) — build: M9·P1 — reliability quick wins (three independent fixes)

- Did: **cut `m9-unified-shell` off `main`** (at `4ca2010`) and shipped **P1 — three independent, individually-mergeable demo-wiring fixes**. Library (`Sources/Iris/`) untouched; `ImageDetailView` rendering / coordinator internals / playback path left alone.
- **A1 — macOS movie + model `.fileImporter` collision** (`ee6fd0f`): two `.fileImporter(isPresented:)` were stacked on the macOS root view (~`ContentView.swift:415`+`:434`); SwiftUI honors only **one** `isPresented` importer per view, so the model picker silently never presented. Fix: a nested `ActiveImporter` enum (`.movie`/`.model`, each carrying its `contentTypes`) + one routed `.fileImporter` switching on the active case — the same enum-routed pattern iOS `ImageContentView` already uses. All five trigger sites rewired (sidebar Open, empty-state Open, Load-model toolbar, two `.onChange` auto-opens). The Images-mode image importer lives on a separate view (no collision) — left as-is.
- **A6 — gate Image-mode detector picker + Tune until a frame loads** (`66e2b0d`): loaded condition is `coordinator.frame != nil`. The shared control bar now always renders (so the disabled state is visible) with `.disabled(coordinator.frame == nil)` on both the detector Picker and the Tune button. Change made in shared `Apps/Shared/ImageDetailView.swift` — mounted by both the iOS Image tab and macOS Images mode, so one edit covers both. (Only the control-bar gating changed; rendering/overlay untouched.) macOS root toolbar picker/Tune belong to the playback path — correctly left alone.
- **A5 — bookmark-resolve logging** (`7639638`): `RecentImages`/`RecentVideos` `resolve()` were silent on staleness or logged at `.debug` (filtered by default). Added an `isStale` log (`.notice` on refresh, `.warning` on failed refresh) and elevated missing-file + unresolvable-bookmark logs `.debug`→`.warning`. Uses the existing `os.Logger` (subsystem `iris.demo`, categories `recent-images`/`recents`).
- Verified: both schemes `xcodebuild`-green (Debug) — `IrisDemo-macOS` + `IrisDemo-iOS` (generic iOS Simulator). No new warnings from the changed code (only the pre-existing unrelated AppIntents note). Library untouched ⇒ `swift test` not re-run (no library delta).
- On branch `m9-unified-shell`; **three commits, unmerged** (each individually mergeable per P1's design). Not yet on `main`.
- 👉 Next: **M9·P2 — shared model store** — an app-level `@MainActor @Observable` holding `selectedDetectorID` + `minConfidence`, **persisted**, lifted to the app root via `.environment`, replacing the **four** independent per-page selections (iOS Playback + Image, macOS Videos + Images). Fixes A2 (Image detector silently flipping on re-appear). → [`BOARD.md`](./BOARD.md)

## 2026-05-30 — build: M9·P2 — shared model store (one global model across Playback + Image)

- Did: lifted the **four** independent per-page detector selections into **one** app-level `ModelSelection` (`Apps/Shared/ModelSelection.swift` — `@MainActor @Observable`, UserDefaults-backed like `RecentDetectors`: `detectorID` default `vision.rectangles` key `iris.model.selectedDetectorID.v1`, `minConfidence` default `0.25` key `iris.model.minConfidence.v1`). Injected at each app root via `.environment`. Both schemes build green; library + `ImageDetailView` rendering + coordinator internals untouched; Capture left for P4.
- Decision taken mid-build (the README premise was wrong): **there is no per-page min-confidence to lift.** Since M5, confidence is **detector-intrinsic** (Vision rectangles has none; YOLO26n's lives in its decoder `Settings`/`TuningModel`). So the global "Min confidence" slider in the mock is something we *introduce*, not lift. User chose **one global model** for the selection; min-confidence → **held + persisted now, behavior wired in P3** (recommended option; not consumed anywhere yet, doc-commented). Uses `object(forKey:)` so a stored `0.0` isn't read as "unset."
- The four call-sites (all were `@State … = "vision.rectangles"`): **iOS Playback** + **iOS Image** now read `@Environment(ModelSelection.self)` (+ local `@Bindable` for the Picker); **macOS Videos** binds the toolbar Picker to the root `modelSelection`; **macOS Images** — `imageSelectedDetectorID` + `imageDidApplyLaunchSelection` **deleted**, images mode reads the *same* shared selection (the deliberate macOS playback≠image split is intentionally collapsed — that's the "one global model" intent). The single root `.onChange(of: detectorID)` now drives **both** coordinators (each `selectDetector` early-returns when nothing's loaded).
- A2 fix (Image detector silently flipping on re-appear): the library coordinators don't expose their installed entry id, so the demo tracks a `syncedDetectorID` (set wherever a detector is installed) and each page's `.onAppear` re-installs **only if** the shared id drifted from the last-synced id **and** a source/frame is loaded — no redundant re-detect flicker. Launch selection now comes from the persisted shared `detectorID` (shared across modes), not the per-page MRU; `RecentDetectors` still drives picker ordering; a stale id falls back to the catalog's first entry.
- Verified: `IrisDemo-macOS` + `IrisDemo-iOS` `xcodebuild`-green (Debug). One **pre-existing** iOS warning (`currentTime` from a Sendable closure in the untouched `DetectionInspector` block) — present on HEAD before this change. `xcodegen generate` run (new `Apps/Shared/` file globbed into both targets; pbxproj committed). **Build-green only — hands-on smoke of the A2 fix (switch model in Playback → open Image → confirm it adopts the global model) is owed.**
- On branch `m9-unified-shell`, commit `3af1ed8` (one logical commit). Unmerged.
- 👉 Next: **M9·P3 — left-pane shell** (the heart) — one cross-platform sidebar replacing the iOS `TabView` + macOS `Videos|Images` toggle; `MODEL` section on top (the P2 selection **+ the min-confidence slider wired to behavior**), page-rows w/ inline Open…/RECENT, reserved bottom DATASET slot; iPad/macOS persistent split + docked inspector, iPhone drawer + bottom-sheet inspector; absorbs the freeze-from-live handoff conduit. Big phase — design pass first. → [`BOARD.md`](./BOARD.md)

## 2026-05-30 (cont.) — design + build: M9·P3 — design pass, then step 1 (render-time confidence filter)

- Design pass (architect subagent) on P3, the milestone's heart. Grounded finding: **macOS is already ~90% the target shell** (a `NavigationSplitView` holding both coordinators with direct freeze-from-live); the real work is migrating **iOS** off its `TabView`. Resolved forks: shell = `NavigationSplitView` + custom `VStack` sidebar content + size-class-routed inspector; detail content (`ImageDetailView`, `playbackArea`, capture preview) survives intact, only picker/Open/RECENT chrome moves to the sidebar (+ extract a shared `PlaybackDetailView`); all coordinators persist for the shell's lifetime (the macOS model — that's what removes A4 tab-reload + A7 scroll-reset), with Capture's camera keyed to active-page selection to preserve the documented AVF safety. Phasing = 6 mergeable sub-steps.
- **The min-confidence conversation (with the user) reframed the whole thing.** We'd conflated two settings roles: **detector input** (changes what's emitted — M5's per-detector Tune) vs **render-time overlay filter** (decides what's *drawn* from what was emitted, raw inspector stays unfiltered). The MODEL Min-confidence slider is the **render-time filter** — which is why it's universal/honest across detectors (keys on `Detection.confidence` after the fact; Vision rects = 1.0 pass any sub-1.0 floor). My earlier "can only drive YOLO26n" was from mis-treating it as a detector input. The seam didn't exist, so we **create** it — in the **`Iris` library** (the overlay lives there), a small principled addition **orthogonal to M5**. North star (user, deferred): one shared per-detector settings bundle fed into the detector AND used to filter the overlay; the backlog's **Per-category tuning** is the generalization. **For now: a simple GLOBAL floor.** Recorded in [`DECISIONS.md`](./DECISIONS.md) (2026-05-30); "demo-wiring only" relaxed for this one seam.
- Built **step 1** (`4c970b7`): library `Sources/Iris/Overlay/DetectionConfidenceFilter.swift` — pure `[Detection].filtered(minConfidence:)` (order-preserving, inclusive `>=`, `<=0` = identity no-op). `DetectionLayer` gained `minConfidence: Float = 0` (behavior-neutral default) and filters the drawn list after the tuning transform; `DetectionInspector` left untouched (raw view). Test `Tests/IrisTests/Overlay/DetectionConfidenceFilterTests.swift` — 6 cases (floor 0 keeps all, boundary `==` survives, Vision-1.0 survives sub-1.0 floors, real-score mix, order, floor 1.0 drops sub-1.0). `swift test` **262 green** (was 256, +6). Demo: `ModelSelection.minConfidence` now consumed — threaded into every `DetectionLayer` (playback/image/capture, both platforms); a "Min confidence" slider (0…1, step 0.05) added to the macOS toolbar + iOS playback control surface (TODO-tagged to relocate to the sidebar MODEL section in a later step). Overlays read the observed value in `body`, so the slider re-filters live (paused playback + held still) with no re-detection. **Needed:** `.environment(modelSelection)` injected at the macOS root (P2 held it only as `@State`; `ImageDetailView` reads it via `@Environment`) — without it macOS would trap at runtime.
- Verified: `swift test` 262 green; `IrisDemo-macOS` + `IrisDemo-iOS` `xcodebuild`-green (Debug); no new warnings.
- On `m9-unified-shell`. P3 now 🌱 in-progress (1 of 6 steps).
- 👉 Next: **M9·P3 step 2 — scaffold the shared shell on macOS** (lowest-risk: `IrisShell` + `SidebarView` with MODEL-top / page-rows / reserved-DATASET, replacing the `Videos|Images` picker; macOS stays green). Then steps 3–6 (shared detail extraction → iOS onto the shell → retire `InspectorHandoff` → iPhone bottom-sheet reflow). → [`BOARD.md`](./BOARD.md)

## 2026-05-30 (cont.) — build: M9·P3 steps 2–6 — the unified shell

- Built the shell rework as 5 sequential commits on `m9-unified-shell`, both schemes green at every gate, `swift test` **262** (unchanged — demo-only), `Sources/Iris/` + `plans/` untouched.
- **Step 2** (`cf22a0b`) scaffold on macOS: new `Apps/Shared/IrisShell.swift` (single cross-platform shell — owns both coordinators, `ModelSelection`, MRUs, model store, flagging/export, the `page` enum + `columnVisibility`; hosts the `NavigationSplitView`, routes `detailPane` by page, owns the shared `inspectorContent` + `detailToolbar`) + `SidebarView.swift` (+ `ShellPage` enum): MODEL section pinned top (detector picker + the **relocated** Min-confidence slider), page-rows (active expands inline to Open…/RECENT), reserved disabled DATASET stub bottom. Replaced the macOS `Videos|Images` segmented picker.
- **Step 3** (`05ffad4`) extracted shared `PlaybackDetailView.swift` (player + overlay + scrubber + bottom bar + on-frame Inspect/Flag; chrome bg parameterized) + `CaptureDetailView.swift` (`#if os(iOS)`).
- **Step 4** (`a8c1730`) moved iOS off the `TabView` onto `IrisShell`; chrome → sidebar; **Capture lifecycle keyed to page selection** (`page == .capture` → `start()`, leaving → `teardown()`/`invalidate()`), preserving the AVF safety. Left `ImageContentView.swift` dead-but-compiling for one commit.
- **Step 5** (`3392e4a`) retired `InspectorHandoff` — freeze-from-live is now a **direct** `imageCoordinator.setImage(frame, detector:)` + `page = .image`; deleted `InspectorHandoff.swift` (both types) + the dead `ImageContentView.swift` + all iOS handoff plumbing.
- **Step 6** (`9b56245`) iPhone reflow: `isRegularWidth` (macOS always true; iOS `horizontalSizeClass == .regular`) routes docked `.inspector` (regular) vs a `.sheet` w/ `[.height(120), .medium, .large]` + background interaction + drag indicator (compact) carrying Live detections + Metrics; bookmark top-right on every size class.
- Shell extensions split for length: `IrisShell+Playback/+Image/+Capture/+Presentation.swift`. `ImageDetailView` reused as-is with a new `showsControlBar: Bool = true` (shell passes `false` — the sidebar owns model selection now); its Tuning sheet is driven by the shell toolbar's Tune toggle.
- **Verification gap:** all gates were **headless `xcodebuild build`** (compile + strict-concurrency only) — the compact reflow + macOS behavior parity were **not** exercised on a running device/sim. **Hands-on smoke owed** before merge (bigger rework than prior phases).
- **🚩 Regression (flagged):** step 2 turned the **working macOS dataset footer** (`Export now`/`Reveal in Finder`) into a disabled stub, over-reading the mock's "reserved-but-deferred DATASET slot" (which was about not *adding* sidebar export, not *removing* the macOS export that worked). Background scenePhase sweeps + the count still run. Decision pending on [`BOARD.md`](./BOARD.md) §Status 🚩 (recommend restore).
- **P4 seam left in place:** Capture detector is still hardcoded to Vision rectangles; `CaptureModel.updateDetector(for:)` is a no-op hook for P4 to fill.
- P3 now 🔀 (all 6 steps built, unmerged).
- 👉 Next: **M9·P4 — Capture joins the shared model** (shared detector + live frame-loop swap + shared min-confidence; fixes A3) — but first settle the 🚩 macOS-export regression + a hands-on smoke of the shell. → [`BOARD.md`](./BOARD.md)

## 2026-05-30 (cont.) — build: M9·P4 — Capture joins the shared model

- Did: the live Capture page now runs the **shared `ModelSelection`** instead of a hardcoded `VisionRectanglesDetector` (fixes A3). `CaptureModel.detector` went `let`→`var`; the detect loop reads `self.detector` **fresh each frame** (no captured snapshot), so a swap takes effect on the next frame with **no camera/session restart**. `start(initialEntry:)` resolves the shared `detectorID` (via `DemoCatalog.detectors(store:)`, catalog-first fallback) so a model chosen while off the Capture page is honored on arrival; `updateDetector(for:)` (the P3 no-op hook) now does the live in-place swap, called from the shell's existing `onDetectorChanged()`.
- Concurrency: `CaptureModel` is `@MainActor @Observable`; both the per-frame read and the swap write are main-actor-isolated → race-free with no actor indirection / locks / `@unchecked`. The detect call awaits off-loop but captures the detector into a local first, so a mid-await swap just lands on the next frame. Resolution builds the entry's session against a **throwaway `ResultStore`** (reads `router.currentDetector` only — zero cache side effect). AVF session start/teardown untouched (still keyed to page selection, P3); only the detector reference changes.
- Min-confidence: already applied to the capture overlay via P3 step 1 (`CaptureDetailView` → `DetectionLayer(minConfidence:)`) — confirmed, not changed.
- No new files (2 edited: `IrisShell+Capture.swift`, `IrisShell.swift`); no xcodegen. Verified: iOS + macOS `xcodebuild`-green; `swift test` **262** (library untouched).
- **Build-verified only — the iOS Simulator has no real camera, so the loop never runs there.** On-device smoke owed (initial detector matches sidebar; live swap within a frame or two, no flicker; min-conf gates capture boxes).
- On `m9-unified-shell`, `b2201e0`. P4 now 🔀.
- 👉 Next: **M9·P5 — simplify** — one enum-routed importer pattern across all pages (the macOS `ActiveImporter` from P1 is the seed); generic MRU + coordinator merge stay backlog. Then an on-device/hands-on smoke of the shell + Capture before M9 → `main`. → [`BOARD.md`](./BOARD.md)
