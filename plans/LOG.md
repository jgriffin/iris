# Work log

<!-- Append-only. Newest at bottom. -->

## 2026-05-21
- Did: migrated to the `plans/` workflow described in [`WORKFLOW.md`](./WORKFLOW.md). Moved `BRIEF.md` from repo root into `plans/`. Pulled "Open design questions" and "Milestone path" out of [`../CLAUDE.md`](../CLAUDE.md) — questions now live in [`QUESTIONS.md`](./QUESTIONS.md), milestones in [`BRIEF.md`](./BRIEF.md). Created [`DECISIONS.md`](./DECISIONS.md) capturing the M0 exploration verdicts.
- Did: deleted `explorations/RECOMMENDATIONS-PRIOR-ART.md` — judged an anti-pattern after auditing the rollup against the per-arc files. Updated the four references that pointed at it.
- Did: enriched [`DECISIONS.md`](./DECISIONS.md) entries from one-liners to paragraph-per-decision with clickable links. Updated [`WORKFLOW.md`](./WORKFLOW.md)'s `DECISIONS.md` template and pruning guidance to match: entries should give enough context to act without opening the reference, blank-line-separated, file refs as markdown links.
- Note: existing exploration folders (`prior-projects/`, `swift-ecosystem/`, `project-shape-and-tooling/`, `runtime-pipeline-architecture/`, `display-pipeline-architecture/`) don't follow the `YYYY-MM-DD-topic/` naming from [`WORKFLOW.md`](./WORKFLOW.md). Leaving them as-is to preserve links; the dated convention applies to new explorations going forward.
- Next: M1 (`IrisCapture`) plans lock once the two M1-blocking opens in [`QUESTIONS.md`](./QUESTIONS.md) are settled — package-layout fork and the `Detector` stateful-conformer shape. Sidecar format can wait until M5.

---

- Did: shipped M1 in six phases — scaffolding ([`Package.swift`](../Package.swift), Swift 6.2 / iOS+macOS 26 / strict concurrency on; full tooling chain: `.swift-format` + `.swiftlint.yml` + `.githooks/pre-commit` + `.github/workflows/ci.yml` + DocC catalog + Git LFS), shared core types (`Frame`, `Source`, `SourceState`, `SourceError`, `MediaType`, `SourceKind`, `PixelFormat`, `AssetID`, `MockSource`), the iOS-only Capture stack (`CaptureSession` actor on a custom `DispatchSerialQueue` executor, `SampleBufferRouter`, `PreviewSource`/`PreviewTarget`, `AVCapturePreviewSource`, `PreviewView`, `CameraPreview`), tests (Swift Testing — MockSource on iOS+macOS, CaptureSession smoke gated to physical-device iOS), and the iOS demo app at [`Apps/IrisDemo-iOS/`](../Apps/IrisDemo-iOS/) generated via xcodegen ([`Apps/project.yml`](../Apps/project.yml)).
- Did: smoke test passed on physical iPhone — camera preview renders, frame timestamps stream to `os.Logger` at host rate, rotation reactively re-orients the preview via `AVCaptureDevice.RotationCoordinator` KVO routed through a new `PreviewSource.previewAngles: AsyncStream<CGFloat>`.
- Did: kept developer-specific values out of git via `Shared.xcconfig` (committed) `#include?`'ing `Local.xcconfig` (gitignored). `Local.xcconfig.template` checked in for bootstrap. Scrubbed an earlier accidental commit of `dev.griff.iris.demo` from the entire history with `git filter-repo` (no remote — safe local rewrite).
- Learned: the locked `@MainActor public protocol PreviewTarget` design in [`display-pipeline-architecture/RECOMMENDATIONS.md`](../explorations/display-pipeline-architecture/RECOMMENDATIONS.md) §Type sketches doesn't satisfy Swift 6 strict concurrency — the `@MainActor`-pinned existential can't be sent into a `Task`. Working amendment: drop `@MainActor` from the protocol declaration, push it onto `setSession` and onto `PreviewSource.connect(to:)`. Caller (`CameraPreview.makeUIView`) is already `@MainActor` via `UIViewRepresentable`, so the internal `Task` hop drops out entirely.
- Learned: `swift build` on the macOS host never compiles `#if os(iOS)` files, so it doesn't catch strict-concurrency bugs in the Capture folder. `xcodebuild -destination 'generic/platform=iOS'` (or a real `xcodebuild build`) is required to validate the iOS-gated subset. Worth adding to CI's test job.
- Learned: xcodegen auto-fills `PRODUCT_BUNDLE_IDENTIFIER = <bundleIdPrefix>.<target-name>` as an INLINE target setting when the target has no explicit value — sitting at higher precedence than xcconfig. Fix: drop `bundleIdPrefix` from `project.yml` so xcconfig wins cleanly.
- Learned: `CaptureSession` conforms via `@preconcurrency Source` because the actor's `var state` can't synchronously satisfy the protocol's nonisolated `get`. Clean long-term fix is making the `Source` protocol declare `var state: SourceState { get async }`. Punted to M2.
- Learned: `MockSource.start()` yields all supplied frames synchronously, and `.bufferingNewest(1)` means a single consumer only sees the LAST frame. Tests had to run one frame per session to assert ordering. Consider letting `MockSource` take a `bufferingPolicy:` parameter in M2.
- Learned: scriptable Xcode project generation via xcodegen + checked-in `.xcodeproj` is the right answer for a one-shot demo bootstrap, even though [`project-shape-and-tooling/RECOMMENDATIONS.md`](../explorations/project-shape-and-tooling/RECOMMENDATIONS.md) §2 deferred it. Manual Xcode-UI walkthrough is unnecessary friction when `project.yml` is 30 lines.
- Deferred to M2:
    - `TODO M1+` markers in `CaptureSession.configureSession(for:)` — interruption recovery via `AVCaptureSession.wasInterruptedNotification` / `interruptionEndedNotification` with the NextLevel-scar 100ms settle delay.
    - `SampleBufferRouter` tags every `Frame` orientation as `.up`; per-frame angle lookup against `rotationCoordinator` is plumbed but not invoked. Profile in M2 before turning it on.
    - Make `Source.state` async on the protocol so `CaptureSession` doesn't need `@preconcurrency` on the conformance.
    - Add a `bufferingPolicy:` parameter to `MockSource`.
- Deferred amendment to [`display-pipeline-architecture/RECOMMENDATIONS.md`](../explorations/display-pipeline-architecture/RECOMMENDATIONS.md): the locked `@MainActor public protocol PreviewTarget` shape doesn't pass Swift 6 strict concurrency. As-shipped design is the working version; update the recommendation doc in a follow-up.
- Next: M2 — `IrisDetection` (Vision adapter, body-pose or object detection) + `IrisOverlay` (`DetectionLayer` SwiftUI `Canvas` with `ResultStore` + `TimelineView`, `NormalizedGeometryConverting`). End-to-end live demo on iOS extends the existing `Apps/IrisDemo-iOS` app. Locked design in [`runtime-pipeline-architecture/RECOMMENDATIONS.md`](../explorations/runtime-pipeline-architecture/RECOMMENDATIONS.md) + [`display-pipeline-architecture/RECOMMENDATIONS.md`](../explorations/display-pipeline-architecture/RECOMMENDATIONS.md).

---

- Did: M2 kickoff — created [`plans/features/`](./features/) as the home for milestone & big-feature plans, registered the convention in [`WORKFLOW.md`](./WORKFLOW.md), and wrote [`plans/features/M2.md`](./features/M2.md) — the 7-phase plan for `IrisDetection` + `IrisOverlay` with M1-deferred items folded in.
- Did: Closed three [`QUESTIONS.md`](./QUESTIONS.md) entries — sidecar dataset format (removed; M5+ concern), package layout fork (stay single-target; the existing 2026-05-20 decision covers it), and `Detector` stateful-conformer shape (protocol stays `Sendable`-only; stateful conformers wrap state in an internal `actor`). The Detector close drew on existing recommendations in [`explorations/swift-ecosystem/RECOMMENDATIONS.md`](../explorations/swift-ecosystem/RECOMMENDATIONS.md) and [`explorations/prior-projects/RECOMMENDATIONS.md`](../explorations/prior-projects/RECOMMENDATIONS.md) — no new exploration needed.
- Did: Shipped M2 Phase 1 — detection scaffolding & core types. New files: `Sources/Iris/Detection/{Detector,Detection,TimestampedDetections,MockDetector}.swift`. `Detector` protocol matches the swift-ecosystem RECOMMENDATIONS shape (four members: `availability`, `modelIdentifier`, `prewarm()`, `detect(in:)`); top-level `DetectorAvailability` enum. `Detection` is a value-type struct with normalized bounding box, label, confidence, optional keypoints, optional `Mask` placeholder, source model ID.
- Did: Resolved M1-deferred `Source.state` `async` cleanup — [`Sources/Iris/Source.swift`](../Sources/Iris/Source.swift) requirement is now `{ get async }`; [`Sources/Iris/Capture/CaptureSession.swift`](../Sources/Iris/Capture/CaptureSession.swift) no longer needs `@preconcurrency` on its `Source` conformance. `MockSource` needed no code change.
- Did: 3 new Swift Testing cases in `Tests/IrisTests/Detection/DetectorTests.swift`; `swift build` clean (macOS + iOS `xcodebuild -destination 'generic/platform=iOS'`), `swift test` 6/6 passing.
- Learned: Swift forbids nested types in protocol extensions, so the swift-ecosystem RECOMMENDATIONS spelling `var availability: Detector.Availability { get }` isn't literally expressible (PFM's `LanguageModelBackend.Availability` only works because `LanguageModelBackend` is a struct). Shipped as top-level `DetectorAvailability`; amended the recommendation doc inline with the rationale.
- Learned: A non-async getter satisfies an `async` protocol requirement. Useful — `MockSource.state` didn't need touching when the `Source` protocol moved to `{ get async }`.
- Deferred to later in M2:
    - Mask payload shape on `Detection.Mask` — `// TODO M2+:` markers; placeholder `width`/`height` only. Phase 3's first segmentation adapter pins down the pixel-payload shape.
    - `MockSource.bufferingPolicy:` parameter — still pending; slated for Phase 1 or 2, will land alongside `ResultStore` tests that benefit from multi-frame replay.
    - 5th `Detector` member `detectStream(in:)` for trajectory / temporal streaming — additive default-impl per swift-ecosystem RECOMMENDATIONS; lands when the first temporal conformer needs it.
- Next: M2 Phase 2 — `ResultStore`. `@MainActor @Observable` sorted ring buffer keyed by `CMTime`; `append` / `clear` / `lookup(at:)` with staleness thresholds (500ms live / 2s playback). Pure value-type tests, no SwiftUI / camera in the test path. Folds in `MockSource.bufferingPolicy:` so multi-frame replay tests are tractable.

---

- Did: Shipped M2 Phase 2 — `ResultStore` and the M1-deferred `MockSource.bufferingPolicy:`. New files: [`Sources/Iris/Overlay/ResultStore.swift`](../Sources/Iris/Overlay/ResultStore.swift) (`@MainActor @Observable public final class`, sorted ring buffer keyed by `CMTime`, `append` / `clear` / `lookup(at:stale:)`, 500ms-live / 2s-playback staleness thresholds as public knobs), and [`Tests/IrisTests/Overlay/ResultStoreTests.swift`](../Tests/IrisTests/Overlay/ResultStoreTests.swift) (8 cases under one `@MainActor @Suite`). `MockSource.init` now takes `bufferingPolicy: AsyncStream<Frame>.Continuation.BufferingPolicy = .bufferingNewest(1)` — default preserves the existing `Source` contract; tests pass `.unbounded` when they need every frame to reach a single consumer. New [`Tests/IrisTests/Capture/MockSourceTests.swift`](../Tests/IrisTests/Capture/MockSourceTests.swift) case covers that. `swift test` 15/15, `xcodebuild -destination 'generic/platform=iOS' build` clean under Swift 6 strict concurrency.
- Learned: the locked `ResultStore` sketch in [`../explorations/display-pipeline-architecture/RECOMMENDATIONS.md`](../explorations/display-pipeline-architecture/RECOMMENDATIONS.md) §Type sketches compiled essentially as-written — `@MainActor` isolation gives `@Observable`'s generated tracking code a coherent home and the public surface stays clean. No amendment needed (unlike M1's `PreviewTarget` scar).
- Learned: swapped the sketch's `firstIndex(where:)` for a shared `upperBound` (C++ `std::upper_bound` semantics) primitive used by both `append` (insertion point preserving FIFO for equal timestamps) and `lookup` (rightmost `timestamp <= displayTime`). Locked rule M2 #2 mandates O(log n) lookup; same primitive cheaply makes the `append` search step O(log n) too.
- Learned: `var state: SourceState { get async }` is satisfiable by a plain synchronous getter, so `MockSource.state == .idle` test sites needed no `await` rewrite. M1-deferred protocol cleanup completed cleanly.
- Learned: the default `liveStalenessThreshold` (500ms) is permissive enough that timestamps in the `1..40 / 60` range used in tests get suppressed by staleness unless you pass a tighter `stale:`. The per-call `stale:` parameter is doing real work in the test surface, not a nicety — kept public.
- Deferred: no new items added. Phase 1's outstanding deferred list still stands (`Detection.Mask` pixel-payload shape pending first segmentation adapter; `detectStream(in:)` 5th `Detector` member additive when first temporal conformer needs it).
- Next: M2 Phase 3 — Vision detector adapter under `Sources/Iris/Detection/Vision/`. First target likely `VNDetectFaceRectanglesRequest` or `VNDetectHumanBodyPoseRequest` (stateless, so the locked `Sendable`-only `Detector` shape is trivially satisfied). `DetectorPipeline.swift` for chaining. Fixture-based test runs the detector over a sample clip in `Tests/IrisTests/Fixtures/` — first real use of the Git LFS-tracked fixture lane.

---

## 2026-05-22

- Did: Shipped M2 Phase 3 — Vision rectangle-detection adapter + `DetectorPipeline`. New files: [`Sources/Iris/Detection/Vision/VisionRectanglesDetector.swift`](../Sources/Iris/Detection/Vision/VisionRectanglesDetector.swift) (struct, stateless `Detector` conformer wrapping Apple's new value-type Vision Swift API), [`Sources/Iris/Detection/DetectorPipeline.swift`](../Sources/Iris/Detection/DetectorPipeline.swift) (parallel detector fan-out via `withThrowingTaskGroup` with indexed slots so input order is preserved), and tests under [`Tests/IrisTests/Detection/`](../Tests/IrisTests/Detection/) — `VisionRectanglesDetectorTests.swift` (hermetic CoreGraphics-drawn rect + pipeline equivalence) and `VisionRectanglesDetectorFixtureTests.swift` (clip-based via `AVAssetReader`, asserts ≥5/10 frames produce ≥1 detection with confidence ≥0.5 — observed 10/10). First LFS-tracked content: [`Tests/IrisTests/Fixtures/clipboard-blank-page.mp4`](../Tests/IrisTests/Fixtures/clipboard-blank-page.mp4) (Pexels, Artem Podrez, Pexels License, 1280×720 / 30 fps / ~9.5 s / 3.0 MB) plus [`Tests/IrisTests/Fixtures/README.md`](../Tests/IrisTests/Fixtures/README.md) documenting per-fixture provenance + license. Two atomic commits — `8f734bb` (hermetic code) and `a83e7aa` (fixture + clip test); binary-content commit shipped only after explicit user approval of the candidate clip. `swift test` 20/20, `xcodebuild -destination 'generic/platform=iOS' build` clean.
- Learned: Picked Apple's new value-type Vision Swift API (`DetectRectanglesRequest`, `RectangleObservation`, `@available(macOS 15.0, iOS 18.0, *)`) over the classic `VN…` NSObject API. Both request and observation conform to `Sendable` natively, `perform(on:orientation:)` is `async throws` directly (no `withCheckedThrowingContinuation` bridge), and tunables are plain `var Float` on a struct. Zero strict-concurrency escape hatches needed at the adapter boundary — `@preconcurrency import Vision` is not required. The [`DECISIONS.md`](./DECISIONS.md) "strict-concurrency escape hatches" entry stays theoretical for built-in detectors; the first Core ML / `VNCoreMLRequest` adapter will re-test that claim.
- Learned: `DetectRectanglesRequest.maximumAspectRatio` defaults to `0.5` — counterintuitively narrow. Only half-aspect-or-narrower rects pass through, so naïvely instantiating the request and pointing it at a square sheet of paper returns zero detections. Tests pass `maximumAspectRatio: 1.0` to accept square-ish rectangles; the default is flagged as a footgun in the adapter's doc comment.
- Learned: `prewarm()` is a defensible no-op for Vision built-ins. The new Swift API exposes no explicit prewarm hook, and running a throwaway request against a synthetic buffer would shift first-frame cost rather than remove it. The `Detector.prewarm()` contract permits an empty implementation. Revisit if first-frame latency is visible in the M2 Phase 7 demo.
- Learned: `Package.swift` already declared `resources: [.process("Fixtures")]` on the test target from M1 setup, so `Bundle.module.url(forResource:withExtension:)` works for fixture lookup with no new package wiring.
- Learned: `swift-format --strict` (run by the pre-commit hook) enforces 100-column line length independent of `.swiftlint.yml`, where `line_length` is disabled. The two linters have independent rulesets and the hook chains both. Worth aligning the configs so a `swiftlint`-passing edit doesn't fail the `swift-format` gate later.
- Deferred: Align `.swift-format` and `.swiftlint.yml` rule sets so the two linters don't disagree on line length / formatting style. Low priority — easy to fix in-place when a commit gets blocked.
- Deferred to later in M2: `Detection.Mask` pixel-payload shape (first segmentation adapter pins it); `Detector.detectStream(in:)` 5th member (additive when first temporal conformer needs it). Both carried over from Phase 1.
- Next: M2 Phase 4 — `NormalizedGeometryConverting` protocol + `PreviewLayerConverter` + `PlayerLayerConverter` under `Sources/Iris/Overlay/`. [`features/M2.md`](./features/M2.md) §Risks flags that `PreviewLayerConverter` needs `@MainActor` access analogous to M1's `PreviewTarget` scar — land the [`../explorations/display-pipeline-architecture/RECOMMENDATIONS.md`](../explorations/display-pipeline-architecture/RECOMMENDATIONS.md) amendment in the same commit so the recommendation doc reflects what shipped. Unit tests cover aspect-fit math directly so `PlayerLayerConverter` ships with coverage even though M3 adds end-to-end playback.

---

- Did: Shipped M2 Phase 4 — `NormalizedGeometryConverting` + `PreviewLayerConverter` + `PlayerLayerConverter`. Three new source files under [`Sources/Iris/Overlay/`](../Sources/Iris/Overlay/) plus [`Tests/IrisTests/Overlay/PlayerLayerConverterTests.swift`](../Tests/IrisTests/Overlay/PlayerLayerConverterTests.swift) (7 cases covering identity, Y-flip top-/bottom-left quadrants, vertical letterbox, horizontal pillarbox, and the two corner-point conversions). `PreviewLayerConverter` is `#if os(iOS)`-gated and delegates to `AVCaptureVideoPreviewLayer.layerRectConverted(fromMetadataOutputRect:)` / `layerPointConverted(fromCaptureDevicePoint:)`. `PlayerLayerConverter` carries an `AVPlayerLayer?` and routes through internal `static func convert(normalizedRect:videoRect:)` / `convert(normalizedPoint:videoRect:)` helpers — the held layer is unused on the math path, which is what makes the static helpers unit-testable without an AVF layer. Commit `80ef0f8`. `swift test` 27/27, `xcodebuild -destination 'generic/platform=iOS' build` clean.
- Learned: **The locked `NormalizedGeometryConverting` protocol shape compiled under Swift 6 strict concurrency on first try — no [`../explorations/display-pipeline-architecture/RECOMMENDATIONS.md`](../explorations/display-pipeline-architecture/RECOMMENDATIONS.md) amendment needed.** The `@MainActor` pressure that bit M1's `PreviewTarget` is absorbed *inside* `PreviewLayerConverter` via `MainActor.assumeIsolated { ... }`, relying on the fact that `DetectionLayer`'s `Canvas` body runs on MainActor by SwiftUI contract. The protocol stays `Sendable`-only. Concrete converters carry `@unchecked Sendable` with documented invariants (layer captured once at init; reads-only) per the [`DECISIONS.md`](./DECISIONS.md) escape-hatches doctrine. The risk flagged in [`features/M2.md`](./features/M2.md) §Risks turned out not to materialize.
- Learned: `PreviewLayerConverter`'s `viewRect(forNormalized:in:)` doesn't need its `in: videoRect` argument — `AVCaptureVideoPreviewLayer.layerRectConverted(fromMetadataOutputRect:)` already accounts for letterbox/aspect internally per §26 of the recommendations doc. Kept the parameter for protocol symmetry; documented the divergence inline.
- Learned: `@preconcurrency import AVFoundation` was needed in both converter implementation files, but neither type leaks AVF through its public surface — the import is contained at the seam.
- Deferred: `PreviewLayerConverter` end-to-end coverage requires a live `AVCaptureSession`, so it lands in the **Phase 7** physical-device smoke alongside the existing capture smoke. Math-only `PlayerLayerConverter` ships with full coverage this commit; the AVF-side will get exercised in M3 playback.
- Deferred (carryover, unchanged): `Detection.Mask` pixel-payload shape; `Detector.detectStream(in:)` 5th member; `.swift-format` ↔ `.swiftlint.yml` config alignment.
- Open for Phase 5: `DetectionLayer`'s default `converter:` argument shape. [`../explorations/display-pipeline-architecture/RECOMMENDATIONS.md`](../explorations/display-pipeline-architecture/RECOMMENDATIONS.md) §Type sketches defaults to `PlayerLayerConverter()`, but a converter without a backing `AVPlayerLayer` can't do anything useful at runtime — the `nil`-layer path is purely for the unit-test surface. Options: (a) drop the default and require callers to supply a converter, (b) ship a no-op `IdentityConverter` as the default for testing/preview scaffolding, (c) keep the `PlayerLayerConverter()` default and document that real use requires `.with(layer:)`. Land in Phase 5 when the call site exists.
- Next: M2 Phase 5 — `DetectionLayer` view in [`Sources/Iris/Overlay/`](../Sources/Iris/Overlay/). SwiftUI `Canvas` driven by `TimelineView(.animation(minimumInterval: 1.0/60))`, `.drawingGroup()`, `.allowsHitTesting(false)`. Reads `resultStore.lookup(at: displayTime)`, runs each `Detection.boundingBox` through an injected `NormalizedGeometryConverting`, draws stroked boxes. Resolves the default-converter open question above before locking the public init signature.

---

- Did: Shipped M2 Phase 5 — `DetectionLayer<Converter: NormalizedGeometryConverting>: View` in [`Sources/Iris/Overlay/DetectionLayer.swift`](../Sources/Iris/Overlay/DetectionLayer.swift) (139 lines) plus [`Tests/IrisTests/Overlay/DetectionLayerTests.swift`](../Tests/IrisTests/Overlay/DetectionLayerTests.swift) (2 cases — init smoke + MainActor-bound construction with synthetic `TimestampedDetections`). Generic over the converter (not existential `any`) for clean `Sendable`. `TimelineView(.animation(minimumInterval: 1.0/60))` wraps a single `Canvas` with `.drawingGroup()` + `.allowsHitTesting(false)`; the body samples `displayTime = CMClockGetTime(CMClockGetHostTimeClock())`, calls `store.lookup(at:stale:)`, and draws stroked rects + label backplates per detection. Hardcoded styling — `OverlayStyle` ships in Phase 6. Commit `3a15d06`. `swift test` 29/29, `xcodebuild -destination 'generic/platform=iOS' build` clean.
- Did: Resolved the Phase 4 open question — **dropped the `converter:` default arg** on `DetectionLayer.init`. The recommendations sketch defaulted to `PlayerLayerConverter()`, but a converter with no backing layer can't do useful runtime work; the call site has to be explicit. Same commit amended [`../explorations/display-pipeline-architecture/RECOMMENDATIONS.md`](../explorations/display-pipeline-architecture/RECOMMENDATIONS.md) §Type sketches + §M2 item 6 with inline `_Amendment 2026-05-22:_` notes (matching Phase 1's `DetectorAvailability` precedent in [`../explorations/swift-ecosystem/RECOMMENDATIONS.md`](../explorations/swift-ecosystem/RECOMMENDATIONS.md)).
- Learned: `TimelineView.Context.date` is wall-clock `Date` — *not* the right `displayTime` source. `Frame.timestamp` from `AVCaptureVideoDataOutput` is in the host-clock reference frame of `CMSampleBuffer.presentationTimeStamp`. The honest read is `CMClockGetTime(CMClockGetHostTimeClock())` directly inside the Canvas body. M3 playback will need a different read (`AVPlayer.currentTime`) — likely a second `init` overload or a `@Binding<CMTime>` override, called out as an M3 open below.
- Learned: `GraphicsContext.ResolvedText.measure(in:)` doesn't tolerate `CGSize(width: .infinity, ...)` — Swift/CoreFoundation overload ambiguity. Use `CGFloat.greatestFiniteMagnitude`. Separately, a 4-arg `CGRect(...)` literal with mixed arithmetic on the y-component blew Swift 6's type-checker budget; split components into pre-computed `CGFloat` locals to unstick it. Both are pure structural fixes, no design impact, but worth knowing as Canvas-body footguns.
- Open for Phase 6: (1) `OverlayStyle` shape — per-class color map and stroke width are floor; whether the label format is a closure / `LocalizedStringKey` / dedicated struct is the open knob. (2) Whether the hardcoded label backplate (semi-transparent black + white 11pt semibold) gets exposed via `OverlayStyle` or stays hardcoded. (3) `#Preview` cases need synthetic `Detection` arrays + a static `videoRect` — `MockDetector` is one option, a `[TimestampedDetections]` literal is another. (4) Static HTML reference for box rendering per CLAUDE.md "favorite pattern" — co-located in `Sources/Iris/Overlay/` showing box rendering at multiple sizes / letterbox configurations.
- Open for M3 (carried, sharpened): `displayTime` plumbing for playback. M2's host-clock read is correct for live capture; M3 will need an override path so the playback consumer can pipe `AVPlayer.currentTime` through. Probably a second `DetectionLayer.init` overload taking `@Binding<CMTime>` rather than a generalized parameter on the existing one — keeps the live-capture call site terse. May want pre-emptive consideration in Phase 6 so adding it after `OverlayStyle` doesn't churn the init twice.
- Deferred (carryover, unchanged): `Detection.Mask` pixel-payload shape; `Detector.detectStream(in:)` 5th member; `.swift-format` ↔ `.swiftlint.yml` config alignment; `PreviewLayerConverter` end-to-end coverage (Phase 7 physical-device smoke).
- Next: M2 Phase 6 — `OverlayStyle` + `#Preview` cases + co-located static HTML reference. Resolve the four Phase 6 opens above; sharpen the M3 `displayTime` override path *before* committing `OverlayStyle` so the init reshape happens once.
