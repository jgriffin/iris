<!-- The board: where work stands (Status), the path (Milestones), what's deferred (Backlog).
     Status rewritten each block; Milestones/Backlog edited as they change. Best viewed monospace.
     §Status IS the overview tree (WORKFLOW.md §"Status trees"): every completed milestone keeps its
     OWN `├─ ✅ Mn — name` line — NEVER collapse them into a "done (all ✅) — M1 · M2 · …" one-liner
     (Rule 6: no horizontal stacking). The active milestone is the last node, expanded inline into its
     phase rows (the embedded focus tree). -->

# Iris — Board
_Snapshot · 2026-06-04_

## Status

├─ ✅ M1 — Capture core — IrisCapture + CameraPreview view + AsyncStream<Frame> (iOS-only)
├─ ✅ M2 — Detection + overlay — Vision adapter + ResultStore + DetectionLayer overlay + coordinate converters; live iOS demo
├─ ✅ M3 — Playback — PlaybackSource video stream + seek/step controls + Scrubber UI; first macOS demo target
├─ ✅ M4 — Tuning — @Observable TuningModel + TunableDetector + built-in Vision tuning UI  ·  filter-time pipeline pass 🚫 dropped (live re-run proved fast enough)
├─ ✅ M5 — Honest detectors — per-detector capability model → derived tuning UI + capability-honest overlays + raw-data inspector
├─ ✅ M6 — Custom models — Core ML adapter + pluggable YOLO OutputDecoder (Path A + B) + model-swap catalog/UI  ·  captioning 🚫 dropped (Foundation Models is text-only)
├─ ✅ M7 — Dataset — flag frames in playback → headless FrameExporter writes provenance-named PNGs (filenames are the dedup ledger; no sidecar)
├─ ✅ M8 — Image — run detectors on a single still + swap/compare models on it; DetectionRunner extraction + still→upright-Frame decode + one-shot ImageDetectionCoordinator + demo Image page + freeze-from-live handoff  ·  dataset tie-in 🗓️ backlog (not training yet)
├─ ✅ M9 — Unified shell — one app-level shared model (detector + min-confidence) across Playback/Image/Capture + one cross-platform left-pane sidebar (`NavigationSplitView` + `ModeSection` accordion) replacing the iOS tabs + macOS Videos|Images toggle. P1 reliability · P2 shared model · P3 shell · P4 Capture joins · P5 simplify · P6 in-canvas design pass (active-section redesign). → merged to `main` → [features/unified-sidebar/README.md](./features/unified-sidebar/README.md)
├─ ✅ M10 — Per-class tuning — per-label confidence floors + per-label hide/show in the sidebar MODEL section (render-side `OverlayFilter` keyed on `Detection.label`, present-only rows w/ visibility toggle + per-label floor + reset-to-global, wired across Playback/Image/Capture); library gained only the generalized filter + an `availableLabels` capability. → merged to `main` · follow-ons picked up as M11 → [features/per-class-tuning.md](./features/per-class-tuning.md)
├─ ✅ M11 — Unified tuning panel — the M10 deferrals delivered in one pass: detector-input knobs joined the per-class Display controls in ONE inspector tuning panel (Detector group w/ confidence affordance suppressed + Display group w/ global floor); per-class rows gained tri-state Hide/Auto/Show visibility (pinned "Show" = the old favorites idea) + per-label floors + a "show all classes" roster expander (`availableLabels`); Freeze + Flag moved to the window toolbar. Smoke passed; merged to `main` (fast-forward). 278 tests. Accumulation left OUT → picked up as M12 → [features/per-class-tuning.md](./features/per-class-tuning.md)
└─ 🚧 M12 — Label accumulation — **in flight** (branch `m12-label-accumulation`). One per-detector store that IS the whole panel's per-class state (`detector id → label → {visibility?, floor?}`; `{}` = "seen", tri-state Auto): accumulated sightings **and** the user's show/hide/floor opinions in one map, keyed by detector. No sources (`AssetFingerprint` exits M12); all three modes write sightings; per-class floor + tri-state visibility go per-detector (absorbed off `ModelSelection`, which keeps only `detectorID` + the global `minConfidence`); M11 binary expander (seen ▸ roster) retained; clear = forget sightings only; persisted, app-side only.
   ├─ ✅ P1 — the store — `DetectorLabelStore` stood up (`Apps/Shared/State/`), per-class state absorbed off `ModelSelection` (bridge shim left for P3), schema-v1 UserDefaults/JSON, clear-sightings-only + clamp semantics; start-clean on legacy keys. Committed `6626ec5`.
   ├─ ✅ P2 — feed it (all 3 modes) — sightings recorded for the detector that **produced** them: Playback + Image via the shell's `.onChange(of: presentLabels)` seam (attributed to `syncedVideo/ImageDetectorID`, the install-time id — race-free since `selectDetector` clears the cache before re-running); Capture at its in-loop production point (`CaptureModel` tracks `installedDetectorID`, swaps it in lockstep with the live detector — the only race-free seam for an in-place swap with no cache clear). Committed `3fc87fb`.
   ├─ ✅ P3 — panel rewiring — roster derives from the store's keys (stable, no flicker); `presentLabels` re-sourced to mark live rows only (live = primary name, accumulated = secondary); M11 binary expander kept (seen ▸ `availableLabels`); per-detector **"Clear seen"** affordance added by the expander (disabled when no clearable bare sightings); `PerClassControls`/`PerClassRow` + the 3 detail views' overlay-filter assembly re-pointed at the store + active detector id. **The P1 bridge shim is fully gone from `ModelSelection`** (the acceptance test passed). Previews refreshed (accumulated-not-live / cleared / fresh-detector). 278 tests; both schemes green; hands-on smoke passed. Committed `900cbf5`.
   ├─ ✅ P4 — polish + static preview gallery — detector-switch state reset (`.onChange(of: detectorID)` clears the open slider + expanded roster); per-case previews consolidated into ONE stacked gallery × light + dark (accumulated-only **new** / mixed / cleared / no-roster / empty). 278 tests; both schemes green. Committed `098d8b4`.
   └─ ✅ P5 — hidden classes leave the working list (user call post-P4) — working list = Auto + Show rows only; hidden labels group under a HIDDEN sub-caption at the top of the expander (quick-unhide surface) + repeat in the roster below; unhide → Auto returns the row (dimmed until live); no-roster detectors get a "Hidden classes" expander when needed; "All seen classes are hidden." empty-state; gallery cases updated (+ no-roster-plus-hidden). 278 tests; both schemes green. Committed `032a349`.
   → [features/label-accumulation.md](./features/label-accumulation.md)

👉 next — **M12 close-out**: all four phases ✅ and the P3 smoke covered the milestone's hands-on surface; `main` carries the branch through P3 + the importer fix. Remaining: eyeball the new gallery in Xcode previews if desired, merge `m12-label-accumulation` → `main` (fast-forward), retire the branch — then pick up **folder sources** as the next milestone → [features/folder-sources.md](./features/folder-sources.md). → [features/label-accumulation.md](./features/label-accumulation.md)

❓ open → [QUESTIONS.md](./QUESTIONS.md)
- ⚖️ Multi-detector pipelines under `TuningModel` (multi-active selection defers here)
- ⚖️ "What if?" mode (BRIEF §5)

📌 recent → [DECISIONS.md](./DECISIONS.md)
- **Folder sources penciled** (user, 2026-06-05) — Playback + Image gain "pick an entire folder": collapsible per-section sidebar block listing the folder's matching files, picking a child loads it + promotes it into RECENT as usual. **MRU of folders** (not one active folder); pulls the backlog "shared MRU generic" in (`RecentFolders` would be sibling #3). Opens: block placement + multi-folder presentation (settle in-canvas, M9·P6-style), right-click MRU-entry removal (maybe; would apply to file recents too), large-folder cap. Pick up **after M12·P4** → [features/folder-sources.md](./features/folder-sources.md) (2026-06-05)
- **M12 pivot — one per-detector store, no sources** (user, 2026-06-04) — the user reviewed a concrete per-source-ledger example and rejected the source layer ("the sources bit gets in the way… makes everything unreadable"). New design: **one per-detector store that IS the whole panel's per-class state** — `detector id → label → {visibility?, floor?}`, where key membership = accumulation (`{}` = seen/Auto) and values = the user's opinions. **Supersedes morning specifics:** sources dropped (no `AssetFingerprint` in M12 → per-video scoping backlogged), scope stepper dropped (M11 binary expander kept), video-first lifted (**all three modes** feed it), and "settings stay global" reversed (**per-label floor + tri-state visibility go per-detector**, absorbed off `ModelSelection`). Clear = forget sightings only (opinions survive). Persisted, app-side. Type name `DetectorLabelStore` (P1 confirms). Phases redrafted: **P1** the store (+ absorb/migrate) · **P2** feed it (all 3 modes) · **P3** panel rewiring · **P4** polish + static preview → [features/label-accumulation.md](./features/label-accumulation.md) (2026-06-04)
- **M12 opens settled + phases drafted P1–P4** (user, 2026-06-04) — the four pickup opens resolved: **video-first** (M12 ships the ladder for Playback only; Image needs a still fingerprint → M8·P6, Capture has no durable identity → both fast-follows), default scope **L2 (All videos)** with **ephemeral** narrowing (resets on video change — assistant's derived call), **presence-only** ledger collapsed to `(detectorID, sourceKey, label)` (no `lastSeen`), hygiene = a per-detector **"clear seen labels"** button (no auto-eviction/caps). Phases drafted: **P1** ledger store · **P2** feed it (Playback) · **P3** scope UI · **P4** polish + static preview. One open left: where the store lives in `Apps/Shared/State/` (lean a sibling `LabelLedger`; settle in P1) → [features/label-accumulation.md](./features/label-accumulation.md) (2026-06-04)
- **M12 — Label accumulation picked up** (📋; number assigned at pickup, per the naming convention) — the penciled multi-level accumulation feature is now the active milestone. Defined, no phases drafted yet; next move is the phase breakdown in the feature doc → [features/label-accumulation.md](./features/label-accumulation.md) (2026-06-04)
- **M11 closed — shipped to `main`** (✅; `m11-unified-tuning-panel` → `main`, fast-forward `9f28420..f015ef1`) — smoke passed; M1–M11 now on `main`. Delivered the unified inspector tuning panel (Detector + Display groups) + tri-state per-class visibility + per-label floors + "show all classes" expander; Freeze/Flag moved to the window toolbar. Accumulation left OUT → picked up as M12. Branch retired (fully merged) → [features/per-class-tuning.md](./features/per-class-tuning.md) (2026-06-04)
- **M11 ships as-is; label accumulation penciled as its own milestone** (user) — present-label accumulation stays OUT of M11 (remaining close-out = hands-on smoke + main-merge). The old "present-label accumulation" backlog one-liner grows into a penciled **multi-level label accumulation** milestone: scope ladder L0 Frame → L1 Video → L2 Detector → L3 Roster, all powered by one per-detector **label ledger** keyed on M7 `AssetFingerprint` sources; **roster-only** (settings stay global), persistence in, similarity-grouping out → config profiles → [features/label-accumulation.md](./features/label-accumulation.md) (2026-06-04)
- **M11 — Unified tuning panel, in flight** (🌱 on `m11-unified-tuning-panel`, `56b5e7a` + hardening) — found built-but-uncommitted in the working tree at the M10 boundary; verified green (278 tests, both schemes) and committed as M11. Delivers three M10-deferred backlog items in one pass: the unified-panel **Detector group** (knobs in the inspector next to Display, confidence suppressed — `hidesConfidenceControl`), full-roster **"show all"** (`DetectorCatalogEntry.availableLabels`), and **favorites** (as tri-state pinned "Show"). Sidebar MODEL section is now picker-only; Freeze/Flag live in the window toolbar. Present-label accumulation NOT included (stays backlog). Includes the in-repo HTML design mock (`Inspector/mocks/`) (2026-06-03)
- **M10 closed — shipped to `main`** (✅; `m10-per-class-tuning` → `main`, fast-forward) — P1–P4 done; M1–M10 now on `main`. Delivered per-class tuning (per-label floors + hide/show) in the sidebar MODEL section. **User accepted the one deferral:** the unified-panel **Detector group** (co-presenting detector-input knobs with the per-class filter — the original "unified panel" intent) shipped only its Display/filter half; the detector-knob surfacing + full-roster "show all" + present-label accumulation are parked in §Backlog (detector knobs still live in the per-coordinator Tune sheet). Library touched minimally: the generalized `OverlayFilter` + `DetectorCapabilities.availableLabels` → [features/per-class-tuning.md](./features/per-class-tuning.md) (2026-06-02)
- **M10 — Per-class tuning defined** (📋; the backlog's "per-category tuning", now scoped) — generalize M9's global floor into per-label floors + hide/show. User's framing: **unified UI presentation, honest data** — one MODEL-section panel co-presents detector-input knobs + the render-time filter, *visually delineated* into a Detector group + a Display/filter group, but the data classes stay distinct (north-star "one settings class" still deferred). Per-class is **render-side data** (keys on `Detection.label` after emit), state lives app-side by the global floor; library gains only the generalized filter + `availableLabels`. UI home = sidebar MODEL section expanded. Labels present-only + "show all". **Favorites** + **config profiles** deferred → §Backlog. 4 phases drafted → [features/per-class-tuning.md](./features/per-class-tuning.md) (2026-06-01)
- **M9 closed — shipped to `main`** (✅; `m9-unified-shell` → `main`) — all 6 phases done; M1–M9 now on `main`. Residuals parked in §Backlog (Image glyph pairing; Capture on-device smoke). M9 was demo-wiring only — the `Iris` library was untouched the whole milestone (the one exception: P3's render-time min-confidence filter) → [features/unified-sidebar/README.md](./features/unified-sidebar/README.md) (2026-06-01)
- **M9·P6·4 active-section redesign done** (✅ on `m9-unified-shell`, `97ad866`) — in-canvas P6 design pass (user driving the look over ~5 rounds via a throwaway `#Preview` gallery): the active mode section is now a square, full-bleed band — a 3pt left accent bar over header+body, an accent-tinted header (0.22) with the **filled** mode icon + an **open-icon button on the header line** (replacing the bordered "Open…"), flowing into a fainter recents body (0.08); recents dropped the RECENT caption + use a distinct file glyph (`film`); inset dividers between sections restore the horizontal rules. `SourcePicker` deleted, `.mode` header style removed, `ShellPage.activeSystemImage` added → [features/unified-sidebar/README.md](./features/unified-sidebar/README.md) (2026-06-01)
- **M9·P6·3 sidebar-is-sections done** (✅ on `m9-unified-shell`, `c1c12f9`) — behavior-preserving recast (user: "these aren't rows, they're entire sections"): Playback/Image/Capture → selectable `ModeSection`s with accordion expansion keyed to the page selection (expanding = selecting); one shared header component, two styles (plain label / selectable mode, kept distinct); `SourcePicker` extracts the Open…+RECENT dup; five sections listed explicitly (no `ForEach`); `NavigationSection`/`SidebarRow` deleted. Init unchanged, both schemes green, 262 tests → [features/unified-sidebar/README.md](./features/unified-sidebar/README.md) (2026-05-31)
- **M9·P6·2 sidebar decomposition done** (✅ on `m9-unified-shell`, `8594444`) — behavior-preserving split of `SidebarView` into design-language components (`SidebarSection` w/ optional collapsibility + `SidebarRow`, in `Shell/Sidebar/Components/`) + per-section files (Model/Navigation/Dataset/RecentList), each previewable; `ShellPage` lifted out; public init byte-identical (call site untouched). Both schemes green, 262 tests. Gives the design language seams to work the in-canvas P6·3+ changes against → [features/unified-sidebar/README.md](./features/unified-sidebar/README.md) (2026-05-31)
- **M9·P6·1 scaffolding done** (✅ on `m9-unified-shell`, `c9a66fa`) — designs weren't landing on paper, so P6 iterates the shell design **live in Xcode previews**: `Apps/Shared/` split into `Shell/`·`Details/`·`State/` (recursive glob → no `project.yml` edit), `PreviewFixtures` (throwaway `iris.preview` defaults suite) + a `SidebarView` `#Preview` matrix that renders iOS vs macOS by scheme-flip. Both schemes green, 262 tests. Design changes themselves = P6·2+ → [features/unified-sidebar/README.md](./features/unified-sidebar/README.md) (2026-05-31)
- **M9 reopened with a P6 design pass** (user) — UI/design work folds into the shell *before* the `main` merge. M9 🔀→🌱 in-progress until P6 lands → [features/unified-sidebar/README.md](./features/unified-sidebar/README.md) (2026-05-30)
- **Lifecycle clarified: a phase committed *directly* on the milestone branch is ✅, not 🔀** (user) — its integration target *is* that branch, so the commits are already merged up; 🔀 is only for a node on its **own** branch awaiting a merge (the milestone → `main`). Don't paint built-on-the-branch phases 🔀 because the milestone hasn't reached `main` — that conflates the levels. M9 P1–P5 corrected 🔀→✅ → [WORKFLOW.md](./WORKFLOW.md) §"Status trees" (2026-05-30)
- **M9·P5 shipped — M9 feature-complete** (✅ on `m9-unified-shell`, `601c70b`) — one `ImportTarget` enum + single `.fileImporting` modifier collapsed the importer duplication (~−67 lines); platform DocumentPicker/.fileImporter seam kept. All 5 phases built, both schemes green; hands-on smoke + main-merge pending → [LOG.md](./LOG.md) (2026-05-30)
- **M9·P4 shipped** (✅ on `m9-unified-shell`, `b2201e0`) — Capture joins the shared model: live in-loop detector swap (no session restart), initial detector from the shared selection, min-confidence via the overlay. Build-only (sim has no camera) → [LOG.md](./LOG.md) (2026-05-30)
- **M9·P3 shipped — the unified shell** (✅ on `m9-unified-shell`, steps 2–6 = `cf22a0b`→`9b56245`) — one `IrisShell` (`NavigationSplitView` + custom sidebar) replaces the iOS `TabView` + macOS Videos/Images toggle; all coordinators in one shell (kills the tab-switch reload + scroll-reset); `InspectorHandoff` deleted (direct freeze-from-live); iPhone drawer + bottom-sheet inspector. Smoke fixes since: macOS export restored, sidebar cleanup, **iPhone compact-split nav fixed** (`preferredCompactColumn`) → [LOG.md](./LOG.md) (2026-05-30)
- **M9·P3 step 1 shipped** (✅ on `m9-unified-shell`) — render-time confidence filter: `[Detection].filtered(minConfidence:)` (+6 tests, 262 green) applied by `DetectionLayer` (raw inspector unfiltered); global Min-confidence slider wired on both platforms. The **one sanctioned `Iris` library** addition this milestone — settled as a render-time *overlay* filter (output), not a detector setting → [DECISIONS.md](./DECISIONS.md) (2026-05-30)
- **M9·P2 shipped** (✅ on `m9-unified-shell`) — one app-level `ModelSelection` (`Apps/Shared/`) shared across Playback + Image; macOS playback≠image split **collapsed** into the single global model (per the M9 intent). Min-confidence held + persisted but **not yet consumed** — wired in P3. Image re-appear flip fixed via a demo-side `syncedDetectorID` re-install on appear (coordinators don't expose installed id). Both demo schemes green → [LOG.md](./LOG.md) (2026-05-30)
- **M9·P1 shipped** (✅ on `m9-unified-shell`) — macOS movie/model file-picker collision → one enum-routed importer; Image detector picker + Tune gated until a frame loads; bookmark-resolve logging on Recent Images/Videos. 3 commits, both demo schemes green, library untouched → [LOG.md](./LOG.md) (2026-05-29)
- **✅ = merged to its integration target; 🔀 = merge-pending** — phase→milestone branch, milestone→`main`; restores board honesty (M8 was ✅ while still unmerged) → [WORKFLOW.md](./WORKFLOW.md) §"Status trees" (2026-05-29)
- **M9 pulled forward** as the active milestone — the unified-shell work (shared model + left-pane-driven shell) supersedes the earlier "sidebar after M8·P5/P6" sequencing → [features/unified-sidebar/README.md](./features/unified-sidebar/README.md) (2026-05-29)
- **M8 landed on `main`** (✅ — P1–P5 incl. freeze-from-live; P5 **shipped**, no longer parked); P6 dataset tie-in stays 🗓️ backlog (not training yet) → [features/M8.md](./features/M8.md) (2026-05-29)
- UI-reliability **audit done** → M9 phased **P1–P5**: P1 reliability quick wins · P2 shared model store · P3 left-pane shell · P4 Capture joins · P5 simplify → [features/unified-sidebar/README.md](./features/unified-sidebar/README.md) (2026-05-29)
- Milestone naming: descriptive slug + one-liner; numbers assigned at pickup only, never reserved for penciled work → [WORKFLOW.md](./WORKFLOW.md) / [DECISIONS.md](./DECISIONS.md) (2026-05-29)
- "Unified sidebar nav" penciled after M8·P5/P6; built on near-final nav not the interim P4 seeds (accepted: P5/P6 wire into interim nav) → [features/unified-sidebar/README.md](./features/unified-sidebar/README.md) (2026-05-29)
- Sidebar's global MODEL = app-level shared detector + confidence across all 3 pages incl. Capture ⇒ live-capture detector-swap in scope → [features/unified-sidebar/README.md](./features/unified-sidebar/README.md) (2026-05-29)
- M8 defined ([features/M8.md](./features/M8.md)): run detectors on a single static image, swap/compare models in that one world. Pipeline+overlay already source-agnostic (`Frame`/`DetectorPipeline`/`VideoGeometry`/`ResultStore` have no video coupling); only `PlaybackDetectionCoordinator`+`Scrubber`+`FlaggingSource` are PTS-coupled. 5 settled forks: image detection is **one-shot** (reuse `DetectorPipeline` w/ frozen timestamp, **no** 1-frame stream); **P1 = full `DetectionRunner` extraction** (resolves the source-agnostic-decomposition question — the image inspector is the second consumer); new `Sources/Iris/Image/` folder; freeze-from-live in scope; dataset tie-in in scope (image-shaped `AssetFingerprint` minus `durationSeconds` + PTS/seek-free `FlaggingSource`). → [DECISIONS.md](./DECISIONS.md) (2026-05-29)
- M7·P4 redefined (user — **refines**, doesn't contradict, the same-day "seam only"): P4 now ships a concrete **`FrameExporter` frame-export sweep** (resumable/interruptible; drives P3's `DatasetBuilder` over `RecentVideos`-resolved URLs; app-side launch/`scenePhase`-background/"Export now" triggers; `export-status.json` operational telemetry incl. unreachable sources). `DatasetExporter` training-FORMAT conversion stays deferred. `RecentVideos` MRU-10 caveat noted (ledger approach (b) = follow-up). (2026-05-28)
- M7 sidecar reframe (user — supersedes the COCO call): **no per-image sidecar, no COCO, no exporter in M7** (a flag = "look again," not an annotation; multiple models tried ⇒ a per-image verdict is false precision). `AssetFingerprint` now **name-independent** (`byteSize`+`durationSeconds`+mandatory head-hash; filename display-only) → rename-stable + edit-sensitive. Provenance rides the export filename (`<sourceNameHash>_<fingerprintID>_<ptsMillis>.png`); **dedup keys on the suffix** — the dataset's own filenames are the ledger (lives WITH the data, can't go stale). P4 → `DatasetExporter` **seam only**, first exporter deferred to when a training pipeline names its format. (2026-05-28)
- M7·P2 UI call (user): the **primary flag affordance lives ON the frame image** (top-right bookmark puck via `VideoRectAligned`/`VideoGeometry`), not a control-row button; **timeline markers are a coarse secondary overview**, never the source of truth (a thin strip can't resolve adjacent frames; ticks inset by thumb radius to align). (2026-05-28)
- M7 defined ([features/M7.md](./features/M7.md)): frame address = `(AssetFingerprint, PTS)`; content fingerprint not URL; cheap flagging / deferred headless extraction; deterministic-naming dedup; output under `<Documents>/iris-dataset/`. Scope = **playback**; live-capture flagging is a follow-on (can't re-seek). *(Sidecar/COCO half superseded by the reframe above.)* (2026-05-28)
- `demo-sim-runnable` fast-forwarded to `main` (`40cf0de`); hands-on smoke skipped (owed) (2026-05-28)
- Swap root-cause corrected: the `f4a6284` cancel→drain→respawn fix proved a **no-op** (`PlaybackSource` exposes a single stored `AsyncStream` that dies permanently on consumer cancel — respawned `for await` gets zero frames); coordinator uses **one loop + in-place router swap** instead. P2/P3 fix the demo swap bug for the first time (2026-05-27)
- PlaybackDetectionCoordinator defined: `@MainActor @Observable` library type in `Playback/`; 4 phases (P1 build+test, P2/P3 rewire demos, P4 deferred) (2026-05-27)
- Playback session orchestration → a library `PlaybackDetectionCoordinator` in `Playback/`; demos keep only file/scope/catalog/layout; source-agnostic core not pre-split (2026-05-27)
- M6 merged to `main` (fast-forward); playback detector-swap fix + this analysis on branch `fix-playback-detector-swap` (2026-05-27)
- M6 closed: P1–P3 ✅; captioning (P4) dropped — Foundation Models is text-only, on-device captioning needs a VLM (2026-05-26)
- M6·P3 closed: model loading (prewarm, bundled-at-launch, file-picked Path-A) shipped (2026-05-26)
- M6·P3: path-B YOLOEnd2EndDecoder + runtime confidence knob (conditional TunableDetector) shipped (2026-05-26)
- M6·P2: Path-A CoreMLDetector shipped; runtime thresholds deferred to P3 (2026-05-26)
- Core ML detector: start with YOLOv12 (Path A), pluggable `OutputDecoder` seam (2026-05-25)
- VideoGeometry = single coordinate-mapping authority; orientation/mirroring upstream (2026-05-25)
- Self-describing detections (skeleton + readout on `Detection`) (2026-05-25)

## Milestones

The roadmap legend — one line per milestone, what it delivers. State lives in §Status above; this section answers "what is M5 again?".

- **M1 — Capture core** — `IrisCapture` + `CameraPreview` SwiftUI view + `AsyncStream<Frame>`; iOS only.
- **M2 — Detection + overlay** — `IrisDetection` Vision adapter + `IrisOverlay` box drawing; end-to-end live iOS demo. → [features/M2.md](./features/M2.md)
- **M3 — Playback** — `IrisPlayback` with the same `Frame` stream; same overlay on recorded video; first macOS target. → [features/M3.md](./features/M3.md)
- **M4 — Tuning** — `IrisTuning` confidence/class/NMS controls via `@Observable`; three-tier change taxonomy. → [features/M4.md](./features/M4.md)
- **M5 — Honest detectors** — per-detector capability model driving derived tuning UI + capability-honest overlays + a raw-data inspector. → [features/M5-honest-detectors.md](./features/M5-honest-detectors.md)
- **M6 — Custom models** — Core ML adapter with a pluggable YOLO-style `OutputDecoder`; model-swap UI. (Captioning dropped — Foundation Models is text-only.) → [features/M6.md](./features/M6.md)
- **M7 — Dataset** — `IrisDataset`: flag frames during playback → extract as provenance-bearing images (filenames are the dedup ledger; no sidecar); training-format export deferred. → [features/M7.md](./features/M7.md)
- **M8 — Image** *(closed at core; P5/P6 → backlog)* — run detectors on a single static image (captured/playback frame, screenshot, any still) + swap/compare models on that one image; `Sources/Iris/Image/` + a demo Image page; triggers the source-agnostic `DetectionRunner` extraction. → [features/M8.md](./features/M8.md)
- **M9 — Unified shell** — one shared model + a left pane that drives the modes; one cross-platform sidebar replaces the iOS tabs + macOS `Videos|Images` toggle (global MODEL section shared across Playback / Image / Capture incl. Capture's new detector-swap, page-rows with inline Open…/RECENT, iPhone drawer + bottom-sheet inspector); folds in the reliability fixes. → [features/unified-sidebar/README.md](./features/unified-sidebar/README.md)
- **M10 — Per-class tuning** — generalize M9's global confidence floor into per-label floors + per-label hide/show, surfaced in a unified model-settings panel (detector knobs + display/filter rows, delineated, in the sidebar MODEL section); render-side data, library gains only the generalized filter + an `availableLabels` capability. → [features/per-class-tuning.md](./features/per-class-tuning.md)
- **M11 — Unified tuning panel** — detector-input knobs + per-class Display controls co-presented in one inspector tuning panel (the M10 unified-UI deferral); tri-state per-class visibility + per-label floors + full-roster "show all"; Freeze/Flag → window toolbar. → [features/per-class-tuning.md](./features/per-class-tuning.md)
- **M12 — Label accumulation** — steady + widen the per-class roster via one per-detector store that IS the whole panel's per-class state (`detector id → label → {visibility?, floor?}`; key = seen, value = the user's opinion). All three modes feed it; per-class floor + tri-state visibility go per-detector (absorbed off `ModelSelection`); M11 binary expander (seen ▸ roster) kept; no sources; persisted, app-side only. → [features/label-accumulation.md](./features/label-accumulation.md)

## Backlog

<!-- Stub = one line (`🗓️ headline — hook`). Add a ≤4-line indented body only when needed.
     Link out (→ features/ or exploration) when the item has a real home. -->

- 🗓️ Folder sources — pick an entire folder in Playback/Image; collapsible sidebar block of its contents; MRU of folders (`RecentFolders` + the shared-MRU-generic pull-in). Penciled as the next milestone candidate after M12. → [features/folder-sources.md](./features/folder-sources.md) (user, 2026-06-05)
- 🗓️ Adopt git workflow policy — branching + auto-commit + merge cadence into `WORKFLOW.md` (the portable lead doc), CLAUDE.md pointer, branch rename.
      The assistant's "commit only when asked" is a **harness default** (+ the intent-guard hook), opposite the user's want: proactive commits, milestone (`mN-<slug>`) + phase (`mN-pX-<slug>`) branches, readiness-gated merges, human out of the loop; `main` the one deliberate gate. Single home = `WORKFLOW.md §Branching & commits`; one open fork (main-merge autonomy). → [features/workflow-git-policy.md](./features/workflow-git-policy.md) (user, 2026-05-29)
- 🗓️ macOS split-view layout-loop crash — intermittent NSGenericException ("more Update Constraints in Window passes than views" safety valve) firing from `-[NSSplitView mouseDown:]` while dragging a NavigationSplitView divider; predates M11 (user-confirmed). Per-class rows hardened defensively (exact row height + lineLimit(1), M11); root cause unconfirmed — no `.ips` reports (debugger-caught). If it recurs: note WHICH divider + what the sidebar/inspector showed; suspect width→height-coupled content in that pane, or a framework-level NavigationSplitView issue. (2026-06-03)
- 🗓️ M8·P6 dataset tie-in — flag→PNG export + image-shaped `AssetFingerprint`; genuinely future (not training yet). → [features/M8.md](./features/M8.md)
- 🗓️ Sidebar Image glyph pairing — the active Image section uses `photo.fill` (header) + `photo` (recents): fill-vs-outline of the *same* glyph, not two distinct glyphs like Playback's `play.rectangle.fill` + `film`. Pick a distinct image-file glyph for the recents if it reads too samey. (M9·P6·4)
- 🗓️ M9 Capture on-device smoke — the live-camera path is unverified on hardware (sim has no camera): initial detector from the shared selection on camera start + in-loop model swap (no session restart) + min-confidence via the overlay. The sidebar redesign + iPhone compact-nav were eyeballed on device during P6; this camera path wasn't. (M9·P4)
- 🗓️ Shared MRU generic — `RecentImages` and `RecentVideos` are near-identical bookmark-backed UserDefaults MRUs; factor a common base. Deliberate siblings for now (M8·P4). Also: `RecentImages` is untested (`Apps/Shared/` test-reachability deferral, as with `RecentVideos`), and macOS custom-model-in-image-mode re-selects the *playback* detector id (minor). (Touched by the unified-sidebar milestone, not closed by it; deferred from M9·P5.) (M8·P4, 2026-05-29)
- 🗓️ Per-class **config profiles** — saved settings bundles (per-class floors + visibility, maybe detector knobs too) so setups don't get re-typed each session. Spun off M10. (user, 2026-06-01) → [features/per-class-tuning.md](./features/per-class-tuning.md)
- 🗓️ Per-video (per-source) label scoping — a `sourceKey → seen-labels` layer **over** M12's per-detector store, if the corpus-vs-single-video distinction ever earns its UI (a "this video" vs "this detector" roster narrowing). `AssetFingerprint` (M7) was the planned key. Dropped from M12 at the 2026-06-04 pivot — sources made the panel unreadable. (user, 2026-06-04) → [features/label-accumulation.md](./features/label-accumulation.md)
- 🗓️ Dead `CaptureModel.start(minConfidence:)` param — `IrisShell+Capture.swift` passes a `minConfidence: () -> Float` closure into `CaptureModel.start` that's never read in the body (capture's floor flows via `CaptureDetailView`/`overlayFilter`). Drop it, or repurpose if capture ever pushes the filter into the detect loop. (noted M10·P2)
- 🗓️ Offline file-reader pre-pass — pre-computed detection tracks for smooth playback; an `AVAssetReader`-backed offline pass that decodes a file frame-by-frame, runs the detector over every frame, and caches the full `[Detection]` track.
      The natural shape for the Mac eval/curation target (the live pipeline stays best-effort + strobes on purpose). Opens when it lands: reuse `ResultStore` or a dedicated dense track? progress/cancel UI? sibling `Frame` source vs. pre-fill step. Likely M6/M7-adjacent. (user, 2026-05-25)
- 🗓️ Revisit bumped SwiftLint thresholds — `file_length`(→1000), `type_body_length`(→600), `nesting`(→2), `cyclomatic_complexity`(→15) were raised in block 8 to silence warnings during detector churn.
      Real length debt: `DetectionLayer.swift`(482), `VisionRectanglesDetector.swift`(734), `PlaybackSource.swift`(523) want splitting. Once churn settles, split the long files and ratchet thresholds back down. See `.swiftlint.yml` dated comment + [LOG.md](./LOG.md) block 8.
- 🗓️ RF-DETR Core ML spike — off the M6 critical path; direct PyTorch→Core ML via patched forks, FP32-only, needs a Swift `DETRSetPredictionDecoder` (path B, no NMS). → [`explorations/2026-05-25-coreml-model-conversion/RECOMMENDATIONS.md`](../explorations/2026-05-25-coreml-model-conversion/RECOMMENDATIONS.md)
- 🗓️ Path-B file-picking — file-picked models accept Path-A only; a Path-B picked model would need a label-supply UI + output-spec auto-detect to pick the right `OutputDecoder`. Bundled Path-B (yolo26n) ships fine (decoder + labels wired in code). (M6·P3) → [features/M6.md](./features/M6.md)
- 🗓️ Playback portrait `preferredTransform` — `PlaybackSource` stamps `Frame.orientation = .up` unconditionally; a portrait clip is delivered sideways but labeled upright, so Vision returns sideways-normalized coords. Fix upstream: derive `CGImagePropertyOrientation` + upright dims from `preferredTransform`. (M5·P6)
- 🗓️ Capture front-camera mirroring — the preview connection's `isVideoMirrored` is never set to `(position == .front)`; front-camera overlays will be unmirrored vs. the displayed selfie. Locked in `explorations/display-pipeline-architecture/RECOMMENDATIONS.md`, omitted in code. (M5·P6)
- 🗓️ DetectionInspector Swift 6 warning — pre-existing strict-concurrency warning in both demos: `displayTimeSource: { controller.currentTime }` (macOS `ContentView.swift:149`, iOS `:310`). Clears with `MainActor.assumeIsolated`. Minor. (M5·P6)
- 🗓️ M7 export follow-ups — deferred polish on the dataset export loop, behind the existing seams. A **delayed-after-launch** sweep; surface `export-status.json` in the demo footer (automatic-run visibility); MRU-cap-10 the unbounded "flagged sources" ledger; the real `DatasetExporter` (training-format conversion). → [features/M7.md](./features/M7.md)
